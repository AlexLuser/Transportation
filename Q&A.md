# 附：答辩 Q&A 参考手册（共 20 题）

> 以下问题覆盖技术原理、架构设计、工程实现、局限性等多个维度，建议熟记核心要点，答辩时用自己的语言表达，不必逐字背诵。

---

### 技术原理类

**Q1：为什么采用微服务架构而不是单体应用？微服务对你这个项目有什么实际收益？**

> 主要考虑两点：一是系统包含四类角色，各角色的业务边界清晰——顾客、商家、司机、管理员的功能几乎互不交叉，微服务按业务域拆分后各服务可以独立开发和部署，互不影响；二是通过实践 Spring Cloud 生态，也是本课题在技术深度方面的主动追求。当然，对于毕业设计规模的项目，微服务引入了额外的工程复杂度（服务间调用、注册中心、统一网关等），这一点在设计时已有充分考量，并不是"为了微服务而微服务"。

**Q2：A* 算法和 Dijkstra 算法有什么区别？为何选择 A*？**

> Dijkstra 是无启发式的最短路算法，每次扩展距离起点代价最小的节点，搜索方向不加约束，会向四面八方均匀扩展，在大规模路网图上要遍历大量节点。A* 在此基础上引入了启发函数 h(n)，用于估计当前节点到终点的代价，使搜索优先向终点方向推进，显著缩小搜索范围。路线规划场景中起终点均已知，以 Haversine 球面距离作为启发函数，满足可采纳性条件（球面直线距离不会高估实际道路距离），保证在找到最优路径的前提下，搜索效率明显优于 Dijkstra。

**Q3：Haversine 公式是什么？你的 A* 启发函数为什么用它而不是欧氏距离？**

> Haversine 公式计算的是地球球面上两点的大圆弧距离，公式为 `2R·arcsin(√(sin²(Δlat/2) + cos(lat1)·cos(lat2)·sin²(Δlon/2)))`，其中 R 为地球半径约 6371km。路线规划用的是真实经纬度坐标，在较大距离尺度下简单欧氏距离（直接用经纬度差值计算）会有较大误差，而 Haversine 能准确反映地球曲面上的实际大圆距离。同时它仍然满足可采纳性（球面直线不超过实际道路距离），所以选择它作为启发函数既准确又合法。

**Q4：Weighted A* 的 ε 值怎么选的？三个值（1.0、1.8、3.5）有什么考虑？**

> ε=1.0 是标准最优 A*，保证找到最短路径。ε=1.8 是中等膨胀，算法趋向终点的倾向更强，会走出稍有不同的路径。ε=3.5 是较大膨胀，路线与最优差异更明显。三个值是经过实验后选定的经验值——差异太小的话三条候选路线几乎一样，没有比较意义；差异太大可能导致路线质量太差。实际使用中，若距离差小于 1% 的候选路线会被去重，保证传给 LLM 的候选路线确实有差异。

**Q5：大语言模型生成的路点坐标出现偏差怎么处理？**

> 采用两层机制防范：第一层在 Prompt 中明确约束，要求路点必须位于上海市区真实道路附近，不得落在建筑内部或水域，绕路不超过直线距离的 50%；第二层在代码中对每个返回坐标做地理边界校验——纬度 30.6°~31.9°N，经度 120.8°~122.2°E，超出范围的路点直接过滤并打印警告，对应 `LlmWaypointRouteStrategy.isWithinShanghaiRegion()` 方法。即便有路点通过了边界校验，GraphHopper 的空间索引也会把任意坐标吸附到最近的路网节点，进一步消除细微偏差的影响。若 LLM 调用完全失败，自动降级为纯 A* 规划。

**Q6：speedRatio 指标的含义和计算方式是什么？低于 0.7 代表什么？**

> speedRatio = 当前小时历史平均车速 / 全天历史平均车速，数据来源为 `logistics_track` 表近 30 天的轨迹记录。例如全天均速为 35km/h，当前早高峰时段均速为 20km/h，则 speedRatio ≈ 0.57，低于阈值 0.7，表明当前时段车速比日常低 30% 以上，系统判定为明显拥堵，Prompt 中会提示 LLM 考虑绕行。这个阈值 0.7 是经验值，可以在配置中调整。

**Q7：LLM 的置信度 HIGH/MEDIUM/LOW 是怎么划分的？**

> 是基于历史数据样本量划分的：样本量超过 100 次对应 HIGH，10 到 100 次对应 MEDIUM，不足 10 次对应 LOW。样本量少时历史数据的参考价值有限，LLM 在 Prompt 中感知到这一点，置信度 LOW 时通常倾向于直达策略，避免基于不充分数据做出错误绕路决策。

---

### 架构与工程设计类

**Q8：API 网关的权限控制是怎么实现的？如果我伪造一个 Token 能绕过吗？**

> 网关的 JwtAuthFilter 会拦截所有非白名单请求，用密钥验证 Token 签名——如果 Token 被篡改，签名校验会直接失败，返回 401。只有用正确密钥签发的 Token 才能通过验证。通过验证后，网关从 Token Payload 中读取 `permission` 字段，与请求路径做角色匹配——例如 `/api/drivers/**` 只允许 driver 角色访问，其他角色即使有合法 Token 也会被返回 403。白名单只有登录接口，不需要 Token 即可访问。

**Q9：多个服务共用同一数据库，存在单点瓶颈和耦合问题吗？**

> 确实是当前架构的一个局限。理想的微服务数据库设计是每个服务有独立的数据库，通过消息队列和分布式事务维护跨服务一致性。当前方案是共用一个 MySQL 实例、按表划分业务边界，优先保证业务逻辑的完整性，避免在毕设阶段引入过多分布式复杂度。数据库层面的进一步拆分是后续可扩展的方向，也是真实生产环境中需要做的事。

**Q10：auth-service 和 user-service 的职责是如何划分的？**

> auth-service 专职负责身份认证，包括登录验证、JWT Token 签发和 Token 校验，是无状态服务。user-service 负责用户账号数据本身的增删改查，主要供管理员进行账号管理。两者职责分离：前者对外提供登录接口，后者管理账号记录，各自独立演进，不相互耦合。网关的 JwtAuthFilter 调用的是 auth-service 的 Token 校验逻辑。

**Q11：order-service 在发货时调用了几个下游服务？如果其中一个失败会怎样？**

> 发货时 order-service 通过 OpenFeign 调用两个下游服务：一是调用 logistics-service 生成物流路径，二是调用 shop-service 扣减仓库库存。目前的处理方式是：两个操作都成功才完成发货；如果其中一个失败，系统会调用回滚接口恢复库存，订单状态不会变更。这是一个简化的补偿机制，后续引入 RabbitMQ 后，发货操作改为事件驱动的异步模式，可以更好地处理这类分布式一致性问题。

**Q12：前端的路由守卫和权限控制是怎么做的？**

> 每个路由的 `meta` 元数据里声明所需角色（如 `requiresRole: 'driver'`），`beforeEach` 守卫在跳转前做检查：未登录则跳转到登录页；角色不匹配则跳转到该用户角色对应的首页。同时，Axios 请求拦截器在每次请求时自动从 Pinia store 读取 JWT Token，附加到 `Authorization: Bearer <token>` 请求头；响应拦截器统一处理 401 未授权——自动清空登录状态并跳转登录页，实现了前后端双层的权限控制。

**Q13：收货地址的 Geocoding 是怎么集成的？为什么不在规划路线时实时调用地图 API？**

> 顾客在前端填写收货地址文字后，customer-service 调用高德地图正向 Geocoding API，把地址字符串转成经纬度坐标，连同文字地址一起存到 `customer_address` 表的 `latitude` 和 `longitude` 字段。仓库地址同理，商家填地址时 shop-service 也会调高德 API 转坐标存入数据库。这样设计的好处是：路线规划时直接读数据库里的坐标，不需要实时调地图 API，避免了频繁调用导致的限流风险，也降低了规划链路的响应延迟。

---

### 代码与实现细节类

**Q14：GraphHopper 加载 OSM 数据大概要多久？演示时会影响启动吗？**

> 首次加载上海 `.pbf` 格式地图文件，解析建图大约需要几分钟。解析结果会缓存到项目根目录下的 `graph-cache` 文件夹，后续启动直接读取缓存，耗时约几秒。这个缓存目录已随项目代码一同提交，所以在当前演示机器上直接启动不需要重新解析，不会影响答辩演示。

**Q15：库存并发控制是怎么做的？**

> 库存扣减使用了原子 SQL 条件更新：`UPDATE ... SET stock = stock - quantity WHERE stock >= quantity`，在数据库层面保证不超卖，避免了应用层"读取-校验-写入"三步操作带来的并发窗口问题（TOCTOU 竞态条件）。目前系统尚未做压测，后续引入 Redis 缓存减少数据库压力、通过 RabbitMQ 异步解耦发货流程后，高并发场景下的表现将进一步改善。

**Q16：什么是商品快照？为什么需要这个设计？**

> 下单时，系统会将当前商品的名称、单价等关键信息复制一份写入 `order_item` 表，作为订单的商品快照。这样即便商家后续修改商品价格或将商品下架，历史订单中记录的商品信息也不会受到影响，保证了历史订单数据的完整性和准确性。这是电商系统中一个常见的设计模式。

**Q17：JWT Token 过期后如何处理？主动退出登录 Token 立刻失效吗？**

> Token 有效期为 2 小时。过期后前端收到 401 响应，Axios 响应拦截器会自动清除本地登录状态并跳转至登录页，用户重新登录后获取新 Token。主动退出登录后让 Token 立即失效（Token 黑名单）的功能目前尚未实现——JWT 是无状态的，服务端不存储 Token，因此现有实现中主动退出只是清除了前端的存储，Token 本身在剩余有效期内理论上仍可用。这个问题计划通过 Redis TTL 机制实现黑名单来解决，属于后续待完成的工作。

---

### 系统局限性与扩展类

---

---

## 附：技术名词详解手册

> 分为**已使用技术**和**计划引入技术**两部分。每项技术提供：定义、核心原理、与同类技术的对比（便于回答"为什么选它"），以及在本项目中的具体作用。

---

## 一、已使用技术

---

### 后端框架层

---

#### Spring Boot

**是什么：** Java 生态的应用框架，核心理念是"约定优于配置"——内置了 Tomcat 服务器、自动配置、依赖管理等，开发者不需要写大量 XML 配置文件，启动一个 `main` 方法就能跑起来一个 HTTP 服务。

**核心原理：** Spring Boot 的自动配置（`@EnableAutoConfiguration`）会扫描 classpath 下的依赖，根据存在哪些 jar 包自动完成对应的 Bean 注册。例如 classpath 里有 MyBatis 就自动配置数据源，有 Spring Web 就自动配置 DispatcherServlet。

**对比传统 Spring MVC：** 传统 Spring MVC 需要手动写 `web.xml`、`applicationContext.xml` 等多个配置文件，Spring Boot 把这些全部自动化，开发效率大幅提升。

**本项目用途：** 系统的 6 个业务服务（auth、customer、shop、order、driver、logistics）以及 gateway，每一个都是独立的 Spring Boot 应用，各自打成独立 jar 包，独立启动在不同端口。

---

#### Spring Cloud

**是什么：** 基于 Spring Boot 构建的微服务治理框架套件，提供了一整套解决微服务间协作问题的组件：服务注册发现、配置中心、负载均衡、断路器、API 网关等。

**核心原理：** Spring Cloud 本身是一套规范和接口集合（如 `DiscoveryClient` 接口），具体实现可以替换——Netflix 的 Eureka/Ribbon/Hystrix 是一套实现，阿里的 Nacos/Sentinel 是另一套。这使得底层组件可以按需替换而不改变业务代码。

**解决的核心问题：** 微服务部署后 IP 和端口可能动态变化，服务间不能硬编码地址。Spring Cloud 通过注册中心让服务"自报家门"、让调用方"按名查找"，解决了服务发现问题。

**本项目用途：** 提供微服务间调用（OpenFeign）、服务注册（通过 Spring Cloud Alibaba 集成 Nacos）、API 网关（Spring Cloud Gateway）的基础能力。

---

#### Spring Cloud Alibaba

**是什么：** 阿里巴巴开源的 Spring Cloud 扩展实现，把阿里系中间件（Nacos、Sentinel、RocketMQ、Seata）接入 Spring Cloud 生态。

**与 Netflix 套件的区别：** Netflix 的 Eureka 已停止维护，Ribbon 已进入维护模式；Spring Cloud Alibaba 的 Nacos 是目前社区更活跃、功能更完整的替代方案，同时具备服务注册和配置管理两项能力，不需要单独部署 Config Server。

**本项目用途：** 主要用于引入 Nacos 作为服务注册中心。在 `pom.xml` 中引入 `spring-cloud-starter-alibaba-nacos-discovery` 依赖后，服务启动时自动向 Nacos 注册，无需额外编码。

---

#### Nacos

**是什么：** 阿里开源的**服务注册与发现 + 配置管理**中间件，名字来源于"Naming and Configuration Service"。

**核心原理：** 服务启动时向 Nacos 注册自己的 IP、端口、服务名；Nacos 维护一张服务注册表。调用方通过服务名向 Nacos 查询可用实例列表，再发起调用。Nacos 还会定期向注册的服务发送心跳检测，若服务下线则自动从注册表中移除，调用方不会再路由到已下线的实例。

**对比 Eureka：** Eureka 纯粹是服务注册中心，不支持配置管理，且已停止维护。Nacos 同时承担注册中心和配置中心两个角色，并且支持配置热更新（修改配置后服务不需要重启即可生效）。

**本项目用途：** 系统所有服务启动时向运行在 8848 端口的 Nacos 注册，OpenFeign 调用时通过服务名（如 `logistics-service`）而非 IP 发起请求，Nacos 负责解析到具体实例。

---

#### OpenFeign

**是什么：** 声明式 HTTP 客户端。只需要定义一个 Java 接口，加上 `@FeignClient` 注解声明目标服务名，再用 `@GetMapping`/`@PostMapping` 等注解标注方法，Spring 会在运行时自动生成代理实现，调用该接口方法时实际发起 HTTP 请求。

**核心原理：** OpenFeign 在启动时扫描所有 `@FeignClient` 注解的接口，通过动态代理生成实现类。调用接口方法时，代理拦截方法调用，根据注解信息构建 HTTP 请求，通过负载均衡器（默认 Spring Cloud LoadBalancer）选择目标实例，再发起实际的 HTTP 请求，最后把响应反序列化为返回值类型。

**对比 RestTemplate：** RestTemplate 是命令式调用，需要手动拼 URL、处理参数序列化、反序列化；OpenFeign 把这些全部封装成接口方法调用，代码更简洁，更接近本地方法调用的体验。

**本项目用途：** order-service 调用 logistics-service 规划路线、调用 shop-service 扣减库存；customer-service 暴露 Feign 接口供 order-service 查询顾客收货地址坐标，这些都通过 OpenFeign 实现，调用方感觉像调本地方法。

---

#### MyBatis Plus

**是什么：** MyBatis 的增强版 ORM（对象关系映射）框架，在 MyBatis 基础上提供了通用 CRUD 方法、条件构造器、分页插件、代码生成器等功能。

**核心原理：** ORM 的本质是把 Java 对象和数据库表做映射——一个实体类对应一张表，类的字段对应表的列。MyBatis Plus 的 `BaseMapper<T>` 接口预置了 `insert`、`deleteById`、`updateById`、`selectById`、`selectList` 等方法，继承后无需写任何 SQL 即可完成基本数据操作。复杂查询通过 `LambdaQueryWrapper` 用链式调用构建条件，避免手写 SQL 字符串拼接。

**对比原生 MyBatis：** 原生 MyBatis 每个方法都需要在 XML 或注解中写 SQL，MyBatis Plus 的通用方法免去了大量重复 SQL，只有业务特定的复杂查询才需要自定义。

**本项目用途：** 所有服务的数据库读写均通过 MyBatis Plus 完成，包括订单 CRUD、商品查询、轨迹记录写入等。

---

#### MySQL

**是什么：** 关系型数据库管理系统（RDBMS），数据以表格形式存储，表与表之间通过外键建立关联，使用 SQL 语言查询和操作数据。

**关系型数据库的核心特性：** 支持 ACID 事务（原子性、一致性、隔离性、持久性），保证数据操作的可靠性；支持 JOIN 操作跨表查询；有成熟的索引机制（B+ 树索引）保证查询性能。

**对比 NoSQL（如 MongoDB）：** 关系型数据库适合结构固定、有关联关系的业务数据（如订单、商品、用户）；NoSQL 适合结构灵活、海量读写的场景（如日志、文档）。物流业务数据结构清晰、有关联关系，MySQL 更合适。

**本项目用途：** 单个 MySQL 实例下建 `transportation` 数据库，共 11 张核心表（用户、商品、订单、库存、物流路线、GPS 轨迹等），各服务按表划分业务边界，不跨库访问其他服务的表。

---

### 认证与安全层

---

#### JWT（JSON Web Token）

**是什么：** 一种开放标准（RFC 7519）的无状态身份令牌。格式为三段 Base64 字符串用 `.` 拼接：`Header.Payload.Signature`。Header 包含算法类型，Payload 包含用户信息（如 userId、角色），Signature 是用密钥对前两段做哈希运算的结果。

**无状态的含义：** 传统 Session 方案中，服务端需要存储每个登录用户的 Session 数据，验证时查 Session 表；JWT 方案中服务端不存储任何状态，收到 Token 后只需用密钥验证签名是否合法、检查是否过期即可完成认证，无需数据库查询，天然支持分布式部署。

**安全性：** Payload 中的数据只是 Base64 编码，并非加密，可以被解码查看。但 Signature 是用密钥做的 HMAC-SHA256 哈希，若 Payload 被篡改，签名就不再匹配，服务端验证会失败。所以 Token 不能存敏感数据（如密码），但存 userId、角色这类非敏感信息是安全的。

**本项目用途：** 用户登录成功后，auth-service 签发包含 `userId`、`username`、`permission`（角色）的 JWT Token，有效期 2 小时。前端每次请求在 Header 中携带 `Authorization: Bearer <token>`，网关的 JwtAuthFilter 验证签名和过期时间，提取角色信息做权限判断。

---

#### BCrypt

**是什么：** 专为密码存储设计的哈希算法。与 MD5/SHA 不同，BCrypt 有以下三个关键特性：①内置随机盐值，相同密码每次哈希结果不同；②计算代价可调（cost factor），可以让哈希运算故意变慢；③不可逆，无法从哈希值反推原密码。

**为什么"计算慢"是优点：** 普通哈希（MD5/SHA）计算极快，攻击者可以用 GPU 每秒尝试数十亿次暴力破解。BCrypt 的 cost factor 让每次哈希需要几十到几百毫秒，使暴力破解的时间成本增加到不可接受的程度。

**本项目用途：** 用户注册和管理员创建账号时，密码经 BCrypt 哈希后存入数据库，字段中存储的是形如 `$2a$10$...` 的哈希字符串。登录时，auth-service 用 `BCryptPasswordEncoder.matches()` 将输入密码与数据库哈希值对比，不需要存储或传输明文密码。

---

### 路线规划层

---

#### GraphHopper

**是什么：** 开源的路网计算引擎（Java 实现），专门用于加载 OpenStreetMap 数据、构建路网图，并在此基础上执行路径搜索、坐标空间索引等地理计算操作。

**核心能力：**
- **路网图构建：** 解析 `.pbf` 格式的 OSM 地图文件，将道路数据转化为图结构（节点=路口/端点，边=路段，边权=距离）。
- **空间索引（LocationIndex）：** 内部维护 R 树空间索引，支持 `findClosest(lat, lon)` 操作，给定任意经纬度坐标，毫秒级找到路网上最近的节点。
- **路网缓存：** 首次解析耗时较长（分钟级），解析结果序列化到 `graph-cache` 目录，后续启动直接反序列化加载（秒级）。

**本项目用途：** GraphHopper 提供路网图数据结构和空间索引能力，是 A* 算法执行路径搜索的底层支撑。系统自主实现 A* 算法，在 GraphHopper 的 `BaseGraph` 上遍历邻接边，而不是使用 GraphHopper 内置的路径规划引擎，这样可以完全控制搜索过程（如 Weighted A* 的 ε 参数）。

---

#### OpenStreetMap（OSM）

**是什么：** 由全球志愿者协作维护的开放地理数据库，类似"地图界的维基百科"。任何人都可以贡献和使用数据，数据以 `.osm`（XML）或 `.pbf`（二进制压缩）格式发布。

**数据内容：** OSM 数据包含道路（way）、路口（node）、建筑（relation）等地理要素，道路附带属性如道路等级（高速/主干道/小路）、限速、单行方向等。`.pbf` 是二进制压缩格式，上海地区数据文件约 200~300MB。

**对比商业地图（高德/百度/谷歌）：** 商业地图 API 通常有调用频次限制和费用，且不能在本地部署；OSM 数据完全开放，可以下载到本地离线使用，适合路网图的本地计算场景。

**本项目用途：** 下载上海市的 OSM `.pbf` 文件，通过 GraphHopper 解析构建路网图，A* 算法在此路网上搜索路径，保证规划出的路线沿真实道路行驶，而非空中直线。

---

#### A* 算法

**是什么：** 一种启发式图搜索算法，用于在带权图中找从起点到终点的最短路径。核心公式 `f(n) = g(n) + h(n)`：`g(n)` 是从起点到当前节点的已知实际代价，`h(n)` 是对当前节点到终点代价的估计（启发函数）。

**相比 Dijkstra 的优势：** Dijkstra 的 `h(n) = 0`，等价于没有方向感的盲目搜索，以起点为中心向四周扩展直到找到终点，会遍历大量不必要的节点。A* 的启发函数让算法优先探索"看起来离终点更近"的节点，将搜索范围限制在起终点之间的区域，大幅减少遍历节点数。

**可采纳性（Admissibility）：** 启发函数 `h(n)` 必须满足"永远不高估真实代价"这一条件，算法才能保证找到最优路径。本项目用 Haversine 球面距离作为 `h(n)`，球面直线距离必然 ≤ 实际道路距离，满足可采纳性。

**加权 A*（Weighted A*）：** 将公式改为 `f(n) = g(n) + ε × h(n)`，ε > 1 时启发函数被放大，算法更激进地朝终点方向搜索，搜索速度更快但不再保证最优，换来的是风格不同的备选路线，这是 LLM_JUDGE 策略生成多候选路线的基础。

**本项目实现：** 自主实现，用 `PriorityQueue`（最小堆）作为开放列表，`HashMap` 存储 `g` 值和 `cameFrom` 回溯记录，在 GraphHopper 的 `BaseGraph` 上通过 `EdgeExplorer` 遍历邻接边，搜索完成后通过 `cameFrom` 回溯还原路径并转为 GeoJSON LineString 返回。

---

#### DeepSeek API

**是什么：** 深度求索（DeepSeek）公司发布的大语言模型（LLM）的 HTTP API 接口，接口格式兼容 OpenAI API 规范。支持 `chat/completions` 接口（对话生成），可以通过设置 `response_format: {type: "json_object"}` 强制模型只输出 JSON，设置 `temperature` 控制输出的随机性。

**大语言模型（LLM）的核心能力：** LLM 是通过海量文本训练的神经网络模型，具有理解自然语言指令、综合多条信息做推理、按指定格式输出结果的能力。它不是数据库，输出的是基于训练数据和 Prompt 上下文的推理结果。

**幻觉问题：** LLM 有时会"一本正经地生成错误信息"，例如输出不存在的地址坐标。这是 LLM 的固有局限性，需要在工程上加以防范（本项目通过坐标边界校验 + 降级机制处理）。

**本项目用途：** 在 LLM_WAYPOINT 策略中，向 DeepSeek 发送包含历史交通数据的 Prompt，要求其以 JSON 格式返回是否需要绕路路点；在 LLM_JUDGE 策略中，把三条候选路线信息发给 DeepSeek，要求其裁判选出最优路线。调用时 `temperature=0.1` 保证输出稳定可复现，开启 `json_object` 模式保证格式合法。

---

#### GeoJSON

**是什么：** 地理空间数据的标准 JSON 格式（RFC 7946）。核心数据类型包括：`Point`（点，一个经纬度坐标）、`LineString`（折线，多个坐标点连成的线段序列）、`Polygon`（多边形），以及包含属性数据的 `Feature`。

**LineString 的结构：**
```json
{
  "type": "LineString",
  "coordinates": [[经度1, 纬度1], [经度2, 纬度2], ...]
}
```
注意坐标顺序是 `[经度, 纬度]`（即 `[lon, lat]`），与常见的 `(纬度, 经度)` 表示相反，这是 GeoJSON 规范的要求。

**本项目用途：** A* 搜索完成后，将路径节点序列转为 GeoJSON LineString 字符串，存入数据库 `logistics_route` 表的 `route_geojson` 字段。前端 Leaflet 地图库的 `L.geoJSON()` 方法可以直接解析并在地图上渲染折线，无需任何格式转换。

---

### 前端层

---

#### Vue 3

**是什么：** 渐进式 JavaScript 前端框架，核心特性是**响应式数据绑定**——数据变化时 UI 自动更新，无需手动操作 DOM。Vue 3 引入了组合式 API（Composition API），用 `setup()` 函数或 `<script setup>` 语法组织逻辑，比 Vue 2 的选项式 API 更灵活，逻辑复用更方便。

**对比 React：** React 是"JavaScript 中写 HTML"（JSX），Vue 是更接近传统"HTML 文件"的单文件组件（`.vue`），模板语法更直观，学习曲线相对更平缓。两者都是主流选择，本项目选 Vue 3 是因为配合 Element Plus 组件库快速搭建管理后台有成熟的生态支持。

**单页应用（SPA）：** 浏览器只加载一次 HTML，后续页面切换通过 JavaScript 动态替换内容，不触发整页刷新。所有路由跳转都在前端处理，服务端只提供数据接口（API），不返回 HTML 页面。

**本项目用途：** 四个角色端（管理员、顾客、商家、司机）共用一个 Vue 3 SPA，通过路由守卫根据登录角色展示对应的页面和菜单，共计 25 个子页面。

---

#### Vite

**是什么：** 新一代前端构建工具，开发模式下基于浏览器原生 ES Module 按需加载模块，不需要预先打包整个项目，启动速度比 Webpack 快数倍至数十倍。生产模式下使用 Rollup 打包，输出高度优化的静态文件。

**对比 Webpack：** Webpack 在启动时需要把所有模块打包成 bundle，项目越大启动越慢；Vite 开发时直接让浏览器原生加载 ES Module，启动时间与项目大小基本无关，热更新（HMR）也极快。

**本项目用途：** 前端项目的构建工具，`npm run dev` 命令启动 Vite 开发服务器，前端运行在 5173 端口。

---

#### TypeScript

**是什么：** JavaScript 的超集，在 JS 基础上增加了静态类型系统。类型信息只在编译时存在，编译为 JS 后运行时与普通 JS 无异。

**解决的核心问题：** JavaScript 是动态类型语言，变量类型在运行时才确定，代码写错了在运行时才报错。TypeScript 在编写代码时就能检查类型错误，IDE 也能提供准确的自动补全和参数提示，减少"属性不存在"、"参数类型错误"等低级 bug。

**本项目用途：** 前端代码全部用 TypeScript 编写，例如 `api/logistics.ts` 中定义了接口请求和响应的类型，确保前后端数据格式对齐，在调用 API 时编译器能检查参数类型是否正确。

---

#### Pinia

**是什么：** Vue 3 官方推荐的状态管理库（Vuex 的现代替代品）。通过定义 Store 来管理跨组件共享的状态，Store 中的状态变化会自动触发使用该状态的所有组件更新。

**对比 Vuex：** Vuex 需要定义 state、mutations、actions、getters 四个概念，样板代码较多；Pinia 简化为 state（数据）+ actions（方法），没有 mutation 概念，配合 TypeScript 类型推导更好，代码量减少约 30%。

**本项目用途：** `userStore` 存储当前登录用户的 `userId`、`username`、`role`（角色）和 `token`（JWT）。通过 `localStorage` 持久化，刷新页面后从 `localStorage` 恢复，登录状态不丢失。路由守卫和 Axios 拦截器都从 `userStore` 读取 token。

---

#### Vue Router

**是什么：** Vue 3 的官方路由管理库。前端路由指的是根据 URL 路径决定渲染哪个组件，不触发页面刷新。Vue Router 支持路由守卫（导航守卫），可以在页面跳转前执行权限检查逻辑。

**路由守卫：** `router.beforeEach()` 注册全局前置守卫，每次路由跳转前执行回调，回调中可以读取目标路由的 `meta` 元数据（如所需角色），与当前登录用户的角色做比对，决定是否放行或重定向。

**本项目用途：** 每个路由配置中声明 `meta.requiresAuth: true` 和 `meta.role: 'customer'|'shop'|'driver'|'admin'`，全局守卫检查未登录则跳转登录页，角色不匹配则跳转到该角色的首页，实现前端的访问控制。

---

#### Axios

**是什么：** 基于 Promise 的 HTTP 客户端库，可在浏览器和 Node.js 中运行。相比浏览器原生的 `fetch` API，Axios 提供了更完善的拦截器机制、请求取消、自动序列化/反序列化 JSON、更好的错误处理等能力。

**拦截器机制：** Axios 允许注册请求拦截器（在请求发出前执行）和响应拦截器（在收到响应后执行）。请求拦截器可以统一添加 Header，响应拦截器可以统一处理错误码。

**本项目用途：** 封装了统一的 Axios 实例，请求拦截器自动从 Pinia userStore 读取 JWT Token 并注入 `Authorization: Bearer <token>` 请求头，所有 API 调用无需手动处理认证；响应拦截器统一捕获 401 响应，自动清空登录状态并跳转登录页。

---

#### Leaflet

**是什么：** 轻量级开源 JavaScript 地图渲染库（压缩后约 40KB），支持加载多种底图瓦片（如 OpenStreetMap 瓦片），在此基础上叠加标记（Marker）、折线（Polyline）、GeoJSON 图层等可视化元素。

**对比高德/百度 JS SDK：** 商业地图 SDK 功能更丰富但有调用限制和费用，且与特定地图服务绑定；Leaflet 是纯客户端渲染库，底图瓦片可以替换为任意 TMS 服务，本项目使用免费的 OpenStreetMap 瓦片服务。

**本项目用途：** 订单详情弹窗和司机导航页的地图组件基于 Leaflet 实现，在地图上叠加：绿色圆点（发货仓库）、红色圆点（收货地址）、蓝紫色折线（系统规划路线，从数据库读取 GeoJSON 直接用 `L.geoJSON()` 渲染）、橙色折线（司机实际行驶轨迹）、蓝色圆点（司机当前位置）。

---

#### 高德地图 Geocoding API

**是什么：** 高德地图开放平台提供的地理编码 API。**正向地理编码（Geocoding）** 是将文字地址（如"上海市黄浦区南京东路100号"）转换为经纬度坐标；**逆向地理编码（Reverse Geocoding）** 是将经纬度转换为文字地址描述。

**调用方式：** 通过 HTTPS GET 请求调用，传入 API Key 和地址字符串，返回 JSON 格式的坐标结果：`{"location": "121.4737, 31.2304", ...}`。

**本项目用途：** 顾客在前端输入收货地址文字后，customer-service 调用高德正向 Geocoding API 将地址转为经纬度坐标，存入 `customer_address` 表的 `longitude`、`latitude` 字段。仓库地址同理。路线规划时直接从数据库读取坐标，不在规划链路中实时调用地图 API，避免增加延迟和调用次数限制。

---

#### SpringDoc OpenAPI

**是什么：** 自动扫描 Spring MVC/WebFlux 接口，根据代码中的注解和类型信息，自动生成符合 OpenAPI 3.0 规范的接口文档，并提供内置的 Swagger UI 页面可视化展示接口列表、参数说明、可直接调用测试。

**本项目用途：** 各服务添加依赖后，访问 `/swagger-ui.html` 即可查看该服务的所有接口文档，方便前后端联调时核对接口参数格式，不需要手写接口文档。

---

## 二、计划引入技术

---

#### RabbitMQ

**是什么：** 基于 AMQP（Advanced Message Queuing Protocol）协议的开源消息队列中间件。消息队列的核心模式是**生产者-消费者**：生产者把消息发送到队列，消费者从队列中异步取出并处理，生产者不需要等待消费者处理完成。

**核心概念：**
- **Exchange（交换机）：** 接收生产者发来的消息，根据路由规则转发到对应队列；
- **Queue（队列）：** 存储消息，消费者从队列消费；
- **Binding（绑定）：** 定义 Exchange 和 Queue 之间的路由关系；
- **持久化：** 消息和队列可以配置持久化，即使 RabbitMQ 重启消息也不丢失。

**解决的核心问题：**
1. **解耦：** 发货时 order-service 只需发布一条消息，不需要直接调用 logistics-service，两服务互不依赖，物流服务改造或重启不影响订单服务；
2. **异步：** 发货接口立即返回成功，路线规划在后台异步执行，用户不需要等待规划完成（目前同步调用 LLM 需要 1~3 秒）；
3. **削峰：** 如果瞬间有大量订单发货，消息队列起到缓冲作用，logistics-service 按自身处理能力逐个消费，不会被瞬间峰值压垮。

**本项目计划用途：** 将发货时的 `order-service → logistics-service` 同步 Feign 调用改造为：order-service 向 `order.shipped` 队列发布消息，logistics-service 订阅该队列，异步消费后完成路线规划并更新数据库。

---

#### Redis

**是什么：** 基于内存的键值型数据库，支持 String、Hash、List、Set、Sorted Set 等多种数据结构，所有读写操作在内存中完成，读写速度比 MySQL 快约 100~1000 倍。数据可以持久化到磁盘（RDB/AOF），防止重启丢失。

**TTL（Time To Live）机制：** Redis 的每个 Key 可以设置过期时间，到期后自动删除，这是实现缓存过期和 Token 黑名单的基础。

**本项目计划用途一——缓存：** 商品列表查询是读多写少的场景，目前每次都查 MySQL。引入 Redis 后：第一次查询从 MySQL 读取，结果存入 Redis（设置合理的 TTL，如 10 分钟）；后续请求先查 Redis，命中则直接返回，不查 MySQL；商家修改商品时主动删除 Redis 中对应的缓存 Key，保证数据一致性。

**本项目计划用途二——Token 黑名单：** JWT 是无状态的，服务端无法主动使 Token 失效。用 Redis 存储已退出登录的 Token 标识（以 Token 的 `jti` 字段或哈希值为 Key），TTL 设为 Token 的剩余有效时长。网关每次校验 Token 时额外检查是否在 Redis 黑名单中，若在则拒绝，实现主动退出登录后 Token 立即失效。

---

#### Docker

**是什么：** 容器化平台，将应用程序及其所有依赖（运行时环境、配置文件、系统库等）打包成一个标准化的**镜像（Image）**，在任意支持 Docker 的机器上可以从镜像启动一个**容器（Container）**，容器内的环境与打包时完全一致，消除"在我机器上能跑"的问题。

**镜像 vs 容器：** 镜像是静态的模板（类似 Java 的 Class），容器是镜像的运行实例（类似对象），同一镜像可以启动多个容器实例。

**对比虚拟机：** 虚拟机需要模拟完整的硬件和操作系统，体积大（GB 级），启动慢（分钟级）；Docker 容器共享宿主机内核，只隔离进程和文件系统，体积小（MB 级），启动快（秒级）。

**本项目计划用途：** 为每个微服务（auth、customer、shop、order、driver、logistics、gateway）分别编写 `Dockerfile`，定义构建步骤：从 JDK 17 基础镜像出发，复制编译好的 jar 包，设置启动命令。前端用 `nginx:alpine` 镜像托管编译后的静态文件，配置 Nginx 将 `/api` 请求反向代理到网关。

---

#### Kubernetes（K8s）

**是什么：** 开源的容器编排平台，自动化管理大量容器的部署、伸缩、负载均衡、自愈（容器崩溃自动重启）、滚动升级等。用 YAML 声明式描述"我想要什么状态"，K8s 负责调度容器达到并维持该状态。

**核心概念：**
- **Pod：** K8s 调度的最小单元，通常包含一个容器；
- **Deployment：** 声明某服务需要运行几个 Pod 副本，负责维持副本数，副本崩溃自动重启；
- **Service：** 给一组 Pod 提供稳定的访问入口（固定的域名/IP），即使 Pod IP 变化也不影响调用方；
- **Ingress：** HTTP 层的路由规则，将外部流量按域名/路径分发到不同 Service。

**本项目计划用途：** 为每个微服务创建 Deployment YAML（声明需要 1 个 Pod 副本，指定使用哪个 Docker 镜像），以及 Service YAML（在集群内提供稳定的服务名访问）。Nacos、MySQL 也以 Pod 形式部署在集群内。最终通过 `kubectl apply -f k8s/` 一键部署整套系统，答辩时无需手动逐个启动服务。

---

#### Nginx

**是什么：** 高性能的开源 HTTP 服务器和反向代理服务器，单机可以支持数万并发连接，常用于静态文件托管、反向代理、负载均衡和 HTTPS 终止。

**反向代理：** 客户端把请求发给 Nginx，Nginx 再转发到后端服务器，客户端不直接访问后端。与正向代理（代理客户端）相反，反向代理代理的是服务端。

**本项目计划用途：** `npm run build` 编译前端 Vue 项目为静态 HTML/CSS/JS 文件，由 Nginx 容器托管这些静态文件（访问根路径返回 `index.html`）。同时配置反向代理：所有以 `/api/` 开头的请求转发到后端网关（8090 端口），前端代码中不需要区分"静态文件服务器地址"和"后端 API 地址"，统一访问 Nginx 的地址即可。

**Q18：你的系统用的历史数据是在哪里生成的？初始数据怎么来的？系统刚上线时历史数据是空的怎么办？**

> 历史轨迹数据来源于 `logistics_track` 表——司机配送途中前端定时上报 GPS 坐标，这些坐标记录构成历史数据。`test-data` 目录下准备了完整的测试数据集，包括模拟的历史轨迹记录，所以演示时 LLM 可以读取到有效的历史数据。系统刚上线时历史数据确实为空，这是一个冷启动问题。当历史样本不足（< 10 次）时，LLM 的置信度会被标记为 LOW，Prompt 中的判断规则会倾向于直达策略，不会基于不充分数据做绕路决策。这是系统对冷启动场景的一个合理降级处理。

**Q19：LLM 每次规划路线都要调 API，延迟大吗？如果 DeepSeek API 挂了会怎样？**

> 实测 DeepSeek API 的响应时间大约在 1~3 秒，这个延迟目前是同步阻塞的，会影响发货确认的响应时间。这也是后续计划引入 RabbitMQ 的原因之一——改为异步后，发货接口立即返回，路线规划在后台异步完成，用户体验会更好。如果 DeepSeek API 调用失败（网络超时等），代码中有 try-catch 捕获异常，自动降级为纯 A* 直达规划，`LlmWaypointRouteStrategy` 中第二步的 catch 块就是处理这个情况的。所以路线一定能生成，不会因为 LLM 不可用而导致系统报错。

**Q20：系统目前没有容器化，如果要在另一台机器上运行，需要做什么准备？**

> 主要需要准备以下几项：一是安装 JDK 17、Node.js、Maven；二是安装并启动 MySQL，导入 `database/all.sql` 建库建表；三是安装并启动 Nacos；四是上海路网 `graph-cache` 缓存目录已随代码提交，如果 OSM `.pbf` 文件也在，可以直接用缓存跳过解析步骤；五是配置各服务的 `application.yml` 中的数据库连接信息和 DeepSeek API Key；六是分别启动各服务，前端执行 `npm install && npm run dev`。后续容器化完成后，这些步骤都会被 Docker Compose 或 Kubernetes 的 YAML 配置文件自动化，实现一键部署。

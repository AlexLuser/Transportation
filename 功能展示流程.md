【第 13 页 · 章节过渡页】

下面按完整业务流程进行现场演示，依次展示各角色的功能界面与实际运行效果。

（切换到系统页面，按以下流程进行实机演示）

---

**【演示第 1 步：登录与基于角色的路由跳转（约 30 秒）】**

（打开登录页）

这是系统的统一登录入口，四类角色共用同一个页面。输入不同角色的账号登录后，前端路由守卫会读取 JWT Token 中携带的 `permission` 字段，自动跳转到该角色对应的首页——管理员登录后进入数据概览 Dashboard，顾客登录后进入商城首页。这里实现的是基于角色的访问控制，不同角色的可见菜单与可访问接口完全隔离。

（演示：分别以管理员账号和顾客账号登录，观察跳转目标）

---

**【演示第 2 步：管理员端——数据概览与商品审核（约 30 秒）】**

（切换至管理员端）

管理员首页以卡片形式展示系统整体数据，包括商品总数、订单总数、仓库数量，以及各功能模块的快速入口。

商品审核页面列出了所有商家提交的待审核商品记录，管理员审核通过后，该商品才会正式出现在顾客端商城中；驳回则退回给商家修改。这是系统对商品上架的管控机制。

---

**【演示第 3 步：顾客端——商城浏览与下单（约 1 分钟）】**

（切换至顾客账号）

顾客端商城页面按店铺展示商品信息，包括商品名称、价格和库存。选中商品后进入下单页面，选择收货地址时有一处值得说明的设计：顾客在添加新收货地址时，填入文字地址后，customer-service 会调用**高德地图正向 Geocoding API** 将地址字符串转换为经纬度坐标并一并存入数据库——这是后续路线规划精确定位配送终点的数据基础，避免了规划时临时调接口的限流风险。

提交订单后，系统完成模拟支付，订单状态变为"待发货"。

（演示：浏览商城 → 选商品下单 → 查看订单列表，确认状态显示"待发货"）

---

**【演示第 4 步：商家端——发货确认与路线规划触发（约 2 分钟）】**

（切换至商家账号）

这一步是整个系统的核心业务环节，路线规划在此处被触发。

商家在订单管理页面找到当前"待发货"的订单，点击"发货"按钮，弹出发货对话框，选择发货仓库后确认——

（演示：点击"发货"，选择仓库，点击确认）

此时后台同步完成以下五项操作：

**第一**，订单状态更新为"待揽件"；

**第二**，order-service 通过 OpenFeign 调用 logistics-service，物流服务以仓库经纬度为起点、顾客收货地址坐标为终点，调用路线规划引擎。规划引擎基于 A* 算法在 GraphHopper 加载的上海路网上搜索最优路径，并通过 DeepSeek 大语言模型结合历史交通数据判断是否需要绕行拥堵区域；

**第三**，规划结果以 GeoJSON LineString 格式持久化至数据库；

**第四**，扣减所选仓库的库存；

**第五**，创建配送任务，等待司机接单。

（演示：刷新订单列表，确认订单状态已变为"待揽件"）

---

**【演示第 5 步：顾客端——查看地图规划路线（约 1.5 分钟）】**

（切换回顾客账号）

顾客在订单列表中点击"查看详情"，弹出订单详情弹窗，弹窗内嵌了地图组件。

（演示：打开订单详情弹窗）

地图上已显示出一条**蓝紫色路线**——这是系统为本次配送规划的路线，基于 A* 算法在上海真实路网上计算得出，路线沿实际道路延伸，并非直线连接。绿色圆点标记发货仓库，红色圆点标记收货地址，右下角图例说明各颜色对应含义。

该路线数据以 GeoJSON LineString 格式存储，前端 Leaflet 地图库可直接解析并渲染，无需额外转换。

---

**【演示第 6 步：司机端——接单与导航路线查看（约 1 分钟）】**

（切换至司机账号）

司机在"待接单"列表中可以看到当前这条配送任务，记录中显示收货人、配送地址和创建时间。点击"接单"确认后，该任务转入"我的配送"列表，状态变为"配送中"。

切换至"配送路线"页面，选择已接的订单后，地图上同样显示系统规划好的配送路线，司机可据此导航行驶。

（演示：完成接单 → 切换至导航路线页面 → 查看地图路线）

---

**【演示第 7 步：实时位置上报与轨迹追踪（约 1.5 分钟）】**

（切换至司机端，模拟 GPS 上报）

配送途中，前端定时调用 GPS 上报接口，将当前坐标发送至 logistics-service 的 `/api/logistics/track/location` 接口，坐标写入 `logistics_track` 表，同时同步更新当前位置字段。

（演示：调用上报接口写入测试坐标，然后切换至顾客端刷新订单详情）

切换至顾客端刷新订单详情后，地图上出现**橙色实线**——即司机已实际行驶的轨迹，随 GPS 上报点逐步延伸；蓝色圆点实时标记司机当前位置。至此，顾客端的实时货物位置追踪功能得以完整展示。

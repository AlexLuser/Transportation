# 基于微服务架构的智能物流管理系统 —— 中期答辩讲稿

> 对应 PPT 页码在讲稿中用 【第X页】 标注，括号内为舞台提示，不需要读出来  
> 总时长目标：约 20 分钟  
> 各部分参考时长见章节标题

---

## 开场（约 1 分钟）

【第 1 页 · 封面】

各位老师好，我叫付苗，指导老师是牛志华老师。我的毕业设计题目是《基于微服务架构的智能物流管理系统设计与实现》，今天进行中期答辩汇报，汇报时长大约 20 分钟，请各位老师多批评指正。

（切换到目录页）

【第 2 页 · 目录】

我先简单说一下今天汇报的整体结构，一共七个部分：选题背景与意义、需求分析、系统设计、功能实现、项目亮点、功能展示，以及后续计划。

---

## 第一部分：选题背景和意义（约 2 分钟）

【第 3 页 · 选题背景】

（本页为章节过渡页，展示时直接开口讲）

先说一下为什么要做这个项目。

当前国内电商物流规模非常庞大，每天产生的订单量极高。传统的单体式系统在高并发场景下很容易出现瓶颈，而且某个功能一旦出问题，可能导致整个系统不可用，维护成本也很高。另外，现有的物流路线规划大多还是依靠人工经验，效率比较低；司机和顾客之间信息不透明，顾客完全不知道快递实时在哪里。

最近这两年大模型技术发展得非常快，我就想能不能把 AI 引入到路线规划里，让系统自动结合历史交通数据做出更智能的调度决策。同时用微服务架构将各业务模块解耦，提升系统的可维护性和扩展性。这就是这个项目的出发点。

简单对比一下传统物流系统和这个系统的区别：传统系统通常是单体架构，扩展性差，路线靠人工经验安排，各角色信息割裂，没有实时轨迹可查。而这个系统采用微服务架构，引入了 A* 算法加大模型做智能路线规划，四类角色在统一平台上协作，并有 GPS 实时轨迹上报和地图可视化。

---

## 第二部分：需求分析（约 2 分钟）

【第 4 页 · 部分标题】

（切换到角色信息页）

【第 5 页 · 需求分析 - 角色信息】

这个系统面向四类用户。

第一类是**管理员**，主要负责系统层面的监控与管理，包括监控系统整体运行情况、管理所有用户账号、监控全部订单状态，以及管理仓库信息。

第二类是**顾客**，可以浏览商城商品、加入购物车、下单支付、查看自己的订单进度，还能在地图上查看配送路径，以及管理自己的收货地址。

第三类是**商家**，管理自己的店铺信息和商品、管理仓库与库存，收到订单后确认发货并选择具体发货仓库。店铺营收数据展示作为后续开发功能。

第四类是**司机**，负责接收配送任务，系统会提前规划好路线，司机可在 App 上查看导航路线，同时定时上报自己的 GPS 位置，还可以管理名下绑定的车辆信息。

【第 6 页 · 需求分析 - 业务流程】

这是整个系统的核心业务流程。

顾客下单之后，系统生成订单，状态为"待支付"或"待发货"。由于当前模拟支付，下单时会自动完成支付流程。商家收到订单后确认发货，选择发货仓库；**系统在此时自动调用路线规划引擎**，根据仓库坐标和顾客收货地址坐标生成最优配送路线，订单状态变为"待揽件"。然后通知司机接单，司机沿规划路线配送，实时上报 GPS 轨迹，订单状态进入"派送中"，顾客端可以在地图上实时追踪位置。最终司机完成配送确认送达，订单状态变为"已完成"。

---

## 第三部分：系统设计（约 3 分钟）

【第 7 页 · 部分标题】

（切换到整体架构图）

【第 8 页 · 系统设计 - 整体架构】

这是系统的整体架构图。

最上层是前端，用 Vue 3 开发，分四个端——管理端、顾客端、商家端、司机端，统一运行在 5173 端口。

前端所有请求都发到 **API 网关**，网关负责JWT 鉴权验证身份、以及路由转发到对应服务。验证通过后，网关把用户身份信息注入请求头，再转发给下游服务。

后端一共有 6 个业务微服务，各自独立运行，所有服务统一注册到 **Nacos**，服务间通过 **OpenFeign** 做 HTTP 同步调用。数据存储用同一个 MySQL 实例下的 `transportation` 库，按表划分业务边界。目前服务间通信使用 OpenFeign，后续计划扩展为 RabbitMQ，并组合 Redis 做系统缓存。

【第 9 页 · 系统设计 - 技术选型】

技术栈方面，我挑几个重点说一下。

后端用 **Spring Boot** 作为各服务的基础框架，**Spring Cloud** 负责微服务治理，**Spring Cloud Alibaba** 集成 Nacos。 对象关系映射使用 MyBatis Plus，服务间调用用 OpenFeign，接口文档由 SpringDoc OpenAPI 自动生成，JWT 令牌用 JJWT 库实现。路线规划部分用到了 **GraphHopper**，这是一个专门做路网计算的开源框架，用来加载 OpenStreetMap 数据来进行路径搜索。

前端用 **Vue 3 + Vite + TypeScript** 构建，UI 组件库是 Element Plus，地图用 Leaflet，状态管理用 Pinia，请求处理用 Axios，路由管理用 Vue Router。

大模型调用方面，用的是 **DeepSeek API**，通过 HTTP 调用

---

## 第四部分：功能实现（约 4 分钟）

【第 11 页 · 部分标题】

（切换到功能实现详细页）

【第 12 页 · 认证与权限】

认证这块，用户登录调用 auth-service（8082 端口），密码用 BCrypt 哈希存储，登录成功后签发 JWT Token，有效期 2 小时。Token 里携带 `userId`、`username` 和 `permission` 权限信息。

网关的 JwtAuthFilter 会拦截所有进来的请求，先验证 Token 有效性，再读取 Token 里的 `permission` 字段，与请求路径要求的角色做匹配——比如 `/api/drivers/**` 开头的接口只有 driver 角色才能访问，其他角色会被拦截返回 403。白名单只有登录接口，不需要 Token 就能访问。验证通过后，网关把用户信息注入请求头，下游各服务直接从请求头里读取，不需要每个服务都重复解析 Token。

【第 13、14、15 页 · 顾客服务】

顾客服务（8083 端口）有三块功能：顾客信息管理、收货地址管理，以及供下游服务调用的内部 Feign 接口。

收货地址管理是一个比较有意思的设计：顾客在前端输入文字地址之后，服务会调用**高德地图正向 Geocoding API** 把地址字符串转换成经纬度坐标，再连同文字地址一并存到 `customer_address` 表里。这样后续路线规划可以直接取到精确坐标，不需要在规划时再临时调地图 API，也避免了频繁调用导致的限流风险。

另外顾客服务还暴露了内部 Feign 接口，供 order-service 在创建订单时查询收货地址的坐标，作为路线规划的终点输入。

【第 16、17、18 页 · 商铺服务】

商铺服务（8084 端口）分商品管理、库存管理、订单操作三块。

商品管理这块有一个审核流程——商户提交上架请求后，需要管理员审核通过才能正式上架，避免商品信息不规范直接到顾客端。

库存管理：商户执行入库操作，在订单发货时选择仓库后自动完成出库。扣库存用的是原子 SQL——`WHERE stock >= quantity`，通过数据库层面保证不超卖，而不是在应用层做读-校验-写的三步操作。

订单操作这块（其实属于 order-service）：商铺用户发货时自动触发，修改订单状态并创建配送记录，同时触发 logistics-service 生成物流路径。

【第 19 页 · 订单服务】

订单服务（8085 端口）是整个后端的**编排中枢**，协调多个服务之间的调用。

订单创建后默认状态是"待支付/待发货"，当前模拟支付所以下单时直接进入待发货。商家发货时，order-service 通过 Feign 同时调用两个下游服务：调用 logistics-service 生成物流路径，调用 shop-service 扣减库存，两个操作都成功才完成发货。如果其中一个失败，提供了回滚接口来恢复库存。

另外有一个**商品快照**设计值得一提——下单时把商品名称、价格等关键信息存一份到 `order_item` 表，这样商家后续改价或者下架商品，历史订单的展示不会受到任何影响。

【第 20、21、22 页 · 司机服务】

司机服务（8086 端口）分三块：司机与车辆管理、运输任务管理、实时位置上报。

司机信息维护和车辆绑定是基础功能，一名司机可以绑定多辆车，选择当前出行使用的车辆。

运输任务管理：司机端可以看到待配送的订单列表，接单后锁定任务；配送完成后调用 order-service 接口将订单改为"已完成"状态。

实时位置上报目前在 logistics-service 中实现——司机端前端每隔一定间隔调用上报接口，把当前 GPS 坐标传过来，存入 `logistics_track` 表，顾客端就可以实时看到司机位置，这些轨迹数据同时也是后续历史分析的原始数据来源。

【第 23、24、25 页 · 路径规划服务】

路径规划服务（8087 端口）有两大核心功能：存储物流信息和规划物流路径。

存储这块：在创建物流订单时执行路径规划并将计划路径持久化到数据库；同时存储司机实时上报的位置信息，形成历史轨迹数据，用于前端展示和数据分析。

规划这块：使用 **GraphHopper** 读取并缓存 OpenStreetMap 路网信息；结合 **DeepSeek API** 和自主实现的 A* 算法提供智能路径规划；通过配置文件的 `routing.strategy` 参数可以切换三种规划策略。这块是整个项目最核心的技术亮点，我下一部分会详细介绍。

【第 26、27、28 页 · 前端实现】

前端用 Vue 3 加 TypeScript 开发，是一个单页面应用。

状态管理用 **Pinia**，`userStore` 存储登录用户信息和 JWT Token。通过 `localStorage` 持久化，刷新页面后登录状态不会丢失。

**Vue Router 路由守卫**：每个路由的 `meta` 元数据里声明所需角色，`beforeEach` 守卫在跳转前做检查——如果未登录则跳转到登录页，如果角色不匹配则跳转到该用户角色对应的首页，实现了基于角色的访问控制。

**Axios 请求拦截**：封装了统一的请求拦截器，每次发出请求时自动从 Pinia store 读取 Token，附加到 `Authorization: Bearer <token>` 请求头，所有接口调用不需要单独处理认证。响应拦截器统一处理 401 未授权——自动清空登录状态并跳转登录页。

---

## 第五部分：项目亮点 —— 智能路线规划（约 6 分钟）

【第 29 页 · 部分标题】

接下来重点介绍这个项目最有技术含量的部分——智能路线规划引擎。整个规划模块我做了分层设计：最底层是 GraphHopper 提供路网数据支撑，中间层是自主实现的 A* 算法做路径搜索，最上层是大模型结合历史数据做智能决策。

（切换到亮点内容第一页）

【第 30 页 · 整体背景与坐标映射】

**GraphHopper + OpenStreetMap**

路网数据来源于 OpenStreetMap，我下载了上海地区的 `.pbf` 格式地图文件，用 GraphHopper 加载解析后构建路网图，缓存到 `graph-cache` 目录。第一次解析需要几分钟，后续启动直接加载缓存，秒级完成初始化。路网图里每个节点代表一个路口或路段端点，每条边代表一段道路，边上带有实际距离属性。

**坐标映射到路网节点**

在运行 A* 之前，有一个关键的预处理步骤：把用户输入的经纬度坐标（来自数据库里存的仓库坐标和顾客地址坐标）映射到路网上最近的节点。这一步通过 GraphHopper 的 `LocationIndex.findClosest()` 实现——它会在路网空间索引里找到距离给定坐标最近的有效路网节点。这样就能保证 A* 的起终点一定落在真实的道路上，而不是某个建筑内部或者空地上。

【第 31 页 · A* 算法】

**核心公式：f(n) = g(n) + h(n)**

`g(n)` 是从起点沿路网到达当前节点 n 的**实际代价**，由已走过的路网边距离累加而来；`h(n)` 是当前节点到终点的**启发式估计**，我用的是 **Haversine 球面距离公式**，也就是地球表面两点的大圆弧距离。

Haversine 的关键性质在于它满足**可采纳性**——球面直线距离永远不会高估实际的道路行驶距离，因为任何真实道路都不可能比两点间的直线更短。这个性质保证了 A* 一定能找到最短路径，而不是找到一条次优路径就停下来。

算法实现上，用一个按 f 值排序的优先队列（最小堆）作为开放列表，每次取 f 值最小的节点扩展它的所有邻接边，如果经过这条边到达邻居节点的 g 值比已知值更小就更新并加入队列。找到终点后，通过预先记录的 `cameFrom` 回溯表从终点往回追溯，还原出完整节点序列，再转换成 GeoJSON LineString 格式返回。

**Weighted A*（加权 A*）**

在标准 A* 基础上引入启发函数权重 ε，公式变为 `f(n) = g(n) + ε × h(n)`。

ε = 1.0 时是标准最优 A*；ε > 1.0 时，启发函数被"膨胀"，算法更激进地趋向终点，搜索方向改变，从而探索出与最优路线风格不同的备选路径。我分别用 ε = 1.0、1.8、3.5 生成三条风格各异的候选路线，这是 LLM_JUDGE 策略的基础。

**三种可切换策略**

基于以上基础，实现了三种路线策略：`A_STAR`（纯算法）、`LLM_JUDGE`（多候选 + LLM 裁判）、`LLM_WAYPOINT`（LLM 决策路点 + A* 执行），通过配置文件 `routing.strategy` 一键切换，当前默认使用 `LLM_WAYPOINT`。

【第 32 页 · LLM_WAYPOINT 策略】

LLM_WAYPOINT 是当前默认策略，核心思想是：**让 LLM 做战略决策，让 A* 做战术执行**。LLM 决定"该往哪个方向走、要不要绕路"，A* 保证每一段都走真实道路。

整个流程五步完成：

**第一步，查询历史数据。** 从 `logistics_track` 表读取近 30 天的历史轨迹，统计当前小时的平均行驶速度与全天均速，计算出 **speedRatio（速度比值）**。同时在起终点中点附近约 10 公里范围内，聚合出近 7 天均速低于 8km/h 的**慢速热点区域**列表，还有近 7 天的异常路线统计。

**第二步，构建 Prompt 调用 DeepSeek。** 系统提示词将 LLM 定位为"物流路线策略专家"，用户提示词注入起终点坐标、直线距离、当前时间时段（早高峰/晚高峰/平峰），以及所有历史数据。判断规则明确写在 Prompt 里：如果 speedRatio < 0.7（当前比平时慢 30% 以上）且存在慢速热点，建议插入绕路路点；否则建议直达。API 调用时温度设为 0.1 保证输出稳定，开启 `json_object` 模式强制只输出 JSON。LLM 返回结构包含策略描述、0 到 2 个路点坐标、预估时速、置信度级别，以及给用户看的一句话摘要。

**第三步，坐标合法性校验。** LLM 有时会产生"幻觉"，返回超出上海范围的坐标。系统对每个路点做边界校验（纬度 30.6°~31.9°N，经度 120.8°~122.2°E），超出范围的路点直接过滤并打印警告，不会影响后续规划流程。

**第四步，分段 A* 规划。** 过滤后的路点构成 [起点 → 路点1 → 路点2 → 终点] 分段序列，对每相邻两点分别调用 A*，得到各段在真实路网上的 GeoJSON 坐标序列。

**第五步，路段拼接。** 各段坐标按顺序合并（相邻段衔接点只保留一次避免重复坐标），累加各段距离和时间。如果 LLM 提供了预估时速，用它修正最终的预计到达时间，比固定按 40km/h 估算更贴近实际。

**降级机制**：如果 LLM 调用失败（网络超时等），或过滤后没有有效路点，系统自动降级为纯 A* 直达规划，保证路线一定能生成，用户不会看到报错。

【第 33 页 · LLM_JUDGE 策略与策略对比】

**LLM_JUDGE 策略：多候选路线 + LLM 裁判**

这个策略思路不同：先用算法生成多条候选，再让 LLM 结合历史数据做评估选最优的。

第一步，用三种 ε 值的 Weighted A*（ε=1.0/1.8/3.5）生成三条候选路线，对距离差小于 1% 的路线做去重，只保留距离有明显差异的候选。

第二步，把所有候选路线的距离、A* 基准时间、路网节点数，连同历史交通数据一起组成 Prompt，让 DeepSeek 做裁判。LLM 的决策优先级在 Prompt 里明确定义：**准时率（历史延误越少越好）> 路线稳定性（节点数越少越好）> 总距离**。

第三步，LLM 返回选中的候选编号，以及用当前时段历史均速重新计算的 `adjustedDurationMs`，比固定时速估算更准确。

**两种策略对比**

LLM_WAYPOINT 中 LLM 参与的是决策层，决定"走哪个方向"，A* 负责在真实路网上执行每段规划——不过即便做了坐标过滤，LLM 给出的路点仍可能在一定程度上影响路线整体质量，这是这种策略需要权衡的地方。

LLM_JUDGE 中 LLM 参与的是评估层，所有候选路线都已经是合法的路网路线，LLM 只做综合评估选最优的，不会引入无效坐标的问题。

当前系统默认执行 LLM_WAYPOINT 模式，可通过配置文件一键切换。

---

## 第六部分：功能展示（约 4 分钟）

【第 34 页 · 功能展示】

（本部分以录屏演示为主）

下面通过录屏来展示系统的实际功能效果。演示顺序是：登录 → 顾客端商城浏览与下单 → 商家端发货 → 查看地图规划路线 → 司机接单 → 实时轨迹追踪。

---

## 第七部分：后续计划（约 2 分钟）

【第 35 页 · 部分标题】

（切换到后续计划内容页）

【第 36 页 · 后续计划】

后续计划主要有三项。

第一项是**容器化部署**。计划为每个服务编写 Dockerfile，通过 Kubernetes 完成容器编排与服务管理。前端使用 Nginx 容器托管，配置反向代理到后端网关。这样整个系统可以通过 Kubernetes 统一部署和管理，具备良好的扩展性与可迁移性，也方便最终答辩演示时一键启动整套环境。

第二项是**引入 RabbitMQ 消息队列**。目前服务间都是 OpenFeign 同步调用，一旦某个服务响应慢或者挂掉，整个调用链会阻塞等待。引入 MQ 之后，比如发货这个操作，order-service 只需要发布一条"订单发货"消息，logistics-service 订阅到之后异步去生成路线，两个服务完全解耦，系统整体吞吐量也会有明显提升。

第三项是**加入 Redis 缓存**。商品列表这类数据读多写少，目前每次请求都直接查数据库，压力比较大。加了 Redis 之后，第一次查完放进缓存，后续直接从缓存返回，能大幅降低数据库压力。另外 Token 黑名单——用户主动退出登录后让 Token 立即失效——也计划用 Redis 的 TTL 机制来实现，目前这个功能还没有做。

---

## 总结与结束（约 1 分钟）

【第 37 页 · 谢谢观看】

总结一下，这个项目的核心亮点有三个：

第一，**微服务架构**，6 个业务服务通过 Nacos 注册、OpenFeign 协作，职责清晰，可独立部署；

第二，**智能路线规划**，自主实现了 A* 算法与 Weighted A*，并设计了两种大模型结合策略——LLM_WAYPOINT 让 LLM 基于历史数据做战略决策、A* 负责路网执行；LLM_JUDGE 用加权 A* 生成多候选、LLM 做综合裁判，历史交通数据贯穿两种策略，让决策更贴近实际通行规律；

第三，**多角色全场景**，管理员、顾客、商家、司机四端全部覆盖，从下单到配送完成的完整业务链路可以跑通，配合 Leaflet 地图对规划路线和实时轨迹做可视化。

整体来看，系统的每个技术选型都是为了解决具体的业务问题，不是为了用技术而用技术。

以上就是我的中期答辩汇报，感谢各位老师的聆听，请老师们多批评指正，谢谢！

---

## 附：可能被问到的问题及参考回答

**Q1：为什么用微服务而不是单体应用？**

> 主要考虑到系统有四种角色，业务边界比较清晰，微服务按业务拆分后各服务可以独立开发、独立部署，各端互不影响。当然对于毕设规模来说微服务确实增加了不少复杂度，但这也是学习目标的一部分，让我对 Spring Cloud 生态有了更深入的实践。

**Q2：A* 和 Dijkstra 的区别？你为什么选 A*？**

> Dijkstra 是无启发式的最短路算法，每次扩展距离起点最近的节点，会向四面八方均匀扩展，在大图上要遍历非常多的节点。A* 加了一个启发函数 h(n) 来估计当前节点到终点的距离，优先探索更有希望的方向，大幅减少了需要扩展的节点数，效率更高。在路线规划这个场景里，起终点都是明确已知的，Haversine 球面距离是一个完美的启发函数——满足可采纳性，保证找到最优路径。

**Q3：LLM 幻觉坐标怎么处理的？**

> 这是开发过程中遇到的真实问题。处理方式是两层保障：第一层在 Prompt 里明确约束，要求路点必须是上海市区真实道路附近的坐标，不得落在建筑内部或水域，绕路不得超过直线距离的 50%；第二层在代码里做上海地理边界校验（纬度 30.6°~31.9°N，经度 120.8°~122.2°E），超出范围直接过滤掉。如果过滤后没有有效路点，或者 LLM 调用完全失败，自动降级为纯 A* 规划，保证一定有路线可用。

**Q4：speedRatio 这个指标是怎么算的，低于 0.7 代表什么？**

> speedRatio = 当前小时历史均速 / 全天历史均速。比如全天均速是 35km/h，当前早高峰小时均速是 20km/h，那 speedRatio = 20/35 ≈ 0.57，低于 0.7，说明当前比平时慢了 30% 以上，系统会判断为明显拥堵，建议 LLM 考虑绕路。这个阈值是可以调整的，当前定在 0.7 是经过测试后的经验值。

**Q5：LLM 的置信度 HIGH/MEDIUM/LOW 是怎么定的？**

> 是基于历史数据样本量定的：样本量超过 100 次对应 HIGH，10 到 100 次对应 MEDIUM，不足 10 次对应 LOW。样本量少的时候历史数据参考价值有限，LLM 也会在决策里体现这一点，置信度 LOW 时通常倾向于直达策略。

**Q6：高德 API 的 Geocoding 怎么用的？**

> 顾客在前端填写收货地址文字之后，customer-service 调用高德地图正向地理编码 API，把地址字符串转成经纬度坐标，然后连同文字地址一起存到 `customer_address` 表的 `latitude` 和 `longitude` 字段。仓库信息也是同理，商家填地址时同样通过 shop-service 调高德 API 转坐标存下来。这样后续路线规划直接读数据库里的坐标，不需要实时调地图 API。

**Q7：多个服务连同一个数据库，这不是单点瓶颈吗？**

> 确实是目前架构的一个不足。理想情况下每个服务应该有独立数据库，通过 MQ 和分布式事务保证一致性。考虑到毕设规模，暂时共用一个库按表划分业务边界，先保证业务逻辑跑通。后续有余力可以拆分数据库。

**Q8：GraphHopper 加载 OSM 数据大概要多久？**

> 第一次加载上海的 .pbf 文件，解析和建图大约需要几分钟。解析结果缓存在项目根目录的 `graph-cache` 文件夹里，后续启动直接读缓存，几秒钟就好了。这个缓存目录已经随项目一起提交，所以在我这台机器上直接启动不需要重新解析。

**Q9：JWT Token 过期了怎么处理？**

> Token 有效期是 2 小时，过期后前端收到 401 响应，Axios 响应拦截器会自动清空登录状态并跳转到登录页，重新登录获取新 Token。主动退出让 Token 立即失效（Token 黑名单）计划用 Redis 实现，存储已失效 Token 的标识符，TTL 设为 Token 的剩余有效时长，这是后续要做的工作。

**Q10：并发控制做了哪些？**

> 库存扣减用了原子 SQL（`WHERE stock >= quantity`），在数据库层面保证不超卖。整体还没有做系统压测，后续引入 Redis 缓存减少数据库压力、引入 RabbitMQ 异步解耦后，高并发场景的表现会有明显改善。

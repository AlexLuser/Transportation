# 基于微服务架构的智能物流管理系统 —— 中期答辩讲稿 v1.2

> **使用说明**  
> - 【第X页】= 对应PPT页码；（括号内）= 舞台提示，不读出来  
> - 总时长目标：约 20 分钟  
> - 功能演示部分以实机操作为主，讲解文字供参考

---

## 开场（约 30 秒）

【第 1 页 · 封面】

各位老师好，我叫付苗，指导老师是牛志华老师。我的毕业设计题目是《基于微服务架构的智能物流管理系统设计与实现》，今天进行中期答辩汇报，汇报时间大约 20 分钟，请老师们多批评指正。

---

## 目录（约 15 秒）

【第 2 页 · 目录】

今天的汇报共七个部分：选题背景和意义、系统功能、系统架构和技术方案、当前进度、功能展示、项目亮点，以及后续计划。

---

## Part One：选题背景和意义（约 2 分钟）

【第 3 页 · 章节过渡页】

（直接开口讲）

【第 4 页 · 选题背景】

先说一下为什么要做这个项目。

本课题的目标是完成一套**基于微服务的智能物流管理系统**，系统覆盖顾客下单、商家发货、司机配送、顾客收货的完整业务流程，并在配送路线规划环节引入 AI，使系统能够结合多源数据自动生成更合理的配送路线。

选这个方向，主要有两个背景：

**第一，微服务架构的必要性。** 传统单体系统各模块高度耦合，"牵一发而动全身"，扩展和维护代价很高。微服务架构允许各模块独立开发、测试与部署，各服务互不影响，在业务边界清晰的物流场景下非常适用。

**第二，大模型技术的引入机会。** 近两年大模型在数据理解、决策推理、多模态信息融合方面展现出显著优势。通过引入大模型对历史路况数据进行分析，可以辅助完成更智能的路线调度决策，而不是依靠固定算法或人工经验。

这就是这个项目的出发点。

---

## Part Two：系统功能（约 2 分钟）

【第 5 页 · 章节过渡页】

【第 6 页 · 四角色功能】

系统面向四类用户。

**管理员**：检测系统整体运行情况——包括商品总数、订单总数、仓库数量等数据概览；管理所有用户账号；监控全部订单状态；管理仓库信息。

**顾客**：浏览各商家商品、加入购物车；下单、完成模拟支付、查看自己的订单进度；在地图上查看配送路径和司机实时位置；管理收货地址。

**商家**：管理店铺信息与商品，需提交上架申请等管理员审核通过后方可显示在顾客端；管理仓库与库存；处理订单发货——这一步是整个系统路线规划被触发的关键节点。

**司机**：接收配送任务；查看系统规划好的路线；配送途中实时上报 GPS 位置；管理名下绑定的车辆信息。

---

## Part Three：系统架构和技术方案（约 3 分钟）

【第 7 页 · 章节过渡页】

【第 8 页 · 架构设计】

这是系统的整体架构图。

最上层是前端，用 Vue 3 开发，四个角色——管理端、顾客端、商家端、司机端——统一运行在 5173 端口，是一个单页应用。

前端所有请求统一发往 **API 网关**（8090 端口），网关承担两项核心职责：一是 JWT 鉴权——验证 Token 合法性，并根据 Token 中携带的角色信息与请求路径做权限比对；二是路由转发——鉴权通过后，把请求转发到对应的下游服务，同时把用户身份信息注入请求头。

后端共 6 个业务微服务，各自独立运行：认证服务（8082）、顾客服务（8083）、商铺服务（8084）、订单服务（8085）、货运服务（8086）、物流服务（8087）。所有服务统一注册到 **Nacos** 注册中心，服务间通过 **OpenFeign** 发起 HTTP 同步调用。数据存储使用同一个 MySQL 实例下的 `transportation` 库，各服务按表划分业务边界。

目前服务间通信使用 OpenFeign，后续计划扩展为 RabbitMQ 异步解耦，并组合 Redis 做缓存层。

【第 9 页 · 技术选型】

技术栈方面，挑几个重点说：

后端用 **Spring Boot** 作为各服务基础框架，**Spring Cloud** 负责微服务治理，**Spring Cloud Alibaba** 集成 Nacos。ORM 使用 MyBatis Plus，服务间调用用 OpenFeign，JWT 令牌由 JJWT 库实现。

路线规划部分用到了 **GraphHopper**——这是一个专门做路网计算的开源框架，用来加载 OpenStreetMap 上海地区的 `.pbf` 格式地图数据，解析后构建路网图，后续 A* 算法在此路网上执行搜索。大模型调用使用 **DeepSeek API**，通过 HTTP 调用。

前端用 **Vue 3 + Vite + TypeScript** 构建，UI 组件库是 Element Plus，地图渲染使用 Leaflet，状态管理用 Pinia，路由管理用 Vue Router。

---

## Part Four：当前进度（约 2 分钟）

【第 10 页 · 章节过渡页】

【第 11 页 · 整体进度】

下面汇报当前的开发进度。

（结合 PPT 进度表说明）

**后端全部 8 个模块的主干功能已全部完成**，涵盖 API 网关、认证服务、用户管理、顾客服务、商铺服务、订单服务、货运服务、物流服务。各模块均已完成接口联调。

**前端 25 个 Vue 页面已全部实现**，四个角色的核心功能页面均已覆盖，包括管理端、顾客端商城、商家端订单管理、司机端接单与导航。

**数据库共 11 张核心表**，并准备了完整的测试数据集。

**尚未完成的部分**：容器化部署（Docker + Kubernetes）、RabbitMQ 消息队列、Redis 缓存层——这三项属于基础设施层面，不影响当前核心业务功能。

【第 12 页 · 细节业务进度】

细节业务层面，以下几项属于后续待完成：认证服务的用户自助注册功能；管理员审核商铺、司机注册信息；订单评价与更完善的状态变更管理；物流服务增加历史轨迹之外的更多信息源供大模型决策参考（如天气、实时路况接口）；以及对应的前端页面开发。



---

## Part Six：项目亮点——智能路线规划（约 4 分钟）

【第 14 页 · 章节过渡页】

下面介绍一下本项目的亮点——结合大模型的智能路径规划模块。整个规划模块我做了分层设计：最底层是 GraphHopper 提供路网数据支撑，中间层是自主实现的 A* 算法做路径搜索，最上层是大模型结合历史数据做智能决策。

---

【第 15 页 · 项目亮点——整体背景】

**GraphHopper + OpenStreetMap**

路网数据来源于 OpenStreetMap，我下载了上海地区的 `.pbf` 格式地图文件，用 GraphHopper 加载解析后构建路网图，缓存到 `graph-cache` 目录。路网图里每个节点代表一个路口或路段端点，每条边代表一段道路，边上带有实际距离属性。首次解析需要几分钟，后续启动直接读缓存，秒级完成初始化。

**坐标映射到路网节点**

在运行 A* 之前，有一个关键的预处理步骤：把数据库里存储的经纬度坐标（仓库坐标和顾客地址坐标）映射到路网上最近的节点。这一步通过 GraphHopper 的 `LocationIndex.findClosest()` 实现——它会在路网空间索引里找到距离给定坐标最近的有效路网节点，确保 A* 的起终点一定落在真实道路上，而不是建筑内部或空地。

---

【第 16 页 · 项目亮点——A* 算法】

**核心公式：f(n) = g(n) + h(n)**

`g(n)` 是从起点沿路网到达当前节点 n 的实际代价，由已走过的路网边距离累加而来；`h(n)` 是当前节点到终点的启发式估计，我用的是 **Haversine 球面距离公式**，即地球表面两点的大圆弧距离。

Haversine 满足**可采纳性**——球面直线距离永远不会高估实际路网行驶距离，因为道路不可能比两点直线更短。这个性质保证了 A* 一定能找到最短路径。

算法实现上，用一个按 f 值排序的优先队列作为开放列表，每次取 f 值最小的节点扩展邻接边。找到终点后，通过预先记录的 `cameFrom` 回溯表从终点往回追溯，还原完整节点序列，再转换为 GeoJSON LineString 格式返回。

**加权 A*（Weighted A*）**

在标准 A* 基础上引入启发函数权重 ε，公式变为 `f(n) = g(n) + ε × h(n)`。ε = 1.0 时是标准最优 A*；ε > 1.0 时，启发函数被"膨胀"，算法更激进地趋向终点，从而探索出与最优路线风格不同的备选路径。我用 ε = 1.0、1.8、3.5 生成三条风格各异的候选路线，这是 LLM_JUDGE 策略的基础。

代码中对应的是 `AStarRouteStrategy.planWithEpsilon()` 方法，LLM_JUDGE 策略调用此方法生成三条候选路线。

**三种可切换策略**

整个规划模块以策略模式封装：`RouteStrategy` 接口统一约束，三个实现类——`AStarRouteStrategy`、`LlmWaypointRouteStrategy`、`LlmJudgeRouteStrategy`——分别对应三种策略，通过配置文件 `routing.strategy` 参数一键切换，业务层代码完全不感知策略的切换，符合开闭原则。

---

【第 17 页 · 项目亮点——LLM_WAYPOINT 策略】

LLM_WAYPOINT 是当前系统**默认策略**，核心思想是：**让 LLM 做战略决策，让 A* 做战术执行**。LLM 决定"该往哪个方向走、要不要绕路"，A* 保证每一段都走在真实路网上。

整个流程分五步：

**第一步，查询历史数据。** 从 `logistics_track` 表读取近 30 天的历史轨迹，统计当前小时的历史平均行驶速度与全天均速，计算 **speedRatio（速度比值）**；在起终点中点附近约 10 公里范围内，聚合近 7 天均速低于 8km/h 的**慢速热点区域**列表，以及近 7 天的异常路线统计。

**第二步，构建 Prompt 调用 DeepSeek。** 系统提示词将 LLM 定位为"物流路线策略专家"，用户提示词注入起终点坐标、直线距离、当前时间时段，以及所有历史数据。判断规则写在 Prompt 里：`speedRatio < 0.7`（当前比平时慢 30% 以上）且存在慢速热点 → 建议插入绕路路点（1-2 个）；否则 → 直达策略。API 调用时温度设为 0.1 保证输出稳定，开启 `json_object` 模式强制输出 JSON。

**第三步，坐标合法性校验。** LLM 有时会产生幻觉路点，系统在代码中对每个路点做上海地理边界校验——纬度 30.6°~31.9°N，经度 120.8°~122.2°E，超出范围直接过滤并打印警告日志，对应代码在 `LlmWaypointRouteStrategy.isWithinShanghaiRegion()` 方法中。

**第四步，分段 A* 规划。** 过滤后的路点构成 `[起点 → 路点1 → 路点2 → 终点]` 分段序列，对每相邻两点分别调用 `AStarRouteStrategy.planWithEpsilon()` 执行路网路径搜索。

**第五步，路段拼接。** 各段坐标按顺序合并，相邻段衔接点只保留一次，避免重复坐标。各段距离和时间累加；若 LLM 提供了预估时速，用它修正最终的预计到达时间，比固定按 40km/h 估算更贴近实际。

**降级机制：** 如果 LLM 调用失败，或过滤后没有有效路点，系统自动降级为纯 A* 直达规划，保证路线一定能生成，用户不会看到报错。

---

【第 18 页 · 项目亮点——LLM_JUDGE 策略与策略对比】

**LLM_JUDGE 策略：多候选路线 + LLM 裁判**

这个策略思路不同：先用算法生成多条候选路线，再让 LLM 结合历史数据做综合评估选最优的。

第一步，用三种 ε 值的 Weighted A*（ε=1.0 / 1.8 / 3.5）生成三条候选路线，对距离差小于 1% 的路线做去重，只保留有明显差异的候选。

第二步，把所有候选路线的距离、A* 基准时间、路网节点数，连同历史交通数据组成 Prompt，让 DeepSeek 做裁判。LLM 的决策优先级在 Prompt 里明确定义：**准时率（历史延误越少越好）> 路线稳定性（节点数越少越好）> 总距离**。

第三步，LLM 返回选中的候选编号，以及用当前时段历史均速重新计算的 `adjustedDurationMs`，比固定时速估算更准确。

**两种策略对比**

LLM_WAYPOINT 中 LLM 参与的是决策层，决定"走哪个方向"，A* 在真实路网上执行每段规划。优点是可以主动绕开历史拥堵区域；不足是即使经过坐标过滤，LLM 给出的路点仍可能在一定程度上影响整体路线质量。

LLM_JUDGE 中 LLM 参与的是评估层，所有候选路线都已经是合法的路网路线，LLM 只做综合评估选最优的，不会引入无效坐标的问题。

当前系统默认使用 LLM_WAYPOINT 模式，可通过配置文件一键切换。

---

## Part Seven：后续计划（约 2 分钟）

【第 19 页 · 章节过渡页】

【第 20 页 · 后续计划】

后续待完成的工作主要有三项：

**第一，容器化部署。** 为每个服务编写 Dockerfile，通过 Kubernetes 进行容器编排与服务管理。前端使用 Nginx 容器托管，配置反向代理到后端网关。完成后整套系统可通过 Kubernetes 统一部署，便于最终答辩时一键搭建演示环境。

**第二，引入 RabbitMQ 消息队列。** 目前 order-service 在发货时通过 OpenFeign 同步调用 logistics-service 规划路线，若物流服务响应延迟，整条调用链路将被阻塞。引入消息队列后，order-service 发布"订单发货"事件，logistics-service 异步消费并完成路线规划，两服务完全解耦，系统吞吐量也会相应提升。

**第三，加入 Redis 缓存。** 商品列表属于读多写少的数据，引入 Redis 缓存可降低数据库压力；此外，计划用 Redis 的 TTL 机制实现 Token 黑名单——用户主动退出登录后即时使 Token 失效，完善当前认证流程中的这一不足。

---

## 总结与结束（约 30 秒）

【第 21 页 · 谢谢观看】

以上就是我的中期答辩汇报，感谢各位老师的聆听，请老师们多批评指正，谢谢！


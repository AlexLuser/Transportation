# 基于微服务架构的智能物流管理系统 —— 中期答辩讲稿 v2.0

> 对应 PPT 页码在讲稿中用 【第X页】 标注，括号内为舞台提示，不需要读出来  
> 总时长目标：约 20 分钟  
> 各部分参考时长见章节标题

---

## 开场（约 1 分钟）

【第 1 页 · 封面】

各位老师好，我叫付苗，指导老师是牛志华老师。我的毕业设计题目是《基于微服务架构的智能物流管理系统设计与实现》，今天进行中期答辩汇报，汇报时长大约 20 分钟，请各位老师多批评指正。

（切换到目录页）

【第 2 页 · 目录】

今天汇报共七个部分：选题背景与意义、需求分析、系统设计、功能实现、**当前完成进度与成果展示**、项目亮点，以及后续计划。

---

## 第一部分：选题背景和意义（约 1.5 分钟）

【第 3 页 · 选题背景】

先说一下为什么要做这个项目。

当前国内电商物流规模非常庞大，传统单体系统在高并发场景下容易出现瓶颈，某个功能出问题可能导致整个系统不可用。现有的物流路线规划大多依靠人工经验，效率较低；司机和顾客之间信息不透明，顾客不知道快递实时在哪里。

这两年大模型技术发展非常快，我就想能不能把 AI 引入到路线规划里，让系统自动结合历史交通数据做出更智能的调度决策，同时用微服务架构将各业务模块解耦，提升系统的可维护性和扩展性。

简单对比：传统物流系统是单体架构，扩展性差，路线靠人工经验，各角色信息割裂，没有实时轨迹。而本系统采用微服务架构，引入 A* 算法加大模型做智能路线规划，四类角色在统一平台协作，并有 GPS 实时轨迹上报和地图可视化。

---

## 第二部分：需求分析（约 1.5 分钟）

【第 4 页 · 部分标题】

（切换到角色信息页）

【第 5 页 · 需求分析 - 角色信息】

系统面向四类用户。

**管理员**：监控系统整体运行情况、管理所有用户账号、监控全部订单状态，以及管理仓库信息。

**顾客**：浏览商城商品、加入购物车、下单支付、查看订单进度，在地图上查看配送路径，以及管理收货地址。

**商家**：管理店铺信息和商品、管理仓库与库存，收到订单后确认发货并选择发货仓库。

**司机**：接收配送任务，在系统规划好的路线上配送，定时上报 GPS 位置，管理名下绑定的车辆信息。

【第 6 页 · 需求分析 - 业务流程】

这是系统的核心业务流程。

顾客下单后，系统生成订单并自动完成模拟支付。商家确认发货，选择仓库；**系统在此时自动调用路线规划引擎**，根据仓库坐标和顾客收货地址坐标生成最优配送路线，订单状态变为"待揽件"。通知司机接单，司机沿规划路线配送，实时上报 GPS 轨迹，顾客端可在地图实时追踪。最终司机完成配送确认送达，订单状态变为"已完成"。

---

## 第三部分：系统设计（约 3 分钟）

【第 7 页 · 部分标题】

（切换到整体架构图）

【第 8 页 · 系统设计 - 整体架构】

这是系统的整体架构图。

最上层是前端，用 Vue 3 开发，分四个端——管理端、顾客端、商家端、司机端，统一运行在 5173 端口。

前端所有请求都发到 **API 网关**（8081 端口），网关负责三件事：JWT 鉴权验证身份、路由转发到对应服务、统一处理跨域 CORS。验证通过后，网关把用户身份信息注入请求头，再转发给下游服务。

后端共有 **7 个业务微服务**，各自独立运行：认证服务 Auth（8082）、用户服务 User（负责管理员账号管理）、顾客服务 Customer（8083）、商铺服务 Shop（8084）、订单服务 Order（8085）、货运服务 Driver（8086）、物流服务 Logistics（8087）。另有公共模块 Common 提供统一的返回结构、工具类等。

所有服务统一注册到 **Nacos**（8848 端口），服务间通过 **OpenFeign** 做 HTTP 同步调用。数据存储用同一个 MySQL 实例下的 `transportation` 库，按表划分业务边界。后续计划扩展为 RabbitMQ 异步通信，并加入 Redis 做系统缓存。

【第 9 页 · 系统设计 - 技术选型】

技术栈方面，挑几个重点说一下。

后端用 **Spring Boot 3.1.5** 作为各服务基础框架，**Spring Cloud 2022.0.4** 负责微服务治理，**Spring Cloud Alibaba** 集成 Nacos，Java 版本为 17。ORM 使用 MyBatis Plus，服务间调用用 OpenFeign，接口文档由 SpringDoc OpenAPI 自动生成，JWT 令牌用 JJWT 库实现。路线规划用到了 **GraphHopper 9.x**，加载 OpenStreetMap 数据进行路径搜索。

前端用 **Vue 3 + Vite + TypeScript** 构建，UI 组件库是 Element Plus，地图用 Leaflet，状态管理用 Pinia，请求处理用 Axios，路由管理用 Vue Router。

大模型调用方面，用的是 **DeepSeek API**，通过 HTTP 直接调用，兼容 OpenAI Chat Completions 协议格式，温度参数设为 0.1 保证输出稳定，开启 `json_object` 模式强制输出 JSON。

【第 10 页 · 系统设计 - 数据库设计】

数据库设计了 11 张核心数据表，按业务模块分组。

几个有设计意图的表：`customer_address` 顾客地址表里，通过高德地图 Geocoding API 将文字地址转换并存储了经纬度坐标，这是配送路线的**终点数据来源**；`warehouse_info` 仓库表同样存了坐标，是路线的**起点数据来源**；`logistics_route` 物流路线表里的 `planned_route` 字段存储 GeoJSON 格式的路线数据，可以直接被前端 Leaflet 地图渲染；`logistics_track` 实时轨迹表记录司机的 GPS 点序列，同时作为历史交通数据供路线规划分析。

这样从仓库出发到顾客地址的完整数据链路有据可查，路线规划所需的所有地理信息都预先存储好了。

---

## 第四部分：功能实现（约 3 分钟）

【第 11 页 · 部分标题】

（切换到功能实现详细页）

【第 12 页 · 认证与权限】

认证这块，用户登录调用 auth-service，密码用 BCrypt 哈希存储，登录成功后签发 JWT Token，有效期 2 小时。Token 里携带 `userId`、`username` 和 `permission` 权限信息。

网关的 JwtAuthFilter 拦截所有请求，先验证 Token 有效性，再读取 Token 里的 `permission` 字段与请求路径要求的角色做匹配——比如 `/api/drivers/**` 开头的接口只有 driver 角色才能访问，其他角色返回 403。验证通过后，网关把用户信息注入请求头，下游各服务直接从请求头读取，不需要每个服务重复解析 Token。

【第 13、14、15 页 · 顾客服务】

顾客服务有三块：顾客信息管理、收货地址管理，以及供下游调用的内部 Feign 接口。

收货地址管理的关键设计：顾客在前端输入文字地址后，服务调用**高德地图正向 Geocoding API** 把地址字符串转换为经纬度坐标，再连同文字地址一并存入数据库。后续路线规划直接取坐标，避免频繁调用 API 导致限流风险。

顾客服务还暴露了内部 Feign 接口，供 order-service 在创建订单时查询收货地址坐标，作为路线规划的终点输入。

【第 16、17、18 页 · 商铺服务】

商铺服务分商品管理、库存管理、订单操作三块。

商品管理有审核流程——商户提交上架请求后，需要管理员审核通过才能正式上架。

库存管理：商户执行入库，发货时选择仓库后自动出库。扣库存用原子 SQL——`WHERE stock >= quantity`，通过数据库层面保证不超卖，而不是在应用层做读-校验-写三步操作。

商家发货时会触发 order-service 修改订单状态，并创建配送记录，同时调用 logistics-service 生成物流路径。

【第 19 页 · 订单服务】

订单服务是整个后端的**编排中枢**，协调多个服务间的调用。

商家发货时，order-service 通过 Feign 同时调用两个下游服务：调用 logistics-service 生成物流路径，调用 shop-service 扣减库存，两个操作都成功才完成发货。如果其中一个失败，提供了回滚接口来恢复库存。

另外有一个**商品快照**设计——下单时把商品名称、价格等关键信息存一份到 `order_item` 表，这样商家后续改价或下架，历史订单的展示不受任何影响。

【第 20、21、22 页 · 司机服务】

司机服务分三块：司机与车辆管理、运输任务管理、实时位置上报。

一名司机可以绑定多辆车，选择当前出行使用的车辆。

运输任务管理：司机端看到待配送的订单列表，接单后锁定任务；配送完成后调用 order-service 将订单改为"已完成"。

实时位置上报：司机端前端每隔一定间隔调用上报接口，把当前 GPS 坐标传过来，存入 `logistics_track` 表，顾客端即可在地图实时看到司机位置。这些轨迹数据同时也是路线规划历史分析的原始数据来源。

---

## 第五部分：当前完成进度与成果（约 4 分钟）

【第 23 页 · 完成进度概览】

（这是中期答辩最重要的部分，请结合 PPT 的进度表或截图一起说）

下面如实汇报目前的完成情况。

**后端方面**，9 个模块全部完成基础框架搭建和主干功能开发：

| 模块 | 完成情况 |
|------|---------|
| gateway-service（网关） | ✅ JWT 鉴权、路由转发、CORS 全部完成 |
| auth-service（认证） | ✅ 登录、注册、Token 签发完成 |
| user-service（用户管理） | ✅ 管理员账号管理完成 |
| customer-service（顾客） | ✅ 顾客信息、地址管理（含高德Geocoding）完成 |
| shop-service（商铺） | ✅ 商品、库存、店铺管理完成；商品审核完成 |
| order-service（订单） | ✅ 下单、发货编排、商品快照设计完成 |
| driver-service（司机） | ✅ 司机信息、车辆绑定、接单任务管理完成 |
| logistics-service（物流） | ✅ 三种路线策略、实时轨迹上报、历史聚合全部完成 |
| common（公共模块） | ✅ 统一返回结构、工具类完成 |

**前端方面**，基于 Vue 3 + Element Plus，共完成 **25 个页面**，四端全部落地：

- **管理端（5个页面）**：Dashboard 仪表盘、用户管理、商品审核、订单监控、系统管理
- **顾客端（5个页面）**：商城首页、商品详情/下单、订单列表（含地图轨迹）、个人中心、收货地址管理
- **商家端（4个页面）**：商品管理、库存管理、订单管理（发货）、店铺信息
- **司机端（4个页面）**：配送管理（接单）、导航路线（地图）、车辆管理、个人信息

**数据库方面**，11 张业务数据表全部建立完成，并准备了完整的测试数据集（见 `test-data` 目录）。

**目前尚未完成的功能**（已列入后续计划）：
- Docker/Kubernetes 容器化部署
- RabbitMQ 异步消息队列
- Redis 缓存层（商品列表缓存 + Token 黑名单）
- 商家营收数据报表页面

【第 24 页 · 完整业务链路演示】

（现场演示或播放录屏，建议现场实机操作）

下面进行现场功能演示，展示整个核心业务链路的跑通情况：

**第一步：登录**
（演示四个角色账号分别登录，展示路由守卫自动跳转到对应角色首页）

**第二步：顾客端下单**
（演示商城浏览 → 选择商品 → 填写收货地址（触发高德Geocoding） → 提交订单 → 查看订单状态"待发货"）

**第三步：商家端发货**
（演示进入订单管理 → 选择订单 → 选择发货仓库 → 点击发货 → 后台自动调用路线规划引擎）

**第四步：查看地图规划路线**
（演示顾客端订单详情 → 点击"查看路线" → Leaflet 地图上显示蓝色虚线规划路线，起点图标和终点图标）

**第五步：司机接单**
（演示司机端配送管理 → 看到待配送任务 → 接单 → 进入"派送中"状态）

**第六步：实时轨迹追踪**
（演示司机端导航页面显示路线地图 → 模拟 GPS 上报 → 顾客端地图上出现橙色实线实际轨迹，以及蓝色位置图标实时移动）

以上六步完整走通，证明从下单到配送完成的核心业务链路已经全部跑通。

---

## 第六部分：项目亮点 —— 智能路线规划（约 5 分钟）

【第 25 页 · 部分标题】

接下来重点介绍这个项目最有技术含量的部分——智能路线规划引擎。整个规划模块做了分层设计：最底层是 GraphHopper 提供路网数据支撑，中间层是自主实现的 A* 算法做路径搜索，最上层是大模型结合历史数据做智能决策。

（切换到亮点内容第一页）

【第 26 页 · 整体背景与坐标映射】

**GraphHopper + OpenStreetMap**

路网数据来源于 OpenStreetMap，下载了上海地区的 `.pbf` 格式地图文件，用 GraphHopper 9.x 加载解析后构建路网图，缓存到 `graph-cache` 目录。第一次解析需要几分钟，后续启动直接加载缓存，秒级完成初始化。

**坐标映射到路网节点**

在运行 A* 之前，有一个关键预处理：把数据库里存的仓库坐标和顾客地址坐标映射到路网上最近的节点，通过 GraphHopper 的 `LocationIndex.findClosest()` 实现。这样保证 A* 的起终点一定落在真实的道路上。

【第 27 页 · A* 算法】

**核心公式：f(n) = g(n) + h(n)**

`g(n)` 是从起点沿路网到达当前节点的**实际代价**；`h(n)` 是当前节点到终点的**启发式估计**，用 **Haversine 球面距离公式**计算，满足**可采纳性**——球面直线距离永远不会高估实际路网距离，保证 A* 一定找到最短路径。

算法实现上，用一个按 f 值排序的优先队列（最小堆）作为开放列表，找到终点后通过 `cameFrom` 回溯表还原完整节点序列，转换成 GeoJSON LineString 格式返回。

**Weighted A*（加权 A*）**

引入权重 ε，公式变为 `f(n) = g(n) + ε × h(n)`。ε = 1.0 是标准最优 A*；ε > 1.0 时算法更激进地趋向终点，搜索出与最优路线风格不同的备选路径。分别用 ε = 1.0、1.8、3.5 生成三条候选路线，这是 LLM_JUDGE 策略的基础。

**三种可切换策略**：`A_STAR`（纯算法）、`LLM_JUDGE`（多候选 + LLM 裁判）、`LLM_WAYPOINT`（LLM 决策路点 + A* 执行），通过配置文件 `routing.strategy` 一键切换，架构上采用策略模式实现，符合开闭原则。

【第 28 页 · LLM_WAYPOINT 策略】

LLM_WAYPOINT 是当前默认策略，核心思想：**让 LLM 做战略决策，让 A* 做战术执行**。

整个流程五步完成：

**第一步，查询历史数据。** 从 `logistics_track` 表读取近 30 天历史轨迹，统计当前小时的平均行驶速度与全天均速，计算出 **speedRatio（速度比值）**。同时在起终点中点附近约 10 公里范围内，聚合出近 7 天均速低于 8km/h 的慢速热点区域列表。

**第二步，构建 Prompt 调用 DeepSeek。** 系统提示词将 LLM 定位为"物流路线策略专家"，用户提示词注入起终点坐标、直线距离、当前时间时段，以及所有历史数据。如果 speedRatio < 0.7（当前比平时慢 30% 以上）且存在慢速热点，建议插入绕路路点。

**第三步，坐标合法性校验。** 对 LLM 返回的每个路点做上海地理边界校验（纬度 30.6°~31.9°N，经度 120.8°~122.2°E），超出范围的路点直接过滤，防止 LLM 幻觉影响路线质量。

**第四步，分段 A* 规划。** 过滤后的路点构成 [起点 → 路点1 → 路点2 → 终点] 序列，对每相邻两点分别调用 A*，得到各段在真实路网上的路线。

**第五步，路段拼接与降级。** 各段坐标按顺序合并，如果 LLM 调用失败或无有效路点，自动降级为纯 A* 直达规划，保证路线一定能生成。

【第 29 页 · LLM_JUDGE 策略与策略对比】

**LLM_JUDGE 策略**

先用三种 ε 值的 Weighted A* 生成三条候选路线，再把所有候选路线的距离、预估时间、路网节点数，连同历史交通数据一起发给 DeepSeek 做裁判。LLM 的决策优先级明确写在 Prompt 里：**准时率 > 路线稳定性 > 总距离**。LLM 返回选中的候选编号，以及用当前时段历史均速重新计算的预计到达时间。

**两种策略对比**

LLM_WAYPOINT 中 LLM 参与决策层，决定"走哪个方向"，A* 负责执行每段路网规划；LLM_JUDGE 中 LLM 参与评估层，所有候选路线已经是合法路网路线，LLM 只做综合评估选最优，不会引入无效坐标问题。两种策略各有侧重，可通过配置文件一键切换。

---

## 第七部分：后续计划（约 1.5 分钟）

【第 30 页 · 部分标题】

（切换到后续计划内容页）

【第 31 页 · 后续计划】

后续计划主要三项。

**第一项：容器化部署。** 计划为每个服务编写 Dockerfile，通过 Kubernetes 完成容器编排与服务管理，前端使用 Nginx 容器托管并配置反向代理。这样整个系统可以一键启动，也方便最终答辩演示。

**第二项：引入 RabbitMQ 消息队列。** 目前服务间都是 OpenFeign 同步调用，一旦某个服务响应慢或挂掉，整个调用链会阻塞。引入 MQ 后，比如发货操作，order-service 只需发布"订单发货"消息，logistics-service 订阅后异步生成路线，两个服务完全解耦，系统吞吐量也会有明显提升。

**第三项：加入 Redis 缓存。** 商品列表这类读多写少的数据目前每次请求都直接查数据库，加了 Redis 之后可大幅降低数据库压力。另外 Token 黑名单——用户主动退出登录后让 Token 立即失效——也计划用 Redis 的 TTL 机制来实现。

---

## 总结与结束（约 1 分钟）

【第 32 页 · 谢谢观看】

总结一下，这个项目的核心亮点有三个：

**第一，微服务架构**：9 个模块通过 Nacos 注册、OpenFeign 协作，职责清晰，可独立部署；

**第二，智能路线规划**：自主实现了 A* 算法与 Weighted A*，并设计了两种大模型结合策略——LLM_WAYPOINT 让 LLM 基于历史数据做战略决策、A* 负责路网执行；LLM_JUDGE 用加权 A* 生成多候选、LLM 做综合裁判。历史交通数据贯穿两种策略，让决策更贴近实际通行规律；

**第三，多角色全场景**：管理员、顾客、商家、司机四端 25 个页面全部完成，从下单到配送完成的完整业务链路已经跑通，配合 Leaflet 地图对规划路线和实时轨迹做可视化。

中期阶段，系统主干功能已全部实现，剩余工作集中在基础设施优化（MQ、Redis、容器化）层面，不影响核心功能完整性。

以上就是我的中期答辩汇报，感谢各位老师的聆听，请老师们多批评指正，谢谢！

---

## 附：可能被问到的问题及参考回答

**Q1：为什么用微服务而不是单体应用？**

> 主要考虑到系统有四种角色，业务边界比较清晰，微服务按业务拆分后各服务可以独立开发、独立部署，各端互不影响。当然对于毕设规模来说微服务确实增加了不少复杂度，但这也是学习目标的一部分，让我对 Spring Cloud 生态有了更深入的实践。

**Q2：A* 和 Dijkstra 的区别？你为什么选 A*？**

> Dijkstra 是无启发式的最短路算法，每次扩展距离起点最近的节点，会向四面八方均匀扩展，在大图上要遍历非常多的节点。A* 加了一个启发函数 h(n) 来估计当前节点到终点的距离，优先探索更有希望的方向，大幅减少了需要扩展的节点数，效率更高。在路线规划这个场景里，起终点都是明确已知的，Haversine 球面距离是一个完美的启发函数——满足可采纳性，保证找到最优路径。

**Q3：LLM 幻觉坐标怎么处理的？**

> 处理方式是两层保障：第一层在 Prompt 里明确约束，要求路点必须是上海市区真实道路附近的坐标，绕路不得超过直线距离的 50%；第二层在代码里做上海地理边界校验（纬度 30.6°~31.9°N，经度 120.8°~122.2°E），超出范围直接过滤掉。如果过滤后没有有效路点，或者 LLM 调用完全失败，自动降级为纯 A* 规划，保证一定有路线可用。

**Q4：speedRatio 是怎么算的，低于 0.7 代表什么？**

> speedRatio = 当前小时历史均速 / 全天历史均速。比如全天均速 35km/h，当前早高峰小时均速 20km/h，那 speedRatio = 20/35 ≈ 0.57，低于 0.7，说明当前比平时慢了 30% 以上，系统判断为明显拥堵，建议 LLM 考虑绕路。这个阈值是经过测试后的经验值，也是可调整的配置参数。

**Q5：LLM 的置信度 HIGH/MEDIUM/LOW 是怎么定的？**

> 是基于历史数据样本量定的：样本量超过 100 次对应 HIGH，10 到 100 次对应 MEDIUM，不足 10 次对应 LOW。样本量少的时候历史数据参考价值有限，置信度 LOW 时 LLM 通常倾向于直达策略。

**Q6：高德 API 的 Geocoding 怎么用的？**

> 顾客在前端填写收货地址文字之后，customer-service 调用高德地图正向地理编码 API，把地址字符串转成经纬度坐标，然后连同文字地址一起存到 `customer_address` 表的 `latitude` 和 `longitude` 字段。仓库信息也是同理，商家填地址时通过 shop-service 调高德 API 转坐标存下来。后续路线规划直接读数据库里的坐标，不需要实时调地图 API。

**Q7：多个服务连同一个数据库，这不是单点瓶颈吗？**

> 确实是目前架构的一个不足。理想情况下每个服务应该有独立数据库，通过 MQ 和分布式事务保证一致性。考虑到毕设规模，暂时共用一个库按表划分业务边界，先保证业务逻辑跑通，这也是后续可以优化的方向。

**Q8：GraphHopper 加载 OSM 数据大概要多久？**

> 第一次加载上海的 .pbf 文件，解析和建图大约需要几分钟。解析结果缓存在项目根目录的 `graph-cache` 文件夹里，后续启动直接读缓存，几秒钟就好了。这个缓存目录已经随项目一起提交，所以直接启动不需要重新解析。

**Q9：JWT Token 过期了怎么处理？**

> Token 有效期是 2 小时，过期后前端收到 401 响应，Axios 响应拦截器会自动清空登录状态并跳转到登录页，重新登录获取新 Token。主动退出让 Token 立即失效（Token 黑名单）计划用 Redis 实现，这是后续要做的工作。

**Q10：并发控制做了哪些？**

> 库存扣减用了原子 SQL（`WHERE stock >= quantity`），在数据库层面保证不超卖。整体还没有做系统压测，后续引入 Redis 缓存减少数据库压力、引入 RabbitMQ 异步解耦后，高并发场景的表现会有明显改善。

**Q11：user-service 和 auth-service 的分工是什么？**

> auth-service 专门负责身份认证——登录、注册、Token 签发和验证，是无状态的认证中心。user-service 负责用户账号本身的 CRUD 管理，主要面向管理员进行账号的新增、修改、禁用等操作。两者职责分离，auth-service 对外暴露登录接口，user-service 对内管理账号数据。

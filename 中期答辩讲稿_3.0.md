# 基于微服务架构的智能物流管理系统 —— 中期答辩讲稿 v3.0

> 对应 PPT 页码用 【第X页】 标注；（括号内）为舞台提示，不读出来  
> 总时长目标：约 20 分钟  
> 各章节参考时长见标题

---

## 开场（约 30 秒）

【第 1 页 · 封面】

各位老师好，我叫付苗，指导老师是牛志华老师。我的毕业设计题目是《基于微服务架构的智能物流管理系统设计与实现》，今天进行中期答辩，汇报大约 20 分钟，请老师们多批评指正。

---

## 第一章：对课题的整体认识（约 8 分钟）

【第 2 页 · 目录】

今天的汇报共分三个部分。第一部分介绍课题整体认识，包括课题定位与研究背景、系统功能范围、界面效果，以及技术方案与架构设计；第二部分汇报当前开发进度并进行功能演示；第三部分说明后续待完成的工作计划。

---

### 1.1 课题定位与研究背景（约 2 分钟）

【第 3 页 · 课题定位】

本课题的研究对象是一套**智能物流管理系统**，系统覆盖顾客下单、商家发货、司机配送、顾客收货的完整业务流程，并在配送路线规划环节引入 AI，使系统能够结合历史交通数据自动生成更合理的配送路线。

选择这一课题，主要源于传统物流系统存在的三个问题：

**第一，架构扩展性不足。** 传统单体系统各模块高度耦合，任何一处功能故障都可能导致整体不可用，在高并发场景下也容易出现性能瓶颈。

**第二，路线规划依赖人工经验。** 现有大多数物流平台的配送路线或由人工指定、或仅按最短距离计算，不考虑历史交通规律，配送效率有限。

**第三，各角色间信息不共享。** 顾客无法实时掌握配送位置，司机缺乏路线优化支持，管理员难以统一监控配送状态。

针对以上问题，本系统的设计方案为：采用微服务架构提升系统的可扩展性与可维护性，基于 A* 算法结合大语言模型实现智能路线规划，搭建统一的多角色协同平台。

---

### 1.2 系统功能范围——四类用户角色（约 2 分钟）

【第 4 页 · 四角色功能全景图】

系统面向四类用户，各角色的功能职责如下。

**管理员**负责系统层面的监控与管理：通过数据概览仪表盘查看商品总数、订单总数、仓库数量等整体运行数据；管理全部用户账号；审核商家提交的商品上架申请；监控所有订单状态。

**顾客**作为消费方：可浏览各商家上架的商品、加入购物车、提交订单（系统模拟支付流程）；在订单列表中查看订单状态的变化，可在地图上查看规划的配送路线与司机的实时位置。

**商家**负责商品供给与发货：管理店铺商品信息及仓库库存；收到订单后在订单管理页面确认发货并选择发货仓库——系统在此时**自动调用路线规划引擎**，以仓库坐标为起点、顾客收货地址为终点，计算最优配送路线。

**司机**负责配送执行：在"待接单"列表中查看并接受配送任务；接单后可在导航路线页面查看系统规划好的路线；配送途中定时上报 GPS 坐标，系统记录实际行驶轨迹。



---

### 1.3 系统界面与交互效果（约 2 分钟）

【第 5 页 · 界面效果展示 / 截图集】

（对应 PPT 上各端的界面截图，结合截图说明）

在界面设计方面，整个系统基于 **Vue 3 + Element Plus** 构建，采用左侧导航栏与右侧主内容区的标准后台管理布局。四个角色共用同一前端应用，通过 Vue Router 路由守卫根据登录角色自动跳转到对应首页，不同角色的可见菜单与可访问接口完全隔离。

以下几处界面设计值得重点介绍：

**地图可视化模块**：基于 Leaflet 地图库与 OpenStreetMap 实现，内嵌于订单详情弹窗中。地图同时呈现三层信息：绿色圆点标记发货仓库位置，红色圆点标记收货地址，蓝紫色实线展示系统规划的配送路线；配送开始后，橙色实线随司机 GPS 上报逐步延伸，代表已实际行驶的轨迹；蓝色圆点实时标记司机当前位置。地图右下角附有图例，说明各颜色对应含义。

**商家发货界面**：点击"发货"按钮后弹出操作对话框，商家选择发货仓库并确认，系统后台同步完成订单状态更新、路线规划、库存扣减、配送任务创建四项操作。

**顾客订单页面**：以标签页按订单状态分组（全部、待支付、待发货、待揽件、派送中、已完成、已取消），每条订单记录展示订单号、金额和当前状态，点击"查看详情"弹出包含地图组件的详情弹窗。

---

### 1.4 技术方案与架构设计（约 2 分钟）

【第 6 页 · 系统架构图】

下面介绍系统的技术方案与架构设计。

**整体架构**采用前后端分离的微服务架构：前端为 Vue 3 单页应用，所有请求统一经由 API 网关转发；网关负责 JWT 鉴权与路由分发；后端拆分为 7 个独立业务服务，各自运行在独立端口，通过 Nacos 实现服务注册与发现，通过 OpenFeign 进行服务间同步调用；数据持久化使用 MySQL。

**后端技术选型**：Spring Boot 3.1.5 + Spring Cloud 2022.0.4 + Spring Cloud Alibaba（集成 Nacos）；ORM 使用 MyBatis Plus；Java 版本为 17；服务间调用使用 OpenFeign。

**前端技术选型**：Vue 3 + Vite + TypeScript；UI 组件库为 Element Plus；地图库为 Leaflet；状态管理使用 Pinia；HTTP 请求封装使用 Axios。

**路线规划模块**是本系统的核心设计亮点：自主实现 A* 算法，基于 GraphHopper 加载 OpenStreetMap 上海路网数据作为底层支撑；在 A* 之上设计了两种大语言模型增强策略——LLM_WAYPOINT 策略由 DeepSeek 根据历史轨迹数据决定是否插入绕路路点，LLM_JUDGE 策略由 DeepSeek 在多条候选路线中综合评估选优；整个规划模块以策略模式封装，三种策略可通过配置文件切换，无需修改业务代码。

【第 7 页 · 技术选型汇总表】

（对应 PPT 上的技术栈汇总表，不逐条朗读，结合表格说明重点选型即可）

这张表是完整的技术选型汇总。每项技术均有明确的适用理由：Nacos 承担服务注册与配置中心的职责，OpenFeign 简化了服务间 HTTP 调用，GraphHopper 提供路网图数据与空间索引支撑，DeepSeek API 提供大语言模型推理能力，Leaflet 负责前端地图与轨迹的可视化渲染。

---

## 第二章：当前完成进度与成果展示（约 10 分钟）

【第 8 页 · 进度总览】

---

### 2.1 整体完成情况（约 1 分钟）

下面报告当前的开发进度与完成情况。

（结合 PPT 进度表说明）

**后端全部 9 个模块的主干功能已完成**，包括：公共模块 common、API 网关 gateway、认证服务 auth、用户管理 user、顾客服务 customer、商铺服务 shop、订单服务 order、司机服务 driver、物流服务 logistics。各模块均已完成接口测试，`test-data` 目录下存有完整的测试数据。

**前端 25 个 Vue 页面已全部实现**，四个角色的核心功能页面均已覆盖。

**数据库 11 张核心表已全部建立**，并准备了完整的测试数据集。

**尚未完成的部分**（不影响核心业务功能）：Docker/Kubernetes 容器化部署、RabbitMQ 消息队列引入、Redis 缓存层，以及商家营收统计页面。

---

### 2.2 功能演示（约 8 分钟）

【第 9 页 · 演示流程图】

下面按完整业务流程进行现场演示，依次展示各角色的功能界面与实际运行效果。

（切换到系统页面，按以下流程进行实机演示）

---

**【演示第 1 步：登录与基于角色的路由跳转（约 30 秒）】**

（打开登录页）

这是系统的统一登录入口，四类角色共用同一个页面。输入不同角色的账号登录后，前端路由守卫会读取 JWT Token 中携带的 `permission` 字段，自动跳转到该角色对应的首页——例如管理员登录后进入数据概览 Dashboard，顾客登录后进入商城首页。此处实现的是基于角色的访问控制（RBAC），不同角色的可见菜单与可访问接口完全隔离。

（演示：分别以管理员账号和顾客账号登录，观察跳转目标）

---

**【演示第 2 步：管理员端——数据概览与商品审核（约 30 秒）】**

（切换至管理员端）

管理员首页以卡片形式展示系统整体数据，包括商品总数、订单总数、仓库数量，以及各功能模块的快速入口。

商品审核页面列出了所有商家提交的待审核商品记录，管理员审核通过后，该商品才会正式出现在顾客端商城中，驳回则退回给商家修改。这是系统对商品上架的管控机制。

---

**【演示第 3 步：顾客端——商城浏览与下单（约 1 分钟）】**

（切换至顾客账号）

顾客端商城页面按店铺展示商品信息，包括商品名称、价格和库存。选中商品后进入下单页面，选择收货地址时有一处值得说明的设计：顾客在添加新收货地址时，填入文字地址后系统会调用**高德地图正向 Geocoding API** 将地址字符串转换为经纬度坐标并存入数据库，这是后续路线规划精确定位配送终点的数据基础。

提交订单后，系统自动完成模拟支付流程，订单状态变为"待发货"。

（演示：浏览商城 → 选商品下单 → 查看订单列表，确认状态显示"待发货"）

---

**【演示第 4 步：商家端——发货确认与路线规划触发（约 2 分钟）】**

（切换至商家账号）

这一步是整个系统的核心业务环节，路线规划在此处被触发。

商家在订单管理页面找到当前"待发货"的订单，点击"发货"按钮，弹出发货对话框，选择发货仓库后确认——

（演示：点击"发货"，选择仓库，点击确认）

此时后台同步完成以下操作：**第一**，订单状态更新为"待揽件"；**第二**，order-service 通过 OpenFeign 调用 logistics-service，物流服务以仓库的经纬度坐标为起点、顾客收货地址坐标为终点，调用路线规划引擎——基于 A* 算法在 OpenStreetMap 上海路网上搜索最优路径，并通过 DeepSeek 大语言模型结合历史交通数据判断是否需要绕行拥堵区域；**第三**，规划结果以 GeoJSON 格式持久化至数据库；**第四**，扣减仓库库存；**第五**，创建配送任务，等待司机接单。

（演示：刷新订单列表，确认订单状态已变为"待揽件"）

---

**【演示第 5 步：顾客端——查看地图规划路线（约 1.5 分钟）】**

（切换回顾客账号）

顾客在订单列表中点击"查看详情"，弹出订单详情弹窗，弹窗内嵌了地图组件。

（演示：打开订单详情弹窗）

地图上已显示出一条**蓝紫色路线**——这是系统为本次配送规划的路线，基于 A* 算法在上海真实路网上计算得出，路线沿实际道路延伸，并非直线连接。绿色圆点标记发货仓库，红色圆点标记收货地址，右下角图例说明各颜色对应含义。

该路线数据以 GeoJSON LineString 格式存储，前端 Leaflet 地图库可直接解析并渲染，无需额外转换。

---

**【演示第 6 步：司机端——接单与导航路线查看（约 1 分钟）】**

（切换至司机账号）

司机在"待接单"列表中可以看到当前这条配送任务，记录中显示收货人、配送地址和创建时间。点击"接单"确认后，该任务转入"我的配送"列表，状态变为"配送中"。

切换至"配送路线"页面，选择已接的订单后，地图上同样显示系统规划好的配送路线，司机可据此导航行驶。

（演示：完成接单 → 切换至导航路线页面 → 查看地图路线）

---

**【演示第 7 步：实时位置上报与轨迹追踪（约 1.5 分钟）】**

（切换至司机端，模拟 GPS 上报）

配送途中，前端定时调用 GPS 上报接口，将当前坐标发送至 logistics-service 的 `/api/logistics/track/location` 接口，坐标记录写入 `logistics_track` 表，同时同步更新 `logistics_route` 表中的当前位置字段。

（演示：调用上报接口写入测试坐标，然后切换至顾客端刷新订单详情）

切换至顾客端刷新订单详情后，地图上出现**橙色实线**——即司机已实际行驶的轨迹，随 GPS 上报点的增加逐步延伸；蓝色圆点实时标记司机当前位置。至此，顾客端的实时货物位置追踪功能得以完整展示。

---

### 2.3 路线规划模块的设计说明（约 1 分钟）

【第 10 页 · 路线规划技术方案】

（时间允许时结合代码或流程图说明）

路线规划模块在工程设计上有以下两处值得说明：

**第一，策略模式封装三种规划算法。** 系统定义了 `RouteStrategy` 策略接口，`AStarRouteStrategy`、`LlmJudgeRouteStrategy`、`LlmWaypointRouteStrategy` 三个实现类分别对应纯 A* 算法、多候选路线 LLM 裁判、LLM 路点分段规划三种策略。`RoutingStrategyConfig` 读取配置文件中的 `routing.strategy` 参数，以 `@Primary` 注解指定当前生效的实现类，业务调用层完全不感知策略切换，符合开闭原则。

**第二，LLM 输出坐标的合法性保障。** 大语言模型在生成路点坐标时有时会产生偏差，系统采用两层机制加以防范：一是在 Prompt 中明确要求路点坐标须位于上海市区真实道路附近；二是在代码层对返回坐标进行地理边界校验（纬度 30.6°~31.9°N，经度 120.8°~122.2°E），不在范围内的路点直接过滤。GraphHopper 的空间索引会将任意坐标自动吸附到最近的路网节点，进一步消除坐标偏差的影响。若 LLM 调用完全失败，系统自动回退至纯 A* 直达规划，确保路线始终可以生成。

---

## 第三章：后续工作计划（约 2 分钟）

【第 11 页 · 后续计划】

后续待完成的工作主要有三项，均属于基础设施层面，不影响现有核心功能：

**第一，容器化部署。** 为每个服务编写 Dockerfile，通过 Kubernetes 进行容器编排，前端以 Nginx 容器托管并配置反向代理至后端网关。完成后整套系统可统一启动与管理，也便于最终答辩时的环境搭建。

**第二，引入 RabbitMQ 消息队列。** 目前 order-service 在发货时通过 OpenFeign 同步调用 logistics-service 规划路线，若物流服务响应延迟，则整条调用链路将被阻塞。引入消息队列后，order-service 发布发货事件，logistics-service 异步消费并完成路线规划，两个服务实现解耦，系统吞吐量也会相应提升。

**第三，加入 Redis 缓存。** 商品列表属于读多写少的数据，引入 Redis 缓存可以降低数据库查询压力；此外，计划使用 Redis 的 TTL 机制实现 Token 黑名单，在用户主动退出登录后即时使 Token 失效，完善当前认证流程中的这一不足。

---

## 总结（约 30 秒）

【第 12 页 · 总结页】

总结中期阶段的工作：**架构方面**，系统采用微服务架构，7 个业务服务各自独立运行，通过 Nacos 注册和 OpenFeign 协作；**功能方面**，四端 25 个页面已全部实现，完整业务流程通过测试验证；**算法方面**，自主实现了 A* 路径搜索算法，并结合大语言模型与历史轨迹数据设计了智能路线规划机制；**可视化方面**，实现了地图规划路线展示与实时 GPS 轨迹追踪。

目前核心功能全部完成，后续工作聚焦于 RabbitMQ、Redis 和容器化部署等基础设施层面的完善。

以上是我的中期答辩汇报，感谢各位老师，请老师们多批评指正，谢谢！

---

## 附：可能被问到的问题及参考回答

**Q1：为什么采用微服务架构而不是单体应用？**

> 主要考虑以下两点：一是系统包含四类角色，各角色的业务边界较为清晰，微服务按业务域拆分后各服务可以独立开发和部署，互不干扰；二是通过实践 Spring Cloud 生态的微服务治理，也是本课题在技术深度方面的学习目标之一。当然，对于毕业设计规模的项目，微服务确实引入了额外的工程复杂度，这一点在设计时已有充分考量。

**Q2：A* 算法与 Dijkstra 算法有什么区别？为何选择 A*？**

> Dijkstra 是无启发式的最短路算法，每次扩展距离起点代价最小的节点，搜索方向不加约束，在大规模图上会遍历大量节点。A* 在此基础上引入了启发函数 h(n)，用于估计当前节点到终点的代价，使搜索优先向终点方向推进，显著缩小搜索范围。路线规划场景中起终点均已知，以 Haversine 球面距离作为启发函数满足可采纳性条件，在保证找到最优路径的前提下，搜索效率明显高于 Dijkstra。

**Q3：大语言模型生成的路点坐标偏差如何处理？**

> 采用两层机制：第一层在 Prompt 中明确约束，要求路点必须位于上海市区真实道路附近；第二层在代码中对返回坐标进行地理边界校验（纬度 30.6°~31.9°N，经度 120.8°~122.2°E），超出范围的路点直接过滤。GraphHopper 的空间索引会将路点吸附到最近的路网节点，进一步消除坐标偏差。若 LLM 调用完全失败，系统自动回退至纯 A* 规划，确保路线始终可以生成。

**Q4：多个服务共用同一数据库，存在单点瓶颈吗？**

> 这确实是当前架构的一个局限。理想情况下每个服务应有独立数据库，通过消息队列和分布式事务维护一致性。考虑到毕业设计的规模，目前采用共用一个 MySQL 实例、按表划分业务边界的方案，优先保证业务逻辑的完整性，数据库层面的进一步拆分是后续可以扩展的方向。

**Q5：GraphHopper 加载 OSM 数据需要多长时间？**

> 首次加载上海 .pbf 格式地图文件，解析建图约需几分钟。解析结果会缓存至项目根目录下的 `graph-cache` 文件夹，后续启动直接读取缓存，耗时约几秒。该缓存目录已随项目一同提交，因此在当前机器上直接启动无需重新解析。

**Q6：JWT Token 过期后如何处理？**

> Token 有效期为 2 小时。过期后前端收到 401 响应，Axios 响应拦截器会自动清除本地登录状态并跳转至登录页，用户重新登录后获取新 Token。主动退出登录后使 Token 立即失效（Token 黑名单）的功能计划通过 Redis TTL 机制实现，属于后续待完成的工作。

**Q7：库存并发控制是怎么做的？**

> 库存扣减使用了原子 SQL 条件更新：`UPDATE ... SET stock = stock - quantity WHERE stock >= quantity`，在数据库层面保证不超卖，避免了应用层"读取-校验-写入"三步操作带来的并发漏洞。整体系统尚未进行压测，后续引入 Redis 缓存减少数据库压力、通过 RabbitMQ 异步解耦发货流程后，高并发场景下的表现将进一步改善。

**Q8：什么是商品快照，为什么需要这个设计？**

> 下单时，系统会将当前商品的名称、单价等关键信息复制一份写入 `order_item` 表，作为订单的商品快照。这样即便商家后续修改商品价格或将商品下架，历史订单中记录的商品信息也不会受到影响，保证了历史订单数据的完整性和一致性。

**Q9：speedRatio 指标的含义和计算方式？**

> speedRatio = 当前小时历史平均车速 / 全天历史平均车速，数据来源为 `logistics_track` 表近 30 天的轨迹记录。例如全天均速为 35km/h，当前早高峰时段均速为 20km/h，则 speedRatio ≈ 0.57，低于阈值 0.7，表明当前时段车速比日常低 30% 以上，系统判定为明显拥堵，提示大语言模型在规划路线时考虑绕行。

**Q10：auth-service 与 user-service 的职责如何划分？**

> auth-service 专职负责身份认证，包括登录验证、Token 签发和 Token 校验，是无状态的认证服务。user-service 负责用户账号数据本身的增删改查，主要供管理员进行账号管理。两者职责分离：前者对外提供登录接口，后者管理账号记录，各自独立演进，互不耦合。

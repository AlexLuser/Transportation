# 提示词模式与经验教训记录 (Prompt Patterns & Lessons Learned)

## 1. 参考图驱动的架构图重绘模式 (Reference-driven Diagram Redraw Pattern)
**适用场景**：当用户明确给出一张"像这样"的参考图，并要求系统架构图、前端架构图等论文插图在视觉上靠拢该参考图时。

**Prompt 通用模式**：
> "请先定位论文中这几张图的源文件与引用方式，再按照我给的参考图重画。系统架构图请采用'左侧层级标签 + 右侧技术矩阵块'的规整布局；如果 Typst 原生布局难以稳定复现，就直接用脚本生成白底高清 PNG，并回写论文引用。与此同时，把另外两张相关图的边框、留白、字号和弱配色统一掉。"

**经验教训 (Lessons Learned)**：
- 用户已经给了参考图时，**第一优先级是对齐视觉语法**（分层、留白、盒子排列、灰度风格），而不是先陷进某个绘图库的局部参数调优。
- 对毕业论文中的**规整分层架构图**，脚本生成静态 PNG 往往比在 Typst/Facade 中硬调复杂布局更稳定，尤其当目标风格接近 draw.io/Visio 这类成品图时。
- `Mermaid` 适合流程图和简化 ER 图，但不适合复刻这种"矩阵式、表格化、低饱和"的系统架构图。
- 重绘时应同时检查：**宽高比是否适配 A4 页宽、底色是否纯白、边框与字号是否统一、是否存在空白过多或元素互相遮挡**。

## 2. 工具选择与调研模式
**适用场景**：当用户提供多个待选方案（echarts、graphviz、typst包等）时。

**Prompt 通用模式**：
> "请先搜索网络或包管理器中的相关生态（如搜索 npm、typst universe 等），对比各工具的特点（如：echarts 适合数据图表、graphviz 适合关系拓扑、facade 适合嵌套分层架构图），根据输入中要求的'特点（比如有分层颜色背景）'，为我推荐最适合并给出直接可运行的代码。"

**经验教训**：
- 仔细阅读需求中指出的"缺陷"（如之前的评价中指出无分层背景、无泳道等），针对性地去查找能弥补这些缺陷的图表工具包。对于带颜色背景和层级包含关系的，直接锁定原生排版的 Flex 布局插件或 PlantUML/draw.io。

## 3. Typst Mermaid 多图渲染 Bug 与规避模式
**适用场景**：当需要在同一个 Typst 文档中使用 `@preview/mmdr`（Mermaid引擎）渲染多张图表（如切分多个 E-R 子图），尤其是放在 `#grid` 等复杂排版结构中时。

**Prompt 通用模式**：
> "由于 `@preview/mmdr` 在 Typst 单文件多次调用时存在文字重叠、画布截断等严重的渲染 Bug，请不要将所有 `#mermaid()` 调用写在同一个主文件中。请将每个 Mermaid 子图独立保存为一个 `.typ` 文件（设置 `#set page(width: auto, height: auto, margin: 5pt)`），独立编译为 `.png` 或 `.svg`，然后在主文件中通过 `#grid(image(), ...)` 进行组合。"

**经验教训 (Lessons Learned)**：
- 避免试图通过调整 Mermaid 代码（如改变实体定义顺序）来强行拯救过于拥挤的 E-R 单图，按照**领域驱动设计（DDD）**将复杂的大图按业务域拆分为多个子图是排版和学术展示的最佳实践。
- 坚决不要在 Typst 主文档的 `#grid` 布局中混用多个 `#mermaid()` 宏块，这会导致无法预测的底层布局碰撞（如生成出来的 PNG 图像损坏或内容重叠错乱）。物理拆分并独立编译图片是最可靠的工程方案。

## 4. 论文型核心 E-R 图收敛模式
**适用场景**：当用户明确要求把 E-R 图做得更像学位论文插图，而不是数据库全表总览或系统后台结构图时。

**Prompt 通用模式**：
> "请不要把所有表和关系都塞进一张 E-R 图。先从数据库中抽取支撑论文论述的核心实体，按业务域分组排版，只保留关键外键和主要基数关系；复杂的中间表、日志表和字段细节下放到关系表说明。整体风格采用低饱和分组框、统一实体卡片和细线连接，输出适合 A4 论文页宽阅读的高清静态 PNG。"

**经验教训 (Lessons Learned)**：
- **论文型 E-R 图优先服务于解释性，而不是穷举性**；把所有表塞满通常会得到"可导出但不可读"的图。
- 当一张图需要同时承担"概念说明"和"字段查阅"两种职责时，应让图只保留核心实体与基数，把精确字段明细交给后续关系表。
- 对于毕业论文页宽，实体数量一多就应主动做抽象收敛；相比继续堆实体，减少一层细节往往比调字号更有效。

## 5. 论文图片"已生成但未刷进PDF"排查模式
**适用场景**：当用户怀疑新重绘的论文插图没有真正进入最终 `PDF`，尤其是在 `Typst` 中同一位置可能同时存在旧的原生绘图代码和新的 `PNG` 方案时。

**本轮原始 Prompt 记录**：
> "架构总览图没有刷上去，你看看你刚才画的图真的刷到 /Users/miao/CodeBuddy/thesis-transportation/thesis-output.pdf 里了吗"

**Prompt 通用模式**：
> "请不要只看图片文件的修改时间。先检查论文主文件里该图究竟是 `image(...)` 引用还是旧的 Typst/Facade 绘图代码；然后重新编译 PDF，并用 PyMuPDF 定位包含该图标题的页面，检查该页是否存在图片对象，必要时把 PDF 内嵌图片提取出来，与源 PNG 做尺寸或像素级比对，最后再确认页码和落版结果。"

**经验教训 (Lessons Learned)**：
- **PNG 文件更新 ≠ 论文已引用新图**；如果正文仍保留旧的 `#figure(...)`/`facade` 内容，重编后 PDF 仍会显示旧版本。
- 判断"有没有刷进去"时，不能只看 `pdf` 的修改时间，必须同时核对**主文档引用方式、目标页图片对象、提取后的内嵌图片内容**。
- 对 `Typst` 论文，`PyMuPDF` 的 `page.search_for()`、`page.get_images()` 和 `extract_image()` 是非常高效的最终验收链路。

## 6. 全局 E-R 总图可读性不足时的撤图模式
**适用场景**：当系统跨多个业务域，哪怕已经做过概念收敛，整张总 E-R 图在 A4 论文页宽下仍显拥挤、凌乱，不足以支撑正式排版时。

**本轮原始 Prompt 记录**：
> "这个ER图还是很丑陋，你自己读取那个png不觉得丑陋吗，思考一下你还有没有什么办法优化。我感觉如果优化不好，要不论文里就不放这个图了"

**Prompt 通用模式**：
> "请先诚实评估当前全局 E-R 图是否真的适合放进学位论文正文。如果已经去掉字段细节、改成概念图之后仍然不好看，就不要继续在单张总图上死磕；改为保留文字概述与核心实体关系汇总表，撤掉正文中的总 E-R 图引用，并说明撤图原因是版面美观和可读性优先。图文件可以保留为工作稿，但正文不要硬放。"

**经验教训 (Lessons Learned)**：
- **不是所有系统都适合在正文里放"一张全局 ER 总图"**；跨域关系一多，即使逻辑正确，也很容易牺牲观感。
- 当图的职责与后续关系表高度重合时，图的增益会下降；这时撤图、保留结构化文字和表格，往往比留下丑图更专业。
- 对学位论文来说，**宁可少一张不够美观的图，也不要为"形式完整"硬塞一张观感欠佳的总图**。

## 7. 项目目录清理与收敛模式 (2026-05-18)
**适用场景**：当项目经历了多次迭代，产生了评估阶段的原始模板副本、重复文档、过期引用等冗余内容时。

**Prompt 通用模式**：
> "帮我清理没用的内容，收口到最佳实践"

**经验教训 (Lessons Learned)**：
- **评估阶段的原始模板不应留在项目目录内**。`thesis-latex/` 和 `thesis-typst/` 是早期下载的学校模板原版，最终选型后内容已全部迁移到独立仓库 `thesis-transportation/`，原模板应删除（随时可从 GitHub 重新下载）。
- **论文独立仓库与代码项目不应合并为 monorepo**：git 历史独立性、仓库公开/私有没有定位、体积膨胀（论文仓库含 slides/node_modules 559MB）、技术栈无关（Typst vs Java/Docker）都是理由。保持 `thesis-transportation` 独立即可。
- **docs/ 下文件做了子目录分类后**，根目录下的旧文件应在 git 中 stage 删除（`git add -u`），否则 git status 会一直显示 `D`（deleted but unstaged）。
- **`.gitignore` 要同步更新**：删除目录后移除对应条目，新增路径的忽略规则要补齐（如 `docs/reference/` 下的 PDF、`docs/thesis/` 下的提取文本）。
- **资源备忘录中的路径引用要同步修正**：比如 `论文源码` 的路径从旧位置更新为 `/Users/miao/CodeBuddy/thesis-transportation/template/thesis.typ`。

## 8. 代码库深度阅读→系统文档生成模式 (2026-05-20)
**适用场景**：当用户需要对一个已有的项目（如 Spring Boot 微服务项目）生成"从按钮到数据库"的完整工作原理文档时。

**Prompt 通用模式**：
> "帮我写一份系统工作原理详解，从用户按下按钮的视角出发，追踪每条操作触发的函数调用链，并详细讲解核心物流算法"

**经验教训 (Lessons Learned)**：
- **文档应按"角色→操作→调用链→算法→数据表"五层结构组织**，让读者能从用户视角理解系统。不是按微服务模块罗列 API，而是追踪一条业务操作从头到尾的完整路径。
- **深度阅读代码时必须跨服务追踪**：一个操作可能跨 3-4 个微服务（如下单 → order-service → logistics-service → shop-service → RabbitMQ），只看单服务的代码会断链。
- **MQ 消息是隐式调用链**：RabbitMQ 的 Exchange/RoutingKey/Queue 必须在文档中明确标注，否则读者不知道消息从哪来到哪去。
- **算法详解要配伪代码+SQL**：比如 Haversine 公式，光讲概念不够，必须贴出 Mapper XML 中的实际 SQL，让读者能直接理解实现。
- **API 速查表是文档的必备收尾**：读者看完原理后需要一个快速查阅的入口，按角色分组（顾客/商户/Hub/运输员）比按服务分组更实用。
- **产出物**：`docs/系统工作原理详解.md`（25KB，10个章节，含完整调用链+算法+API速查）。

## 9. 技术文档→趣味化重写模式 (2026-05-20)
**适用场景**：当用户已有严肃版技术文档，希望用更生动有趣（如 RPG 游戏/故事/比喻）的风格重新讲述同样的技术内容时。

**Prompt 通用模式**：
> "把这个系统说明文档用更生动有趣的方式重写，像讲一个快递奇幻漂流记"

**经验教训 (Lessons Learned)**：
- **趣味化重写的核心是"本体映射"**：给每个技术概念找一个日常/游戏比喻（顾客=任务发布者、Hub=中转驿站、MCMF=全国干线军师、K-Means=末端分组派对）。映射一旦建立，全文保持一致。
- **不要为了有趣而牺牲准确性**：每个比喻背后必须对应真实的技术术语和实现细节，读者应该能从趣味版反推出严肃版的内容。
- **结构可以和严肃版完全对应**（角色→下单→发货→中转→干线→末端→配送→算法→数据表→API），但每节标题和叙述风格全部替换。
- **交互式元素（如"点击展开技能树"）更适合网页版**，Markdown 版用表格+emoji 模拟即可。后续 VitePress 转换时再真正实现交互。
- **趣味版往往是网页化的最佳素材**：因为趣味版已有角色、比喻、叙事线索，转成交互式网页（卡片翻转、流程动画、步骤演示）更自然。
- **产出物**：`docs/系统工作原理详解-趣味版.md`（30KB，和严肃版章节一一对应）。

## 10. Markdown 文档转精致交互式网页模式 (2026-05-21)
**适用场景**：当用户有一份 Markdown 格式的系统说明文档，想让它变成带图片、可交互的精致网页时。

**Prompt 通用模式**：
> "使用 mkdocs 或 vitepress 等工具，让这个说明文档变成带图片、可交互等精致网页"

**经验教训 (Lessons Learned)**：
- **VitePress 1.x 的 `markdown.mermaid: true` 配置并不能让 ` ```mermaid ` 代码块自动渲染**。实测无效，需要用自定义 Vue 组件方式。
- **Mermaid 渲染的正确方案**：安装 `mermaid` npm 包 → 创建 `.vitepress/theme/MermaidDiagram.vue` 组件（动态导入避免 SSR 问题）→ 在 `theme/index.js` 中全局注册 → Markdown 中用 `<MermaidDiagram :code="..." />` 替代 ` ```mermaid ` 代码块。
- **MermaidDiagram.vue 组件要点**：必须 `onMounted` 中动态 `import('mermaid')`，不能用顶层 import（SSR 会报错）；`mermaid.initialize()` 只调用一次；渲染后 `innerHTML` 赋值给 `ref`。
- **文档网站和前端项目要分离**：文档网站应有独立端口（如 5175），不要和前端项目混在同一端口，用户希望它们是两个完全独立的可访问地址。
- **vite.config.js 的 SSR noExternal 要加 `mermaid`**，否则会出现 SSR 构建错误。

## 11. 系统内部 API 与算法学习路径追问模式 (2026-05-21)
**适用场景**：当用户在阅读系统文档后，追问某个流程步骤（如"分配 Hub""入调度池"）的具体 API 接口和涉及算法时。

**Prompt 通用模式**：
> "那分配 Hub 和入调度池是什么 API 接口，有哪些我需要学习的算法"

**经验教训 (Lessons Learned)**：
- **不是所有步骤都是 HTTP 接口**：比如"入调度池"实际上是 RabbitMQ 异步消息（`AddToPoolMessage`），不是 REST API。搜索时要同时查 MQ Consumer 和 Controller。
- **回答应按"请求→处理→响应"结构**，附上 DTO 字段、内部算法逻辑和 SQL（如 Haversine 公式），让用户能完整理解数据流。
- **算法推荐应按优先级分层**，并给出算法之间的调用关系图（如 Haversine→MCMF→K-Means→A*），帮助用户理解系统全貌。
- **推荐视频资源时，B 站搜索比通用搜索引擎更有效**：通用搜索引擎很难直接返回 B 站 BV 号，应直接用 `web_fetch` 抓取 `search.bilibili.com` 的搜索结果页。

## 12. B 站视频资源检索与推荐模式 (2026-05-21)
**适用场景**：当用户要求推荐 B 站（bilibili.com）上的学习视频时。

**Prompt 通用模式**：
> "给我推荐一些生动形象的 MCMF 算法的视频资料，尤其是 bilibili 网站的"

**经验教训 (Lessons Learned)**：
- **通用搜索引擎（Google/Bing）搜索 `site:bilibili.com` 效果很差**，基本不会返回 B 站链接。
- **正确做法**：直接用 `web_fetch` 抓取 `https://search.bilibili.com/all?keyword=关键词`，从搜索结果页中提取标题、BV号、UP主、播放量。
- **B 站搜索 URL 编码**：中文关键词需要 URL 编码（如 `%E6%9C%80%E5%B0%8F%E8%B4%B9%E7%94%A8%E6%9C%80%E5%A4%A7%E6%B5%81` = 最小费用最大流）。
- **推荐视频应分层分级**：第一梯队（直接相关）→ 第二梯队（前置知识）→ 第三梯队（趣味补充），并给出学习路线建议。
- **BV 号是 B 站视频的唯一标识**，比标题更稳定可靠，推荐时一定要附上。完整链接格式：`https://www.bilibili.com/video/BV号`。

## 13. 对话结束后要求 AI 补录 prompt 模式 (2026-05-21)
**适用场景**：当一轮对话结束后，发现 AI 没有主动将本轮的 prompt 模式记录到 `prompt_patterns.md` 时。

**Prompt 通用模式**：
> "回忆一下今天的 prompt 你记入到 md 里面了吗" / "昨天的是不是也没有记录上，按照顺序插入吧"

**经验教训 (Lessons Learned)**：
- **AI 不会自动做收尾总结**。它完成当前任务后就会停下来，不会主动打开 `prompt_patterns.md` 记录模式。需要用户主动提醒。
- **跨天遗忘是常态**。05-20 和 05-21 的模式都是用户提醒后才补记的。如果隔了几天再补，对话细节已经模糊，记录质量会下降。
- **建议在每次对话结束前主动提醒**："把今天的 prompt 模式记录一下"。或者养成习惯，每轮对话最后一个 prompt 就是要求补录。
- **prompt_patterns.md 是用户的学习笔记**，不是 AI 的行为约束。记录的目的是让用户以后遇到类似场景时能复用成熟的 prompt 模式。


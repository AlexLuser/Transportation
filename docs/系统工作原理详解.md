# Transportation 物流系统工作原理详解

> 本文档从"用户按下按钮"的视角出发，追踪每条操作触发的函数调用链，并详细讲解核心物流算法。

---

## 目录

1. [系统角色与订单生命周期总览](#1-系统角色与订单生命周期总览)
2. [顾客下单：按钮→数据库的完整链路](#2-顾客下单按钮数据库的完整链路)
3. [商户发货：Hub分配与入池](#3-商户发货hub分配与入池)
4. [中转站作业看板：五阶段操作详解](#4-中转站作业看板五阶段操作详解)
5. [全国干线调度：MCMF 运力规划](#5-全国干线调度mcmf-运力规划)
6. [末端调度：K-Means 聚类 + LLM 建议](#6-末端调度k-means-聚类--llm-建议)
7. [运输员配送：状态机与路径规划](#7-运输员配送状态机与路径规划)
8. [智能算法详解](#8-智能算法详解)
9. [关键数据表结构](#9-关键数据表结构)
10. [API 速查表](#10-api-速查表)

---

## 1. 系统角色与订单生命周期总览

### 四个角色

| 角色 | 前端路由 | 数据库用户名 | roleCode | 核心能力 |
|---|---|---|---|---|
| 顾客 | `/customer/` | sender | customer | 下单、发起寄件、查看运单 |
| 商户 | `/shop/` | merchant | shop | 管理承运物、仓库库存、发货 |
| 运输员 | `/driver/` | driver | driver | 承接任务、取件、配送、签收 |
| 管理员 | `/admin/` | admin | admin | 中转站作业、干线调度、末端调度 |

### 订单全生命周期（B2C 模式）

```
顾客下单 → 商户发货(分配Hub) → 入调度池 → 揽收入仓 → 入库 → 分拣
    → MCMF干线规划 → 出库 → 干线运输(发车/到达) → 入目标城市调度池
    → K-Means末端聚类 → 创建配送批次 → 运输员承接 → 取件 → 配送 → 签收
```

### 订单全生命周期（C2C 个人寄件）

```
顾客发起寄件 → 支付(自动分配Hub) → 入调度池 → 揽收入仓 → ...（后续同上）
```

---

## 2. 顾客下单：按钮→数据库的完整链路

### 2.1 B2C 平台下单

**前端页面**：`Customer/CreateOrder.vue` → 点击"提交订单"

```
submitOrder()
  → POST /api/orders  { shopId, addressId, items, remark }
    → OrderController.createOrder()
      → 校验: roleCode 必须是 "customer"
      → resolveCustomerId(userId)              // Feign 调 customer-service，userId → customerId
      → orderService.createOrder(customerId, request)
        → 校验收货地址归属
        → 校验商品信息 + 库存预检（Feign 调 shop-service）
        → 生成订单号: "ORD" + 时间戳 + 4位随机
        → 订单状态 = 1(待发货)，支付状态 = 1(已支付)  // 下单即支付
        → MQ 异步扣减库存: StockDeductMessage → ROUTING_STOCK_DEDUCT
  → router.push('/sender/home/orders')
```

### 2.2 C2C 个人寄件

**前端页面**：`Customer/CreateShipment.vue` → 三步骤后点击"提交寄件申请"

```
submitShipment()
  → POST /api/orders/personal-shipment  { senderAddressId, deliveryAddressId, cargoName, weight, ... }
    → OrderController.createPersonalShipment()
      → 校验: roleCode 必须是 "customer"
      → resolveCustomerId(userId)
      → orderService.createPersonalShipment(customerId, request)
        → 查取件地址（含坐标，用于 Hub 分配）
        → 查收件地址
        → 运费计算: 基础 12 元 + 超出 1kg 部分 2 元/kg
        → 订单状态 = 0(待支付)，支付状态 = 0(未支付)
        → 创建 OrderItem（货物名 + 申报价值）
  → router.push('/sender/home/orders')
```

**个人寄件需要额外支付**：
```
payOrder(orderId)
  → PUT /api/orders/{id}/pay
    → OrderServiceImpl.payOrder()
      → 个人寄件分支 (orderType=1):
        → logisticsFeignClient.assignHubs()    // 分配 Hub
        → orderStatus = 2(待揽件)              // 跳过商户发货
        → MQ: AddToPoolMessage → ROUTING_ADD_TO_POOL  // 入调度池
        → dispatchOriginType = 2(个人取件/上门揽收)
```

---

## 3. 商户发货：Hub 分配与入池

**前端页面**：`Shop/Order.vue` → 点击"发货" → 选择仓库 → 确认

```
handleConfirmShip()
  → POST /api/orders/{id}/ship  { warehouseId }
    → OrderController.shipOrder()
      → resolveShopId(userId)                   // userId → shopId
      → orderService.shipOrder(orderId, shopId, warehouseId)
        → 校验: 订单归属 + 状态必须是已支付且待发货
        → logisticsFeignClient.assignHubs()     // 分配 Hub（判断是否跨城）
          → LogisticsService.assignHubs(orderId, warehouseId)
            → 根据 warehouseId 查仓库坐标
            → 根据 endAddress 地理编码查收货坐标
            → 查找最近 Hub: 按坐标距离排序
            → 如果发货Hub ≠ 收货Hub → isCrossCity = 1
            → 写入 originHubId, destHubId
        → 更新订单: warehouseId + originHubId + destHubId + orderStatus = 2(待揽件)
        → MQ: AddToPoolMessage → ROUTING_ADD_TO_POOL
          → LogisticsConsumer.handleAddToPool()
            → 写入 dispatch_pool 表
            → dispatchOriginType = 0(跨城揽收待处理)
        → 自动创建出库单:
          → shopFeignClient.createOutboundOrder()
          → shopFeignClient.completeOutboundOrder()  // 直接完成出库
```

### Hub 分配的核心逻辑

```
assignHubs(orderId, warehouseId)
  1. 查仓库地址 → 高德地图地理编码 → (lat, lng)
  2. 查收货地址 → 高德地图地理编码 → (lat, lng)
  3. 查所有 Hub（level=2，城市配送中心级别）
  4. 对仓库坐标和收货坐标分别找最近的 Hub
  5. 如果 originHub == destHub → 同城单，不跨城
  6. 如果 originHub ≠ destHub → 跨城单，isCrossCity = 1
```

---

## 4. 中转站作业看板：五阶段操作详解

**前端页面**：`Admin/HubOperations.vue`

这是一个五阶段看板，管理员选择某个 Hub 后，可以逐阶段处理该 Hub 下的订单。

### 页面初始化

```
onMounted()
  → loadHubsWithOrders()
    → GET /api/logistics/national-network/hubs?level=2   // 所有城市配送中心
    → 对每个 Hub 并行: GET /api/logistics/dispatch/collection-queue?originHubId={id}
    → 过滤: 只保留有待揽收订单的 Hub
    → 自动选中第一个 Hub → loadAll()
      → 并行加载五个阶段的数据
```

### Stage 0: 待揽收

展示该 Hub 下所有**跨城已发货但尚未入干线的订单**（来自 `dispatch_pool` 表，`dispatch_origin_type=0`）。

| 操作 | 前端函数 | API 调用 | 后端逻辑 |
|---|---|---|---|
| 确认已到仓（单条） | `doCreateInbound(item)` | `POST /logistics/hub-operations/inbound` | 创建 `hub_inbound_record`，状态 PENDING |
| 一键全部到仓 | `batchCreateInbound()` | 对每个 poolItem 调用 `createHubInboundRecord` | 逐条创建入库记录 |

### Stage 1: 待入库

展示该 Hub 下所有**状态为 PENDING 的入库记录**。

| 操作 | 前端函数 | API 调用 | 后端逻辑 |
|---|---|---|---|
| 确认入库 → 进入分拣 | `doConfirmInbound(rec)` | ① `PUT /logistics/hub-operations/inbound/{id}/confirm` → ② `POST /logistics/hub-operations/sorting` | ① 入库记录状态 PENDING→DONE，记录入库时间 ② 创建 `hub_sorting_record`，状态 PENDING |
| 手动登记 | `submitInbound()` | `POST /logistics/hub-operations/inbound` | 创建入库记录 |
| 全部入库 | `batchConfirmInbound()` | 对每条记录串行执行 确认+创建分拣 | 同上，逐条处理 |

### Stage 2: 待分拣

展示该 Hub 下所有**状态为 PENDING 的分拣记录**。

| 操作 | 前端函数 | API 调用 | 后端逻辑 |
|---|---|---|---|
| 手动登记分拣 | `submitSorting()` | `POST /logistics/hub-operations/sorting` | 创建分拣记录（可指定 destHubId） |
| 前往全国干线规划 | — | — | 页面提示"前往全国干线规划，自动分配批次" |

> **关键点**：分拣阶段实际上是通过 MCMF 干线规划来自动分配批次的，不是手动操作。

### Stage 3: 待出库

展示该 Hub 下所有**状态为 PENDING 的出库记录**。

- **当前无操作按钮**，纯展示
- 出库记录在 MCMF 规划创建干线批次时自动生成

### Stage 4: 干线运输

展示从该 Hub 发出的所有**干线运输批次**（来自 `inter_city_batch` 表）。

| 操作 | 前端函数 | API 调用 | 后端逻辑 |
|---|---|---|---|
| 标记发车 | `doDepartBatch(id)` | `POST /logistics/dispatch/national/{id}/depart` | `interCityBatchService.onDeparture(batchId)` → 状态 CREATED→DEPARTED |
| 标记到达 | `doArriveBatch(id)` | `POST /logistics/dispatch/national/{id}/arrive` | `interCityBatchService.onArrival(batchId)` → 状态 DEPARTED→ARRIVED + 将批次内订单写入目标城市调度池 |
| 批量发车 | `batchTrunkAction('depart')` | 对每个 CREATED 批次调 departBatch | 同上，逐条 |
| 批量到达 | `batchTrunkAction('arrive')` | 对每个 DEPARTED 批次调 arriveBatch | 同上，逐条 |

---

## 5. 全国干线调度：MCMF 运力规划

**前端页面**：`Admin/Dashboard.vue` 中的"全国干线"Tab

### 操作流程

```
1. 点击"规划运力" 按钮
   → POST /api/logistics/dispatch/national/plan?planDate=...&llmReferenceDate=...
     → DispatchController.triggerNationalPlan()
       → flowPlanService.triggerManually(date, llmRef)
         → 收集各 Hub 的供给量/需求量（从 dispatch_pool 汇总）
         → LLM 费率校准（调用 DeepSeek API 修正 HubLink 上的运输费用）
         → 执行 MCMF 算法（单商品或多商品模式）
         → 生成 FlowPlan + FlowPlanEdge（各边的流量分配）
         → 创建 InterCityBatch（干线运输批次）
         → 创建 HubOutboundRecord（出库记录）
         → 更新 HubSortingRecord（分拣记录 → ASSIGNED）
         → 写入 dispatch_pool（目标端待调度条目）

2. 查看规划结果
   → GET /api/logistics/dispatch/national/latest-plan
   → 返回 FlowPlanDetailDTO（含各边流量、各 OD 对路由方案）

3. 发车/到达（在中转站看板操作，见上一章）
```

### MCMF 触发时发生了什么

```
flowPlanService.triggerManually(date, llmRef)
  ├── Step 1: 收集各 Hub 的供给量/需求量
  │     → 遍历 dispatch_pool，按 originHubId/destHubId 汇总
  │
  ├── Step 2: LLM 费率校准（可选）
  │     → LlmEdgeCostCalibrationServiceImpl.calibrate()
  │     → 调用 DeepSeek API，传入各 HubLink 的基本信息
  │     → 返回校准后的 costPerUnit
  │
  ├── Step 3: 执行 MCMF 算法
  │     → 单商品模式: McmfServiceImpl.computeMinCostFlow()
  │     → 多商品模式: McmfServiceImpl.computeMultiCommodityFlow()
  │
  ├── Step 4: 保存规划结果
  │     → 写 flow_plan 表
  │     → 写 flow_plan_edge 表（各边流量）
  │
  └── Step 5: 创建运输批次
        → 遍历有流量的边，创建 InterCityBatch
        → 对每条边上的订单：
          → 创建 HubOutboundRecord（出库记录）
          → 更新 HubSortingRecord.sortResult = ASSIGNED
          → 写入目标端 dispatch_pool（dispatchOriginType=1，跨城到达待调度）
```

---

## 6. 末端调度：K-Means 聚类 + LLM 建议

**前端页面**：`Admin/Dashboard.vue` 中的"末端调度"Tab

### 操作流程

```
1. 加载调度池
   → GET /api/logistics/dispatch/pool?destHubId=...
   → 返回该城市下所有待调度订单（同城单 + 跨城到达单）

2. 调度预览
   → GET /api/logistics/dispatch/preview?k=...&skipLlm=...
     → DispatchController.previewDispatch()
       ├── K-Means 聚类
       │     → clusterService.cluster(items, k)
       │     → KMeansClusterServiceImpl.cluster()
       │       → Haversine 距离计算
       │       → 均匀抽样初始化
       │       → 迭代至收敛（最多 100 次）
       │       → 返回 List<ClusterResultDTO>
       │
       └── LLM 全局建议（skipLlm=false 时）
             → globalDispatchAdviceService.advise(clusters, warehouseId, lat, lng)
             → 调用 DeepSeek API，传入各簇的地理分布
             → 返回: 建议批次数、每簇是否经 Hub、紧急程度

3. 执行调度
   → POST /api/logistics/dispatch/execute
     → DispatchController.executeDispatch()
       → 对每个批次:
         → logisticsBatchService.createBatch(batchReq)
           → 创建 LogisticsBatch 记录
           → 创建 Delivery 记录（分配运输员）
           → 规划配送路线（A* 或 LLM 策略）
         → markDispatched(orderIds, batchId)
           → dispatch_pool 状态 0→1
```

---

## 7. 运输员配送：状态机与路径规划

**前端页面**：`Driver/Delivery.vue`

### 配送状态机

```
待承接(0) → 已接单(1) → 配送中(2) → 已送达(3)
                                      ↘ 已取消(-1)
```

### 操作详解

| 操作 | API 调用 | 后端逻辑 |
|---|---|---|
| **确认承接** | `POST /drivers/deliveries/{id}/accept` | 分配车辆，状态 0→1 |
| **开始配送** | `PUT /drivers/deliveries/{id}/status` + status=2 | 状态 1→2 |
| **确认完成** | `PUT /drivers/deliveries/{id}/status` + status=3 | 状态 2→3，更新订单状态 |
| **逐站送达** | `PUT /drivers/deliveries/{id}/complete-stop` | 多停靠点时，标记单个订单已送达 |
| **查看导航** | 跳转 `/driver/home/navigation?deliveryId=xxx` | 加载 Leaflet 地图 + 路线 GeoJSON |

### 路径规划

运输员查看导航时，系统调用路径规划服务：

```
RouteStrategy.plan(startLat, startLon, endLat, endLon, plannedTime)
```

三种策略可选（通过配置切换）：
- **AStarRouteStrategy**：纯 GraphHopper A* 路径规划
- **LlmJudgeRouteStrategy**：A* 生成 3 条候选 + LLM 裁决最优
- **LlmWaypointRouteStrategy**：LLM 指定绕行路点 + A* 分段执行

---

## 8. 智能算法详解

### 8.1 MCMF 最小费用最大流

**文件**：`logistics-service/.../impl/McmfServiceImpl.java`（456 行）

#### 单商品模式：SSP + SPFA

**核心思想**：将全国物流网络建模为流量网络，用 SSP（逐次最短路）算法找到最小费用的流量分配方案。

**建图过程**：

```
1. 拆点：每个 Hub 拆成入港(in)和出港(out)两个节点
   - out(i) = 2*i,  in(i) = 2*i+1
   - 超级源 S = 2*hCount, 超级汇 T = 2*hCount + 1

2. 建边：
   - Hub 内换载边：in(i) → out(i)，容量 = max(总供给,总需求)，费用 = 0
   - 实际运输边：out(i) → in(j)，容量 = link.capacityDaily，费用 = costPerUnit × 100
   - 超级源 → 各 Hub 出港，容量 = 该 Hub 的供给量
   - 各 Hub 入港 → 超级汇，容量 = 该 Hub 的需求量
   - 每条正向边配一条反向边（容量 0，费用为负）

3. SSP 主循环：
   while (SPFA 找到 S→T 的最短费用路径) {
     沿路径增广（推送瓶颈流量）
     更新残差网络
   }

4. 收集结果：遍历原始边，flow = 原始容量 - 残余容量
```

**为什么费用乘 100？** 避免浮点误差——所有费用取整后用 `long[]` 存储，最终结果除以 100 还原为 `BigDecimal`。

#### 多商品模式：Dijkstra-SSP

**与单商品的本质区别**：每个 OD 对（Origin-Destination）有独立的供需，不会出现"同一 Hub 供给与需求代数抵消"的建模缺陷。

```
1. 构建残差图：costAdj（费用邻接表）+ resCap（剩余容量，跨商品共享）

2. 按 OD 需求量降序处理（高需求优先占容量）：
   for each OD pair:
     while (该 OD 还有剩余需求):
       dijkstraOnResidual(originHub, destHub)  // 找最短费用路径
       bottleneck = 路径上最小残差
       增广 min(bottleneck, remaining) 单位流量
       更新共享残差容量

3. 汇总各边流量
```

**Dijkstra 实现**：标准优先队列 Dijkstra，O((V+E)logV)，只走残差 > 0 的边。

---

### 8.2 K-Means 末端聚类

**文件**：`logistics-service/.../impl/KMeansClusterServiceImpl.java`（110 行）

#### 算法流程

```
1. 确定聚类数 k
   - 若未指定，默认 k = ceil(sqrt(n))
   - k = min(k, n)

2. 均匀抽样初始化
   - 将 n 个订单按某种顺序排列
   - 均匀抽取 k 个作为初始中心：
     idx = round(i / (k-1) * (n-1))  (i=0,1,...,k-1)
   - 不是随机选，保证初始中心在数据分布上均匀覆盖

3. 迭代（最多 100 次）：
   E-step: 每个订单分配到 Haversine 距离最近的中心
   M-step: 重新计算各簇中心的经纬度算术平均
   收敛条件: 无任何订单改变簇归属 → 提前终止

4. 输出：List<ClusterResultDTO>，每个簇含中心坐标、成员订单列表
```

#### Haversine 距离

```java
// 地球半径 6371 km
// 标准球面距离公式
d = 2R × arcsin(√(sin²((lat2-lat1)/2) + cos(lat1)×cos(lat2)×sin²((lon2-lon1)/2)))
```

> **注意**：簇中心重计算用的是经纬度算术平均，在小区域内误差可接受（城市级别）。

---

### 8.3 A* 路径规划

**文件**：`logistics-service/.../impl/AStarRouteStrategy.java`

#### 基础 A*

```
1. 经纬度 → GraphHopper LocationIndex → 最近路网节点
2. A* 搜索：
   - openSet: PriorityQueue，按 f(n) = g(n) + h(n) 升序
   - g(n): 起点到当前节点的实际距离
   - h(n): Haversine(当前节点, 终点)，可采纳的启发函数
   - 每次取 f 最小的节点扩展
3. 重建路径 → 输出 GeoJSON LineString
```

#### Weighted A*（多候选路线生成）

通过调整 `epsilon` 参数生成不同的备选路线：

| epsilon | 效果 | 用途 |
|---|---|---|
| 1.0 | 标准最优 A* | 最短路线 |
| 1.8 | 膨胀启发值，更激进趋向终点 | 较快但可能次优 |
| 3.5 | 极度膨胀 | 明显不同的备选路线 |

```
h(n) = epsilon × Haversine(当前节点, 终点)
```

epsilon > 1 时不再保证最优，但能快速生成不同路线供 LLM 裁决。

---

### 8.4 LLM 裁决策略

**文件**：`logistics-service/.../impl/LlmJudgeRouteStrategy.java`

```
1. 对同一 OD 调用三次 A*（epsilon=1.0, 1.8, 3.5），生成三条候选路线
2. 构造 Prompt：
   - 三条路线的距离、预计时间、途经道路类型
   - 要求 LLM 返回 JSON: { recommended: 1|2|3, reason: "..." }
3. 调用 DeepSeek API
4. 解析 JSON，返回推荐路线
5. 降级容错：如果 LLM 返回异常，回退到 epsilon=1.0 的最优路线
```

### 8.5 LLM 路点策略

**文件**：`logistics-service/.../impl/LlmWaypointRouteStrategy.java`

```
1. 将起点、终点、途经关键点信息发给 LLM
2. LLM 决定绕行路点（战略层）: "建议经过 XX 路/YY 桥"
3. A* 分段执行（战术层）: 起点→路点→终点
4. 拼接多段 GeoJSON
```

### 8.6 LLM 费率校准

**文件**：`logistics-service/.../impl/LlmEdgeCostCalibrationServiceImpl.java`

```
1. 收集各 HubLink 的基本信息（距离、城市、货物类型）
2. 构造 Prompt：要求 LLM 根据当前市场情况修正运输费用
3. DeepSeek API 返回各边的校准后费率
4. 更新内存中的 costPerUnit（不修改数据库）
```

### 8.7 LLM 全局调度建议

**文件**：`logistics-service/.../impl/LlmFlowPlanAdviceServiceImpl.java`

```
1. 输入：K-Means 聚类结果 + 仓库坐标
2. 构造 Prompt：各簇的地理分布、订单量、距仓库距离
3. DeepSeek API 返回：
   - 建议批次数
   - 每簇是否经 Hub 中转
   - 紧急程度判断
4. 与 K-Means 结果合并，返回 DispatchPreviewDTO
```

---

## 9. 关键数据表结构

### dispatch_pool — 调度池

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| order_id | BIGINT | 关联订单 |
| shop_id | BIGINT | 商户 ID |
| warehouse_id | BIGINT | 仓库 ID |
| end_address | VARCHAR | 收货地址 |
| end_lat / end_lng | DOUBLE | 收货坐标 |
| origin_hub_id | BIGINT | 发货 Hub |
| dest_hub_id | BIGINT | 收货 Hub |
| is_cross_city | TINYINT | 是否跨城 (0/1) |
| dispatch_origin_type | TINYINT | 0=跨城揽收待处理, 1=跨城到达待调度, 2=个人取件 |
| dispatch_origin_lat/lng | DOUBLE | 调度起点坐标 |
| status | TINYINT | 0=待调度, 1=已调度 |
| batch_id | BIGINT | 分配的配送批次 |

### national_hub — 全国 Hub 节点

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| name | VARCHAR | Hub 名称 |
| city | VARCHAR | 所在城市 |
| lat / lng | DOUBLE | 坐标 |
| level | INT | 层级 (1=全国枢纽, 2=城市配送中心) |
| daily_capacity | INT | 日处理能力 |

### hub_link — Hub 间连线

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| from_hub_id | BIGINT | 起点 Hub |
| to_hub_id | BIGINT | 终点 Hub |
| distance_km | DOUBLE | 距离 |
| capacity_daily | INT | 日运量上限 |
| cost_per_unit | DECIMAL | 单位运输费用 |

### inter_city_batch — 干线运输批次

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| from_hub_id | BIGINT | 发货 Hub |
| to_hub_id | BIGINT | 收货 Hub |
| status | VARCHAR | CREATED / DEPARTED / ARRIVED |
| order_ids | VARCHAR | 批次内订单 ID 列表（逗号分隔）|
| planned_ship_time | DATETIME | 计划发车时间 |
| actual_depart_time | DATETIME | 实际发车时间 |
| actual_arrive_time | DATETIME | 实际到达时间 |

### logistics_batch — 末端配送批次

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| hub_id | BIGINT | 所属 Hub |
| warehouse_id | BIGINT | 出发仓库 |
| driver_id | BIGINT | 分配的运输员 |
| vehicle_id | BIGINT | 分配的车辆 |
| status | INT | 0=待发车, 1=配送中, 2=已完成 |
| planned_route | TEXT | GeoJSON 路线 |

### hub_inbound_record / hub_sorting_record / hub_outbound_record

三个表结构类似：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| hub_id | BIGINT | 所属 Hub |
| order_id | BIGINT | 关联订单 |
| status | VARCHAR | PENDING / DONE（入库/出库）或 PENDING / ASSIGNED（分拣）|
| arrive_time / inbound_time / sort_time / outbound_time | DATETIME | 各阶段时间戳 |

---

## 10. API 速查表

### 订单服务 (order-service, port 8085)

| Method | URI | 说明 |
|---|---|---|
| POST | `/api/orders` | B2C 下单 |
| POST | `/api/orders/personal-shipment` | C2C 个人寄件 |
| PUT | `/api/orders/{id}/pay` | 支付订单 |
| POST | `/api/orders/{id}/ship` | 商户发货 |

### Hub 作业 (logistics-service, port 8087)

| Method | URI | 说明 |
|---|---|---|
| GET | `/api/logistics/hub-operations/inbound` | 查询入库记录 |
| POST | `/api/logistics/hub-operations/inbound` | 创建入库记录 |
| PUT | `/api/logistics/hub-operations/inbound/{id}/confirm` | 确认入库 |
| GET | `/api/logistics/hub-operations/sorting` | 查询分拣记录 |
| POST | `/api/logistics/hub-operations/sorting` | 创建分拣记录 |
| PUT | `/api/logistics/hub-operations/sorting/{id}/assign` | 分配分拣到批次 |
| GET | `/api/logistics/hub-operations/outbound` | 查询出库记录 |
| POST | `/api/logistics/hub-operations/outbound` | 创建出库记录 |
| PUT | `/api/logistics/hub-operations/outbound/{id}/confirm` | 确认出库 |

### 调度服务 (logistics-service, port 8087)

| Method | URI | 说明 |
|---|---|---|
| GET | `/api/logistics/dispatch/pool` | 查询调度池 |
| GET | `/api/logistics/dispatch/collection-queue` | 查询揽收队列 |
| GET | `/api/logistics/dispatch/preview` | K-Means+LLM 调度预览 |
| POST | `/api/logistics/dispatch/execute` | 执行调度 |
| GET | `/api/logistics/dispatch/check-urgency` | LLM 紧急性检测 |
| POST | `/api/logistics/dispatch/national/plan` | 触发 MCMF 干线规划 |
| GET | `/api/logistics/dispatch/national/batches` | 查询干线批次 |
| GET | `/api/logistics/dispatch/national/latest-plan` | 查询最新规划结果 |
| POST | `/api/logistics/dispatch/national/{id}/depart` | 标记发车 |
| POST | `/api/logistics/dispatch/national/{id}/arrive` | 标记到达 |

### 全国网络 (logistics-service, port 8087)

| Method | URI | 说明 |
|---|---|---|
| GET | `/api/logistics/national-network/hubs` | 查询 Hub 列表 |
| GET | `/api/logistics/national-network/hubs/{id}` | 查询单个 Hub |
| POST | `/api/logistics/national-network/hubs` | 新增 Hub |
| PUT | `/api/logistics/national-network/hubs/{id}` | 更新 Hub |
| GET | `/api/logistics/national-network/links` | 查询 Hub 连线 |
| POST | `/api/logistics/national-network/links` | 新增连线 |
| GET | `/api/logistics/national-network/topology` | 查询网络拓扑 |

### 运输员服务 (driver-service, port 8086)

| Method | URI | 说明 |
|---|---|---|
| POST | `/api/drivers/deliveries/{id}/accept` | 承接任务 |
| PUT | `/api/drivers/deliveries/{id}/status` | 更新配送状态 |
| PUT | `/api/drivers/deliveries/{id}/complete-stop` | 逐站送达 |
| POST | `/api/drivers/deliveries/{id}/cancel` | 取消配送 |

 

本科毕业论文（设计）

UNDERGRADUATE THESIS (PROJECT)

题 目：*基于微服务架构的智能物流管理系统*

学    院：*计算机科学与工程学院*

专    业：*网络空间安全*

学    号：*21122119*

学生姓名：*付苗*

指导教师：*牛志华*

起讫日期：*2025年2月20日起2025年6月2日止*

|            |                |
|------------|----------------|
| 姓名：付苗 | 学号：21122119 |
| 论文题目： |                |

**原 创 性 声 明**

本人声明：所呈交的论文是本人在指导教师指导下进行的研究工作。除了文中特别加以标注和致谢的地方外，论文中不包含其他人已发表或撰写过的研究成果。参与同一工作的其他同志对本研究所做的任何贡献均已在论文中作了明确的说明并表示了谢意。

签 名：*        *日 期：*        *

**本论文使用授权说明**

本人完全了解上海大学有关保留、使用学位论文的规定，即：学校有权保留论文及送交论文复印件，允许论文被查阅和借阅；学校可以公布论文的全部或部分内容。

（**保密的论文在解密后应遵守此规定**）

签 名：*      *指导教师签名：*      *日期：*      *

# 

摘　要

随着电商交易、个人寄件和跨城配送需求不断增加，物流系统已经不再只是记录订单状态的后台工具，而需要同时处理多角色协同、库存履约、运输执行、路径规划和运力分配等问题。传统单体系统在业务扩展、模块维护和复杂调度方面容易形成耦合瓶颈。针对这些问题，本文基于当前项目实现，设计并完成了一套基于微服务架构的智能物流管理系统，围绕顾客下单与寄件、商户商品与库存管理、司机配送执行、仓储作业、智能导航和全国干线调度等场景展开系统建设。

系统后端采用 Spring Boot 3.1.5、Spring Cloud 2022.0.4 与 Spring Cloud Alibaba 2022.0.0.0 构建 Maven 多模块微服务工程，实际划分为 common 公共模块、认证服务、用户服务、顾客服务、商店与库存服务、订单服务、司机服务、物流服务和网关服务。各业务服务通过 Nacos 完成服务注册发现，通过 Spring Cloud Gateway 提供统一入口，并使用 OpenFeign 完成必要的同步服务调用。系统使用 MyBatis-Plus 访问 MySQL 数据库，使用 Redis 承载 Spring Cache 缓存，使用 RabbitMQ 处理库存扣减、订单入调度池、路线绑定、状态联动、Hub 到达和末端任务激活等异步协作流程。

前端采用 Vue3、Vite、TypeScript、Vue Router、Pinia、Element Plus 与 Leaflet 构建多角色单页应用。系统为管理员、顾客、商户和司机分别提供独立的页面入口，覆盖用户审核、商品浏览、个人寄件、订单发货、仓库与库存维护、配送接单、GPS 上报、物流轨迹查看、城市末端调度、全国网络拓扑展示和运力规划结果查看等功能。部署方面，项目提供 Dockerfile 与 Docker Compose 编排文件，对 MySQL、Redis、RabbitMQ、Nacos、各后端服务、网关和前端 Nginx 容器进行统一组织。

本文将智能导航与调度能力作为与后端业务服务、前端交互界面并列的核心部分进行设计与实现。物流服务集成 GraphHopper 9.1 读取 OpenStreetMap 路网数据，并在底层道路图上实现 A\* 路径规划；系统还提供可配置的 LLM_JUDGE 与 LLM_WAYPOINT 路线策略，分别用于多候选路线裁决和战略途经点建议。在城市末端场景中，系统使用 K-Means 对待调度订单进行空间聚类，并结合规则或大语言模型生成批次建议；在跨城干线场景中，系统以全国 Hub 网络为基础，采用多商品最小费用流思路和 Dijkstra-SSP 求解方式完成运力分配，并将规划结果落库展示。系统运行结果表明，该原型能够支撑较完整的物流履约链路，也验证了微服务架构与智能导航算法在物流管理场景中的结合方式。

**关键词：**微服务架构；智能物流；路径规划；GraphHopper；Hub-and-Spoke物流网络

ABSTRACT

With the growth of e-commerce, personal shipping, and inter-city delivery, logistics systems are expected to do more than record order states. They need to coordinate multiple roles, manage inventory fulfillment, support delivery execution, plan routes, and allocate transportation capacity. Traditional monolithic systems often become difficult to maintain and extend in such scenarios. Based on the current project implementation, this thesis designs and implements an intelligent logistics management system using a microservice architecture. The system covers customer shopping and personal shipment, merchant product and inventory management, driver delivery execution, warehouse operations, intelligent navigation, and nationwide trunk-line scheduling.

The backend is implemented as a Maven multi-module Spring Cloud project using Spring Boot 3.1.5, Spring Cloud 2022.0.4, and Spring Cloud Alibaba 2022.0.0.0. The actual modules include a common module, authentication service, user service, customer service, shop and inventory service, order service, driver service, logistics service, and gateway service. Services are registered and discovered through Nacos, accessed through Spring Cloud Gateway, and coordinated through OpenFeign when synchronous calls are required. MyBatis-Plus and MySQL are used for data persistence, Redis is used as the Spring Cache backend, and RabbitMQ is used for asynchronous collaboration such as stock deduction, dispatch-pool insertion, route binding, status synchronization, hub arrival notification, and last-mile task activation.

The frontend is a Vue3 single-page application built with Vite, TypeScript, Vue Router, Pinia, Element Plus, and Leaflet. Separate workspaces are provided for administrators, customers, merchants, and drivers. The implemented pages include user review, product browsing, personal shipment creation, order shipping, warehouse and stock management, delivery acceptance, GPS reporting, route tracking, city-level dispatching, national network visualization, and flow-plan result display. The project also provides Dockerfiles and a Docker Compose configuration to orchestrate MySQL, Redis, RabbitMQ, Nacos, backend services, the gateway, and the Nginx-based frontend container.

This thesis treats intelligent navigation and scheduling as a core part parallel to backend services and frontend interaction. The logistics service integrates GraphHopper 9.1 to load OpenStreetMap road data and implements A\*-based route planning over the road graph. It also provides configurable LLM_JUDGE and LLM_WAYPOINT strategies for candidate-route selection and strategic waypoint suggestion. For last-mile dispatching, K-Means is used to cluster pending orders and generate batch suggestions with rule-based or large-language-model assistance. For inter-city trunk-line scheduling, the system models the national Hub network and applies a multi-commodity minimum-cost-flow approach based on Dijkstra-SSP. The implementation shows that the prototype can support a relatively complete logistics fulfillment process and demonstrates a feasible integration of microservice architecture and intelligent navigation algorithms in logistics management.

**Keywords:** Microservice Architecture; Intelligent Logistics; Route Planning; GraphHopper; Hub-and-Spoke Logistics Network

# 目录

[摘　要](#摘要)

[ABSTRACT](#abstract)

[**1 绪论**](#1-绪论)

[1.1 研究背景](#11-研究背景)

[1.2 研究意义](#12-研究意义)

[1.3 主要研究内容](#13-主要研究内容)

[1.4 各章内容与结构](#14-各章内容与架构)

[**2 相关技术综述**](#2-相关技术综述)

[2.1 微服务架构相关技术](#21-微服务架构相关技术)

[2.2 后端数据与缓存技术](#22-后端其他技术)

[2.3 前端技术栈](#23-前端技术栈)

[2.4 容器化部署技术](#24-容器化部署技术)

[2.5 智能导航与调度算法技术](#25-智能导航与调度算法技术)

[**3 系统设计**](#3-系统设计)

[3.1 需求分析](#31-需求分析)

[3.2 总体架构设计](#32-架构设计)

[3.3 后端服务设计](#33-后端服务设计)

[3.4 智能导航与调度设计](#34-智能导航与调度设计)

[3.5 前端设计](#35-前端设计)

[3.6 本章小结](#36-本章小结)

[**4 系统实现**](#4-系统实现)

[4.1 微服务运行环境实现](#41-微服务运行环境实现)

[4.2 后端业务服务实现](#42-后端业务服务实现)

[4.3 智能导航与调度实现](#43-智能导航与调度实现)

[4.4 前端实现](#44-前端实现)

[4.5 容器化部署实现](#45-容器化部署实现)

[4.6 本章小结](#46-本章小结)

[**5 系统展示**](#5-系统展示)

[5.1 并发性能测试](#51-并发性能测试)

[5.2 个人用户功能展示](#52-个人用户功能展示)

[5.3 商户用户功能展示](#53-商户用户功能展示)

[5.4 司机用户功能展示](#54-司机用户功能展示)

[5.5 管理员功能展示](#55-管理员功能展示)

[**6 结论**](#结论)

[6.1 总结](#61-总结)

[6.2 不足与展望](#62-不足与展望)

[参考文献](#参考文献)

[致谢](#致谢)

# **1 绪论**

本章主要介绍论文的研究背景、研究意义、主要研究内容以及组织结构。首先讨论了物流行业的背景、智能物流行业使用微服务架构的原因及意义； 接着， 概述了微服务架构及相关技术的研究现状，包括其思想演进及技术栈分析；然后，明确了本文的研究内容，即基于微服务架构的智能物流管理系统；最后，简要介绍了论文的组织结构，包括各章的主要内容以及结构。

## 1.1 研究背景

物流行业作为支撑国民经济发展的基础性、战略性、先导性产业，在全球化和电子商务迅猛发展的浪潮下，其重要性日益凸显。然而，传统的物流管理模式在应对日益增长的业务复杂性、时效性要求以及成本控制压力时，逐渐显现出诸多瓶颈。当前物流行业普遍面临着高成本、低效率、信息孤岛等痛点。例如，中国的物流成本占GDP比重曾高达14.1%，远高于许多发达国家的平均水平，同时仓库闲置率等资源浪费问题也较为突出。这些问题严重制约了物流行业的进一步发展和整体服务水平的提升。

为应对这些挑战，在物联网、大数据、人工智能以及云计算等新一代信息技术的驱动下，物流行业正经历着深刻的数字化和智能化转型\[1\]。智能物流管理系统应运而生，旨在通过技术创新优化物流运作的各个环节。物联网技术通过各类传感器和标识设备，实现了对货物、车辆、仓库等物流要素状态的实时感知与数据采集，有效打破了传统模式下的信息壁垒\[2\]。大数据技术则能够对海量的物流运作数据进行深度挖掘与分析，为路径优化、需求预测、库存管理等方面提供决策支持，显著提升运营效率\[3\]。人工智能技术，尤其在智能调度、风险预警、自动化处理等方面的应用，进一步提升了物流系统的自动化水平和智能化程度\[4\]。

然而，随着业务的扩展和深化，传统的单体式系统架构在支撑复杂的智能物流应用时，其固有的技术局限性愈发明显。单体架构系统往往功能模块耦合度高，任何微小的改动或升级都可能影响整个系统的稳定性，导致维护成本高、迭代周期长，难以快速响应市场变化和满足高并发、高可用的需求\[5\]。特别是在物流高峰期（如电商促销活动期间），单体系统容易因资源瓶颈而出现性能问题甚至服务中断。因此，寻求一种更灵活、更具扩展性、更适应现代物流业务动态需求的系统架构成为业界的迫切需求。

在此背景下，微服务架构作为一种新兴的软件架构模式，受到了广泛关注。微服务架构倡导将复杂的系统拆分为一组小而自治的服务单元，每个服务单元围绕特定的业务能力构建，可以独立开发、部署、扩展和管理\[6\]。这种架构模式天然契合了智能物流系统对灵活性、可扩展性和高可用性的要求。通过采用微服务架构，可以将智能物流管理系统中的订单管理、库存管理、运输调度、配送跟踪、路径优化等核心功能模块解耦为独立的服务，从而提高系统的整体韧性、开发效率和技术选型的灵活性。

## 1.2 研究意义

基于上述背景，本研究旨在设计并实现一个基于微服务架构的智能物流管理系统。该系统的研究与实现具有重要的理论价值和现实意义：

1\. 提高物流运作效率： 通过微服务实现各业务模块的解耦和高效协同，结合路径优化、智能调度等功能，可以显著缩短运输时间，提高车辆、仓储等资源的利用率，从而全面提升物流运作效率。

2\. 降低运营成本： 微服务架构的弹性伸缩能力有助于根据业务负载动态调整资源，避免资源浪费。同时，优化的路径规划和库存管理也能有效降低运输成本和仓储成本。系统自动化水平的提高也能减少对人力的依赖，降低人力成本。

3\. 提升用户体验： 系统提供实时的订单跟踪、精准的预计送达时间以及便捷的查询服务，能够显著提升货主、承运方以及最终客户的使用体验和满意度。各服务独立部署和升级的特性也能保障核心业务的连续性，减少因系统维护带来的服务中断。

4\. 促进物流行业数字化转型： 本研究探索将先进的微服务架构应用于复杂的物流管理场景，为传统物流企业向数字化、智能化转型提供了可借鉴的技术方案和实践经验。系统的模块化设计也便于未来集成更多新兴技术（如区块链、数字孪生等），推动行业的持续创新。

5\. 验证微服务架构优势： 通过在智能物流领域的具体实践，可以深入验证微服务架构在应对高并发、复杂业务逻辑、快速迭代需求方面的优势和适用性，为相关领域的系统架构选型提供参考。

## 1.3 主要研究内容

本课题的主要工作不是单独实现某一个物流页面或算法接口，而是围绕“业务履约 + 智能导航 + 多角色协同”构建一个能够运行的系统原型。结合当前项目源码，本文的研究内容主要包括以下几个方面：

1. 微服务架构与基础支撑能力设计。系统采用 Maven 多模块方式组织后端工程，将认证、用户审核、顾客资料、商户商品与库存、订单、司机配送、物流服务和网关接入拆分为独立服务，并通过 Nacos、Spring Cloud Gateway、OpenFeign、RabbitMQ 等组件完成服务注册、统一入口、同步调用和异步协作。

2. 多角色物流业务链路实现。系统围绕管理员、顾客、商户和司机四类角色，完成注册登录、商户与司机审核、商品浏览、顾客下单、个人寄件、商户发货、库存扣减、配送接单、GPS 上报、订单签收和仓储作业等功能，使订单能够从创建进入可追踪的履约流程。

3. 智能导航与调度模块实现。系统在 `logistics-service` 中集成 GraphHopper 路网引擎，基于 OpenStreetMap 数据实现 A\* 路径规划，并提供 LLM 辅助的候选路线裁决和战略路点建议；在城市末端配送中使用 K-Means 对待调度订单进行空间聚类；在跨城干线场景中基于全国 Hub 网络实现多商品最小费用流规划。

4. 前端多角色交互与可视化实现。前端采用 Vue3、Vite、TypeScript、Element Plus、Pinia 与 Leaflet，按照角色组织页面入口，并通过地图组件展示路线、轨迹、城市调度结果和全国网络拓扑，使后端业务与算法结果能够被用户直观使用。

5. 系统集成与部署验证。项目通过 Dockerfile 和 Docker Compose 组织 MySQL、Redis、RabbitMQ、Nacos、各后端服务、网关和前端 Nginx 容器，并挂载 OSM 地图文件与 GraphHopper 图缓存目录，以验证系统在统一环境中的部署和运行能力。

## 1.4 各章内容与架构

全文围绕“需求分析—系统设计—系统实现—系统展示—总结与展望”的思路展开，并将智能导航与调度作为与后端服务、前端界面并列的核心内容进行说明。

第一章为绪论，主要介绍选题背景、研究意义、主要研究内容以及论文整体结构。

第二章为相关技术综述，介绍系统使用的微服务治理、服务调用、消息队列、MySQL、MyBatis-Plus、Redis、Vue3、Vite、Docker 等工程技术，并对 GraphHopper、A\*、K-Means、大语言模型和最小费用流等智能导航与调度技术进行说明。

第三章为系统设计，首先分析系统角色和功能需求，然后给出总体架构、后端服务设计、智能导航与调度设计以及前端页面设计。该章重点说明各模块的职责边界和协作方式。

第四章为系统实现，结合当前项目代码说明微服务运行环境、认证鉴权、订单履约、库存管理、司机配送、路径规划、末端调度、全国运力规划、前端页面和容器化部署的具体实现。

第五章为系统展示，围绕性能测试思路和顾客、商户、司机、管理员四类角色展示系统主要功能，并说明智能导航、城市末端调度和全国运力规划等特色模块的运行效果。

结论部分总结本文完成的主要工作，分析当前系统在数据来源、生产部署、算法能力和测试覆盖方面仍存在的不足，并提出后续优化方向。

# **2 相关技术综述**

本章围绕基于微服务架构的智能物流管理系统所采用的关键技术展开综述。内容主要包括微服务治理与服务通信技术、后端数据访问与缓存技术、前端开发与地图可视化技术、容器化部署技术，以及系统中涉及的路径规划、聚类调度、运力优化与大语言模型辅助决策等智能导航与调度算法技术。这些技术共同构成了系统设计与实现的理论基础与工程支撑。

## 2.1 微服务架构相关技术

微服务架构是一种软件工程概念，主要强调在后端项目管理中实现高内聚、低耦合。在传统单体架构中，随着需求的不断引入，软件各个组件紧密耦合，管理成本越来越高。在微服务架构中，每个单位可以根据独立的需求设计、开发、测试、部署和维护，因而可以实现低成本的扩展和维护。

对于智能物流管理系统而言，其业务流程复杂、涉及模块众多，且对系统的可用性、可扩展性要求高。微服务架构能够很好地满足这些需求：将不同的物流功能拆分为独立服务，便于功能的快速迭代和优化；可以根据业务高峰独立扩展订单服务或调度服务；单个模块的升级或故障不会影响其他核心业务的运行。

为了实现微服务架构，首先需要对各个子模块进行精确划分与设计，这点将在下一章详细讨论。此外，微服务架构在实现中需要引入多种中间件，将各个部分有机地组合在一起。

### 2.1.1 注册中心

在微服务架构下，各服务可能独立启动、停止或扩容，因此系统需要注册中心维护服务实例信息，并提供服务发现和负载均衡支持。注册中心通常通过心跳机制感知实例状态，常见实现包括 Zookeeper、Eureka 和 Nacos 等。

Zookeeper是Hadoop的子项目，在分布式系统中提供管理集群、服务请求、数据订阅等功能。Zookeeper在实现注册中心时会以路径的形式维护各个微服务。然而，Zookeeper保障一致性、分区容忍性而不保障可用性，因此在故障发生时可能导致系统服务中断。

Eureka 是 Spring Cloud 早期常用的注册中心组件，强调可用性和分区容忍性，在出现网络分区时仍可继续提供注册发现能力，但可能返回短时间内不够新鲜的实例信息。Nacos 是 Spring Cloud Alibaba 生态中的注册中心组件，支持服务注册发现，也可扩展用于配置管理，部署和接入方式相对简单。

结合本项目的技术栈和部署方式，系统实际使用 Nacos 作为服务注册中心。需要说明的是，当前代码主要使用 Nacos Discovery 完成服务发现，并未将 Nacos 配置中心作为论文实现重点。

### 2.1.2 微服务网关与服务调用

微服务架构中，服务间调用关系较多，权限管理也需要覆盖多个服务，因此通常需要由网关统一接收外部请求并完成路由、鉴权和限流等处理。服务内部还需要一种简洁的远程调用方式，以降低编写 HTTP 调用代码的复杂度。

Spring Cloud Gateway 是 Spring Cloud 官方推出的新一代 API 网关，基于 Spring WebFlux 和 Reactor 模型构建，能够提供统一入口、请求路由、过滤器链处理、身份认证与权限控制等能力。与早期的 Zuul 1.x 相比，Spring Cloud Gateway 在性能、可扩展性和与现代 Spring 生态集成方面更具优势，已成为当前微服务系统的主流网关方案。

OpenFeign 是声明式服务调用组件，开发者只需定义带注解的接口，即可完成对远程微服务的 HTTP 调用封装。在配合 Spring Cloud LoadBalancer 使用时，OpenFeign 可以自动完成基于服务名的实例发现与负载均衡，降低服务间调用的样板代码。

在本项目中，系统使用 Spring Cloud Gateway 作为统一入口，实现 JWT 认证过滤、角色路径权限控制以及用户身份透传；同时使用 OpenFeign 完成订单、库存、顾客、商户和物流等服务之间的同步调用，形成完整的微服务协作链路。

### 2.1.3 消息队列

在微服务架构中，服务间的通信是关键环节。除了同步的RESTful API调用外，异步消息传递机制在许多场景下更为适用，而消息队列是实现异步通信的核心组件，它主要有服务解耦、异步通信、削峰填谷等作用。

使用消息队列时，各个服务不需要直接与其他服务进行紧密的代码或接口调用，只需要关注自己与消息队列的交互，有效降低了服务间的耦合度，使得服务可以更容易的独立开发和部署。此外，消息队列可以让调用方将消息发送到队列后，无需等待被调用方的响应即可继续执行后续操作，从而提高系统的吞吐量和响应速度。在业务高峰期时，会出现大量请求同时涌入的情况，此时便可以将请求先放入消息队列中，从而起到缓冲的作用，消费者服务按照自身处理能力进行消费，避免瞬时高并发冲垮下游服务，保证系统的稳定性。

当前常用的消息队列中间件有RabbitMQ、Kafka等。RabbitMQ提供了灵活的路由和可靠的消息确认机制，适合业务逻辑复杂的场景。Kafka 则以高吞吐量和持久化日志著称，适合大数据流处理和日志收集。

在本项目中，RabbitMQ 承担跨服务异步协作任务，主要用于库存扣减、订单入调度池、路线绑定、路线状态更新、订单状态同步、Hub 到达通知和末端任务激活等场景。通过交换机、路由键、重试和死信队列机制，系统能够在不强行拉长同步调用链的情况下保持业务流程的最终一致性。

## 2.2 后端其他技术

除微服务治理组件外，后端系统还需要稳定的数据持久化、便捷的数据访问抽象与高效的缓存机制。为此，本项目采用 MySQL 作为主要关系型数据库，以 MyBatis-Plus 作为数据访问增强框架，并结合 Redis 提升热点数据读取效率。

### 2.2.1 MySQL与MyBatis-Plus

MySQL 是当前广泛使用的开源关系型数据库管理系统，具备成熟的事务支持、索引机制与较高的工程稳定性，适合支撑订单、库存、配送和调度等结构化业务数据存储需求。在本系统中，MySQL 8.0 主要用于保存用户、商品、仓库、订单、路线、批次以及全国运力规划结果等核心业务数据。

MyBatis-Plus 是构建在 MyBatis 之上的增强工具，在保留原生 SQL 控制能力的基础上，提供了通用 CRUD 接口、条件构造器、分页插件、自动填充等功能，可显著减少样板代码。在本项目中，各服务普遍采用 ServiceImpl&lt;M,T&gt; 形式构建业务服务层，并通过 LambdaQueryWrapper 实现类型安全的条件查询，从而提升开发效率与可维护性。

此外，系统在运行时使用 Spring Boot 默认集成的 HikariCP 连接池来管理数据库连接，通过限制最大连接数、配置超时时间和复用空闲连接等方式，提高并发场景下的数据访问稳定性。相较于完全响应式的数据访问方案，本项目采用阻塞式的 MyBatis-Plus + JDBC 技术路径，更符合系统当前的业务实现方式和工程复杂度要求。

### 2.2.2 Redis缓存

在高并发状态下，MySQL由于基于块设备存储数据，速度远慢于内存，因此需要基于内存的NoSQL实现缓存加速。

Redis是目前最主流的内存KV存储中间件，它支持多种数据类型的存储，同时具有极高性能的读写能力，并提供了RDB和AOF两种数据持久化技术和集群模式、哨兵模式等高可用方案。

基于Redis的特性，本项目在多个业务服务中通过Spring Cache结合Redis实现对热点数据的缓存加速。具体而言，shop-service对仓库全量列表查询使用@Cacheable(value = "warehouseAll")缓存，商品详情使用@Cacheable(value = "product")缓存；customer-service对地址列表与顾客信息使用对应@Cacheable注解；driver-service对司机进行中配送列表使用@Cacheable(value = "inProgressDeliveries")缓存，以减少司机端频繁刷新首页时对数据库的扫描压力；logistics-service对按订单 ID 查询路线结果使用@Cacheable(value = "logisticsRoute")缓存。对于新增、修改、删除等写操作，各服务均配置了对应的@CacheEvict，确保缓存与数据库状态一致。认证方面，系统采用无状态JWT，Token 由前端本地持有，服务端不存储会话信息，因此Redis不用于 Token 存储。

## 2.3 前端技术栈 

用户交互界面的质量直接影响本系统的用户体验。现代前端开发普遍是基于Webpack、Vite等打包器的Typescript项目。这些项目使用Vue或React这样的响应式框架，通过axios、ofetch等ajax请求组件实现与后端的交互。

### 2.3.1 响应式框架

当今，前端开发主要基于MVC或MVVM的范式，将页面结构与注入的数据分离。前端代码主要关心数据的schema模型、获取方式，以及页面的组织结构、组织样式等。上述开发范式主要通过Vue、React、Angular等响应式框架实现。本项目规模较小，为了开发效率，选择轻量级的Vue进行前端开发。

Vue.js是一个流行的、渐进式 JavaScript 框架，以其易学易用、性能优越和灵活性著称。本系统中所使用的Vue3版本通过优化的虚拟DOM 算法、编译时优化等手段，提供了更好的运行时性能和更小的包体积，相较于先前的Vue2性能实现了优化。Vue3还提供了Composition API，这是一种更灵活、可组合的组织组件逻辑的方式，特别适合大型复杂组件的状态管理和逻辑复用，提高了代码的可维护性和可读性。Vue3还有着更好的TypeScript 支持，对 TypeScript 的集成更加友好，有助于构建类型安全、更健壮的前端应用。

### 2.3.2 构建工具与界面组件

Vite 是新一代前端构建工具，充分利用浏览器原生 ES Module 特性以获得更快的开发服务器启动速度，并在生产环境通过高效打包提升构建性能。对于本系统这类以后台管理和多角色交互为主的应用场景，采用 Vue3 + Vite 的单页应用模式即可满足开发效率与运行性能要求，无需引入以 SSR 为主要特征的框架。

Element Plus 是基于 Vue 3 的组件库，提供表格、表单、弹窗、分页、菜单等通用后台组件，可帮助开发者快速构建风格统一的管理界面。本项目的大部分后台页面均基于 Element Plus 实现，并结合 Pinia 进行前端状态管理，结合 axios 完成与后端接口的数据交互。

在地图与轨迹展示方面，系统前端采用 Leaflet 作为基础地图渲染组件，并结合后端返回的 GeoJSON 路线数据实现运输轨迹、司机位置和全国网络拓扑的可视化展示。这种组合方式既保证了开源可控性，也满足了物流地图场景下的交互需求。

## 2.4 容器化部署技术

容器是一个隔离的运行环境，具有独立性、可移植性、快速启动和资源利用率高的特点。在微服务架构下，各个模块间彼此独立、可分布式扩展。容器化部署技术即将这些模块各自打包为容器镜像，再利用统一的编排配置文件，根据客户端调用负载强度按需启用和扩容。

微服务架构只有与标准化部署方式结合，才能更好地体现独立启动、独立扩展和环境一致性的优势。容器化部署可以减少运行环境差异，也便于后续向更复杂的编排平台迁移。

### 2.4.1 Docker容器

Docker是目前最主流的开源容器化平台\[30\]。它利用Linux内核的特性，允许开发者将应用程序及其所有依赖打包到一个轻量级、可移植的独立容器镜像中。容器在运行时相互隔离，共享宿主机的操作系统内核。

前文中提到的各种中间件依赖可以直接使用标准化的官方容器，从而无需配置运行环境，且可便捷指定端口号和配置参数。本项目的前端、后端实现也会提供对应的Dockerfile，从而在任何支持Docker的环境中可以标准化部署，不存在兼容性问题。

### 2.4.2 Docker Compose

Docker Compose是一个用于定义和管理多容器Docker应用的工具，开发者可以通过一个YAML文件来配置应用所需的所有容器、网络、数据卷等，此后仅需一条命令即可启动、停止和重建所有服务。此外，Docker Compose还可以为多个关联容器创建一个隔离的局域网，并可指定容器间的依赖关系。

本系统中的前端、后端、中间件之间依赖关系复杂，端口号配置繁多。在使用Docker Compose后，仅需提供一个全局配置文件即可让用户快速部署。

### 2.4.3 容器编排扩展讨论

对于毕业设计阶段的部署需求，Docker Compose 已能够满足数据库、中间件、微服务与前端应用的一键编排与统一启动要求，因此本文采用单机容器化部署方案。需要指出的是，在面向生产环境的大规模部署场景中，仍可进一步引入 Kubernetes 等成熟编排平台，以获得自动扩缩容、故障自愈和更强的运维能力，但这并非本文系统实现的重点。

## 2.5 智能导航与调度算法技术

除常规信息管理能力外，系统还需要解决“从哪里发货、走哪条路线、如何分批配送、跨城干线如何分配运力”等问题。因此，本文将智能导航与调度算法单独作为一类核心技术进行综述。

### 2.5.1 路网引擎 GraphHopper

GraphHopper 是基于 Java 的路径规划引擎，能够读取 OpenStreetMap 数据并构建道路图结构。相比仅使用直线距离估算，基于路网的规划更接近真实车辆行驶场景。在本项目中，`logistics-service` 通过 `GraphHopperConfig` 加载 `shanghai-260310.osm.pbf` 和 `graph-cache`，并以 `car` 作为车辆配置建立可查询的道路图。系统首次启动时会导入 OSM 数据，后续启动可复用图缓存，以减少加载时间。

### 2.5.2 A\* 路径搜索与多策略路线规划

A\* 算法通过 \(f(n)=g(n)+h(n)\) 对搜索方向进行引导，其中 \(g(n)\) 表示起点到当前节点的实际路径代价，\(h(n)\) 表示当前节点到终点的启发式估计。本项目的 `AStarRouteStrategy` 在 GraphHopper 的底层道路图上运行 A\* 搜索，通过 Haversine 球面距离作为启发函数，使用道路边距离累积实际代价，并将最终节点序列转换为 GeoJSON LineString 返回前端。系统还提供 Weighted A\* 能力，通过不同 epsilon 值生成候选路线，为后续智能裁决提供输入。

### 2.5.3 大语言模型辅助决策

大语言模型适合对结构化算法结果进行解释和辅助判断，但不适合直接替代确定性路径算法生成道路坐标。因此本项目采用“传统算法给候选结果，模型做辅助判断”的方式：`LLM_JUDGE` 策略先生成多条 A\* 候选路线，再结合历史速度、延误和慢速热点信息调用 DeepSeek API 选择路线并修正预计耗时；`LLM_WAYPOINT` 策略则由模型判断是否需要战略途经点，再交由 A\* 分段规划。对于模型调用失败或返回异常的情况，系统会回退到标准 A\* 或规则策略，避免导航接口不可用。

### 2.5.4 K-Means 聚类与末端配送预览

末端配送通常面对多个地理位置相近的订单。K-Means 可以将订单按收货坐标聚类，把空间上接近的订单划分为同一批次候选。本项目在 `KMeansClusterServiceImpl` 中实现了基于 Haversine 距离的聚类逻辑，支持管理员指定聚类数量，也支持在未指定时按订单数量自动取值。聚类结果随后会进入调度预览流程，由规则或 LLM 给出是否使用中转站、推荐 Hub、紧急程度和批次说明等建议。

### 2.5.5 多商品最小费用流与全国运力规划

跨城物流调度需要在多条干线和多个 Hub 之间分配运力。最小费用流模型能够在容量约束下寻找低成本运输方案。本项目在传统最小费用流思想基础上，进一步采用多商品建模方式，将每个起始 Hub 与目标 Hub 的 OD 对作为一种独立需求，使用 Dijkstra-SSP 在共享残差容量图上逐商品寻路。这样可以避免简单汇总供需时同一 Hub 供给与需求相互抵消的问题，更符合跨城订单的实际流向。规划结果会写入 `flow_plan`、`flow_plan_item` 和 `inter_city_batch` 等表，并在前端展示各线路流量、容量和成本。

## 2.6 本章小结

本章梳理了系统实现所依赖的关键技术。微服务治理、网关、OpenFeign 和 RabbitMQ 为后端服务协作提供基础；MySQL、MyBatis-Plus 与 Redis 支撑数据持久化和热点查询缓存；Vue3、Vite、Element Plus、Pinia 与 Leaflet 用于前端页面和地图展示；Docker Compose 用于统一部署；GraphHopper、A\*、K-Means、大语言模型和多商品最小费用流共同构成系统的智能导航与调度能力。这些技术为后续系统设计和实现提供了工程基础。

# **3 系统设计**

本章从需求、架构、后端服务、智能导航与调度、前端页面五个方面说明系统设计。与早期仅围绕前后端功能划分的写法不同，本文将智能导航与调度单独列为系统核心组成部分，因为当前项目中路径规划、末端聚类和全国运力规划已经不只是后端接口的附属功能，而是直接影响订单履约流程的关键能力。

## 3.1 需求分析

### 3.1.1 总体分析

物流系统的核心目标是让订单从“需求产生”进入“可执行、可追踪、可调度”的履约过程。本系统面向四类主要角色：顾客、商户、司机和管理员。顾客侧主要完成商品浏览、电商下单、个人寄件、地址维护和物流查询；商户侧主要完成商品管理、仓库管理、库存维护和订单发货；司机侧主要完成车辆管理、配送接单、运输状态推进、Hub 到达确认、末端逐站送达和 GPS 上报；管理员侧主要完成用户审核、仓储作业管理、城市末端调度、全国干线调度和运力规划查看。

从业务流程看，系统既支持普通电商订单，也支持个人寄件。电商订单由顾客下单后进入待发货状态，商户选择仓库发货，订单进入调度池，再由物流服务根据是否跨城、所在 Hub 和调度方案生成后续路线与批次。个人寄件则以发件地址和收件地址为输入，由系统分配起始与目标 Hub，并进入相同的调度体系。为了降低实现复杂度，当前项目不接入真实支付系统，下单后订单直接视为已支付；这一处理方式与源码中的 `paymentStatus=1` 和 `paymentTime` 写入逻辑一致。

### 3.1.2 顾客功能需求

顾客需要完成注册登录、个人资料维护、收货地址管理、商品浏览、订单创建、个人寄件、订单列表查询、订单详情查看、物流轨迹查看和订单签收等操作。地址保存时，系统会尽量通过高德地理编码补全经纬度，为后续路径规划提供基础数据；若地理编码失败，地址保存流程不会中断。

### 3.1.3 商户功能需求

商户需要维护店铺信息、商品信息、商品上下架状态、仓库信息、商品库存和订单发货。当前项目还实现了共享仓绑定、库位管理、入库单、出库单、盘点单和调拨单等仓储扩展能力，因此商户侧并不只是简单的商品管理页面，而承担了订单履约前的库存与仓储基础数据维护职责。

### 3.1.4 司机功能需求

司机需要维护司机资料和车辆信息，查看待接单任务，接受普通配送、干线段或末端配送任务，推进配送状态，并在运输过程中上报位置。对于干线任务，司机到达 Hub 后需要触发 Hub 到达事件；对于末端多停靠点路线，司机需要逐站完成配送确认，使系统能够更新批次项状态并判断整批任务是否完成。

### 3.1.5 管理员功能需求

管理员需要进行用户审核、订单查看、商品与仓储数据查看、Hub 作业管理、末端调度、全国干线批次控制和运力规划查看。管理员侧是系统智能调度能力的主要操作入口，需要能够查看调度池订单、触发 K-Means 调度预览、执行批次创建、查看全国网络拓扑，并手动触发或查看 MCMF 运力规划结果。

### 3.1.6 非功能需求

系统需要满足以下非功能需求：一是可维护性，服务之间按业务边界拆分，避免单个模块过度膨胀；二是可扩展性，新增物流算法、仓储流程或调度策略时能够在独立服务中迭代；三是安全性，通过 JWT 和网关路径权限控制保护接口；四是可用性，前端页面应按角色清晰组织；五是性能与稳定性，通过 Redis 缓存和 RabbitMQ 异步消息降低热点查询和跨服务协作压力。

## 3.2 架构设计

系统采用基于微服务的分层架构，整体可分为接入层、服务层、通信层、数据层、智能导航与调度层以及部署层。

接入层由 Vue3 前端、Nginx 和 Spring Cloud Gateway 构成。前端通过 `/api` 访问网关，网关根据路径将请求转发到对应服务，并在全局过滤器中完成 JWT 校验、角色权限判断和用户身份透传。

服务层由 `auth-service`、`user-service`、`customer-service`、`shop-service`、`order-service`、`driver-service`、`logistics-service` 和 `gateway-service` 构成，`common` 模块提供统一响应、异常、JWT、权限枚举和地理编码等公共能力。各服务统一注册到 Nacos 中，网关使用 `lb://服务名` 完成路由。

通信层同时包含同步调用和异步消息。同步调用主要通过 OpenFeign 完成，例如订单服务校验地址、查询商品和库存、调用物流服务分配 Hub。异步消息通过 RabbitMQ 完成，例如订单创建后异步扣减库存，商户发货后订单进入调度池，司机接单后绑定路线，司机状态变化后同步路线与订单状态，干线到达后激活末端路线。

数据层以 MySQL 为核心数据库，保存用户、地址、商品、仓库、库存、订单、配送、路线、轨迹、批次、Hub 网络和运力规划结果等数据；Redis 作为 Spring Cache 后端，用于缓存顾客信息、地址列表、商品、仓库、司机进行中任务和物流路线详情等热点数据。

智能导航与调度层集中在 `logistics-service` 中，包括 GraphHopper 路网加载、A\* 路径规划、LLM 路线辅助决策、K-Means 末端聚类、Hub 选择、全国 Hub 网络、MCMF 运力规划和批次生成等功能。

部署层使用 Docker Compose 编排 MySQL、Redis、RabbitMQ、Nacos、后端服务、网关和前端容器。物流服务额外挂载 `shanghai-260310.osm.pbf` 和 `graph-cache`，用于真实路网规划。

## 3.3 后端服务设计

后端服务按照职责边界划分如下：

| 服务或模块 | 端口 | 主要职责 |
|------------|------|----------|
| `common` | - | 统一响应、异常处理、JWT、权限枚举、地理编码公共组件 |
| `gateway-service` | 8090 | 统一入口、路由转发、JWT 鉴权、角色路径权限控制 |
| `user-service` | 8081 | 用户分页查询、待审核商户与司机查询、审核通过或拒绝 |
| `auth-service` | 8082 | 注册、登录、退出、BCrypt 密码校验、JWT 签发 |
| `customer-service` | 8083 | 顾客信息、收货地址、默认地址、高德地理编码补全坐标 |
| `shop-service` | 8084 | 店铺、商品、仓库、库存、共享仓、库位、入库、出库、盘点、调拨 |
| `order-service` | 8085 | 电商订单、个人寄件、订单状态、商户发货、订单签收、库存扣减消息 |
| `driver-service` | 8086 | 司机资料、车辆、配送单、接单、状态推进、Hub 到达、末端逐站送达 |
| `logistics-service` | 8087 | 路线、轨迹、Hub、批次、调度池、全国网络、路径规划、运力规划 |

认证与权限设计以 `user.permission` 为角色来源，角色编号为 1=管理员、2=顾客、3=商户、4=司机。`auth-service` 在注册时写入用户基础记录；商户和司机注册后状态为待审核，需要管理员在 `user-service` 中审核通过后才能登录。登录成功后，系统签发包含 `userId`、`username` 和 `roleCode` 的 JWT。网关白名单仅开放登录、注册和 Swagger 相关路径，其他请求必须携带有效 Token。

顾客服务主要围绕 `customer_info` 与 `customer_address` 两张表设计。地址表保存省、市、区、详细地址、收件人、电话、是否默认、经纬度等信息。地址新增或更新时，服务会调用公共的高德地理编码组件补全坐标，并通过 Redis 缓存地址列表和地址详情。

商店与库存服务围绕 `shop_info`、`product_info`、`warehouse`、`warehouse_product`、`warehouse_shop`、`warehouse_location` 等表设计。商品与仓库之间通过 `warehouse_product` 维护库存数量，仓库与商户之间通过 `warehouse_shop` 支持共享仓关系。入库、出库、盘点和调拨分别通过对应单据表和明细表记录仓储作业过程。

订单服务围绕 `order_info` 与 `order_item` 设计。`order_info` 中保存订单号、顾客、商户、地址、仓库、订单状态、支付状态、起始 Hub、目标 Hub、跨城标记、干线批次和运力规划等字段。系统当前省略真实支付接入，下单即写入已支付状态。发货后订单会进入调度池，等待后续城市末端或跨城干线调度。

司机服务围绕 `driver_info`、`vehicle_info` 和 `order_delivery` 设计。配送单状态用于约束司机操作流程，支持待接单、已接单、运输中、已送达、已取消等状态变化。对于批次路线，配送单还会保存 `routeId`、`batchId`、`hubId` 和 `routeType` 等字段，以区分普通路线、干线路线和末端路线。

物流服务的数据模型最为复杂，主要包括 `logistics_route`、`logistics_node`、`logistics_track`、`logistics_hub`、`logistics_batch`、`logistics_batch_item`、`dispatch_pool`、`national_hub`、`hub_link`、`flow_plan`、`flow_plan_item`、`inter_city_batch` 以及 Hub 入库、出库、分拣记录等表。该服务既承载路线和轨迹查询，也承载调度池、城市配送批次、全国 Hub 网络和运力规划结果。

## 3.4 智能导航与调度设计

智能导航与调度模块在系统中相对独立，其输入来自订单、仓库、地址、Hub 和历史轨迹数据，输出则反向影响路线、批次、配送任务和前端可视化。

### 3.4.1 路径规划设计

路径规划以 `RoutePlanningService` 为统一入口，实际策略由 `RoutingStrategyConfig` 根据 `routing.strategy` 配置选择。系统目前支持三类策略：`A_STAR`、`LLM_JUDGE` 和 `LLM_WAYPOINT`。`A_STAR` 是默认策略，直接在 GraphHopper 底层路网图上搜索；`LLM_JUDGE` 先通过不同 epsilon 生成多条候选路线，再由 DeepSeek 结合历史交通统计选择路线；`LLM_WAYPOINT` 先由大语言模型判断是否需要途经点，再调用 A\* 分段规划。三种策略统一返回距离、预计耗时和 GeoJSON 路线数据。

### 3.4.2 路线与轨迹设计

`logistics_route` 保存路线主信息，包括路线编号、订单、配送单、司机、仓库、批次、路线段类型、起终点地址、经纬度、当前位置、状态、预计到达时间和规划路线 GeoJSON。`logistics_track` 保存司机上报的位置点。前端查询物流详情时，后端会返回路线节点、最近轨迹和状态描述，供 Leaflet 地图与物流时间线展示。

### 3.4.3 城市末端调度设计

末端调度以 `dispatch_pool` 为输入。订单进入调度池后，管理员可按目标 Hub 查看待调度订单，并调用预览接口。系统首先对订单收货坐标执行 K-Means 聚类，得到若干批次候选；随后结合仓库或 Hub 坐标、订单数量和距离信息，由规则或大语言模型判断是否使用中转站以及推荐哪个 Hub。管理员确认后，系统创建 `logistics_batch`、`logistics_batch_item` 和对应末端路线，并将调度池记录标记为已调度。

### 3.4.4 全国干线运力规划设计

全国干线网络由 `national_hub` 和 `hub_link` 描述。`national_hub` 表示全国枢纽、省级中心或城市配送中心，`hub_link` 表示 Hub 之间的运输连接、运输方式、日容量、单位成本和距离。系统根据指定日期的跨城订单统计 OD 需求，使用多商品最小费用流算法为各 OD 对寻找低成本可行路径，并将结果写入 `flow_plan` 与 `flow_plan_item`。同时，系统会根据规划路径生成或更新 `inter_city_batch`，用于后续干线发车、到达和下游批次激活。

### 3.4.5 Hub-and-Spoke 流程设计

系统采用 Hub-and-Spoke 思路连接跨城干线和城市末端。订单发货时先确定起始 Hub 与目标 Hub；跨城订单进入揽收待处理队列，并在全国运力规划后绑定干线批次；干线批次发车、到达后，系统将订单写入目标城市调度池；城市末端调度再生成末端配送批次。通过该设计，系统把“仓库—起始 Hub—干线 Hub 网络—目标 Hub—收货地址”串联为完整履约链路。

## 3.5 前端设计

前端采用 Vue Router 按角色划分页面。管理员入口为 `/admin/home`，包含仪表盘、订单、用户审核、商品、仓储管理、Hub 作业、城市末端调度和全国业务页面；顾客入口为 `/sender/home`，包含商品、订单、个人信息、个人寄件和商户页面；商户入口为 `/merchant/home`，包含商品、订单、资料、库存和仓库页面；司机入口为 `/driver/home`，包含配送任务、导航、车辆、资料和 GPS 测试页面。

前端通过 Pinia 保存登录态和用户角色，并在全局路由守卫中根据 Token 与角色限制页面访问。所有请求通过统一 axios 实例发出，请求拦截器自动携带 `Authorization: Bearer <token>`，响应拦截器在未授权时清理登录态并跳转登录页。地图展示主要由 `RouteMap.vue` 和 `NationalNetworkMap.vue` 承担，前者展示路线、起终点和轨迹点，后者展示全国 Hub 网络拓扑。

## 3.6 本章小结

本章基于当前项目源码重新梳理了系统设计。系统总体上由微服务后端、前端多角色界面、数据与消息中间件、智能导航与调度模块、容器化部署环境共同组成。与普通物流管理系统相比，本系统的特点在于将路线规划、末端聚类调度和全国运力规划纳入核心设计，使订单履约不再只是状态流转，而能够结合路网、坐标、Hub 网络和算法结果进行辅助决策。

# **4 系统实现**

本章在第三章设计基础上，结合当前项目代码说明系统实现。为避免将智能导航能力淹没在后端业务描述中，本章按照“运行环境—后端业务服务—智能导航与调度—前端实现—容器化部署”的顺序展开。

## 4.1 微服务运行环境实现

后端项目采用 Maven 多模块结构，父工程 `pom.xml` 统一管理 Java 17、Spring Boot 3.1.5、Spring Cloud 2022.0.4、Spring Cloud Alibaba 2022.0.0.0 和 MyBatis-Plus 3.5.5 等版本。各服务均通过 `@EnableDiscoveryClient` 注册到 Nacos，并根据需要启用 OpenFeign、RabbitMQ、Redis 和 Swagger。

服务端口按源码配置如下：`user-service` 为 8081，`auth-service` 为 8082，`customer-service` 为 8083，`shop-service` 为 8084，`order-service` 为 8085，`driver-service` 为 8086，`logistics-service` 为 8087，`gateway-service` 为 8090。前端开发环境由 Vite 运行在 5173 端口，并将 `/api` 代理到网关；容器化部署时，前端由 Nginx 对外提供 80 端口服务。

网关服务使用 Spring Cloud Gateway 与 WebFlux。`application.yml` 中配置了基于服务名的路由规则，例如 `/api/auth/**` 转发到认证服务，`/api/orders/**` 转发到订单服务，`/api/logistics/**` 转发到物流服务。网关过滤器从 `Authorization` 请求头提取 Bearer Token，验证通过后向下游透传 `userId`、`username` 和 `roleCode`。

## 4.2 后端业务服务实现

### 4.2.1 认证与权限实现

认证服务负责注册、登录和退出。注册时，系统先检查用户名唯一性，再使用 BCrypt 对密码进行哈希处理并写入 `user` 表。顾客注册后可直接使用；商户注册会写入 `shop_info` 并将状态置为待审核；司机注册会写入 `driver_info` 并将状态置为待审核。管理员可在用户服务中查看待审核列表，并执行通过或拒绝操作。

登录时，系统通过用户名读取用户记录，使用 `passwordEncoder.matches()` 校验密码，再检查商户或司机是否已通过审核。认证成功后，`JwtUtil` 生成包含 `userId`、`username` 和 `roleCode` 的 JWT，默认有效期为 7200 秒。退出接口不维护服务端会话或 Token 黑名单，由前端清除本地 Token，这与系统无状态认证设计一致。

网关的权限控制采用“白名单 + Token 校验 + 路径角色判断”的方式。登录、注册和 Swagger 路径直接放行；顾客、商户、司机和管理员分别访问对应前缀接口；管理员拥有全局访问权限。该实现减少了各业务服务重复解析 JWT 的代码，也保证了入口权限规则的一致性。

### 4.2.2 订单服务实现

订单服务是业务编排的核心。创建电商订单时，系统首先通过 OpenFeign 调用顾客服务校验收货地址，并确认地址归属；随后调用商店与库存服务查询商品信息、校验商品是否上架，并检查各仓库库存。库存充足后，订单服务生成订单号，写入 `order_info` 和 `order_item`。当前项目不接入真实支付系统，因此订单创建后直接设置为已支付、待发货。

库存扣减采用异步最终一致性。订单落库成功后，订单服务向 RabbitMQ 发送 `stock.deduct` 消息，商店服务消费该消息并执行 `UPDATE ... WHERE stock >= quantity` 的原子扣减。若扣减失败，消息进入死信队列，便于后续人工处理或补偿。

商户发货时，订单服务会根据仓库、地址和坐标信息调用物流服务分配起始 Hub 与目标 Hub，并将订单写入调度池。对于个人寄件，系统同样根据发件地址和收件地址确定 Hub，并进入调度流程。这样，电商订单和个人寄件最终都能汇入统一的物流调度体系。

### 4.2.3 商店、库存与仓储实现

商店与库存服务负责店铺、商品、仓库、库存和仓储作业。商品管理围绕 `product_info` 实现，支持列表、搜索、详情、新增、修改、删除和状态更新；仓库管理围绕 `warehouse` 实现，支持新增、修改、删除、容量调整和地理编码。仓库新增或更新时，系统使用公共 `AmapGeocodingService` 调用高德地理编码接口，尽量补全经纬度；如果接口不可用，则保留原坐标或允许保存空坐标，不阻断主流程。

库存由 `warehouse_product` 表维护。订单服务通过商品库存查询接口选择可发货仓库，库存扣减则由 RabbitMQ 消费者异步执行。服务还实现了共享仓绑定、库位管理、入库单、出库单、盘点单和调拨单等能力，使仓储管理不再只是简单的库存数字维护，而可以支持更完整的仓库作业演示。

在缓存方面，商品详情、商城商品列表、店铺信息、仓库列表和仓库详情等高频查询均使用 Spring Cache 注解，并由 Redis 提供缓存存储。写操作会通过 `@CacheEvict` 清理相关缓存，避免页面展示旧数据。

### 4.2.4 司机配送服务实现

司机服务围绕司机资料、车辆和配送单实现。司机可以维护个人资料和车辆信息，查看待接单任务和进行中任务。系统针对进行中配送列表使用 Redis 缓存，以减少司机端频繁刷新造成的数据库压力。

配送状态推进由 `DeliveryServiceImpl` 负责。司机接单后，服务会更新本地配送单状态，并向物流服务发送路线绑定消息；进入运输中或已送达时，会通过 RabbitMQ 同步路线状态和订单状态。对于干线配送，司机到达 Hub 后调用到达接口，司机服务发送 `hub.arrival` 消息，物流服务收到后激活下游末端路线，并发送 `last.mile.activate` 消息创建末端配送单。对于多停靠点末端路线，司机逐站完成配送后，系统更新 `logistics_batch_item` 状态，并在全部站点完成后推进批次状态。

## 4.3 智能导航与调度实现

### 4.3.1 GraphHopper 路网加载

物流服务通过 `GraphHopperConfig` 初始化 GraphHopper。配置文件中使用 `graphhopper.osm-file` 指向 OSM PBF 文件，使用 `graphhopper.graph-cache` 指向图缓存目录，使用 `graphhopper.vehicle` 指定车辆类型。代码中构建 `CustomModel`，限制不可通行道路并使用 `car_average_speed` 等编码值，然后调用 `importOrLoad()` 导入或加载图数据。Docker Compose 部署时，`shanghai-260310.osm.pbf` 和 `graph-cache` 会被挂载到物流服务容器中。

### 4.3.2 A\* 路径规划实现

`AStarRouteStrategy` 是系统默认路线策略。服务首先通过 GraphHopper 的 `LocationIndex` 将起点和终点经纬度吸附到最近路网节点，然后在 `BaseGraph` 上运行 A\* 搜索。开放集合使用优先队列维护，`gScore` 保存起点到节点的已知最短距离，`cameFrom` 用于路径回溯，`closedSet` 防止重复扩展。启发函数采用 Haversine 球面距离，边代价使用路网边距离。搜索成功后，系统将节点序列转换为 GeoJSON LineString，并按平均 40km/h 估算行驶时间。

为了支持候选路线生成，系统实现了 Weighted A\*。当 epsilon 为 1.0 时执行标准搜索；当 epsilon 为 1.8 或 3.5 时，启发项权重增大，搜索会更倾向于朝终点推进，从而可能得到不同候选路线。这些候选路线会被用于 LLM 辅助裁决。

### 4.3.3 LLM 路线辅助策略实现

系统提供两种 LLM 路线策略。`LlmJudgeRouteStrategy` 会生成多条 Weighted A\* 候选路线，并按距离差异去重；随后从历史轨迹和路线表中读取当前时段平均速度、全天平均速度、延误、慢速热点和异常路线等信息，组织为提示词调用 DeepSeek API。模型返回 JSON 后，系统读取其选择的候选路线和修正后的预计耗时。如果模型调用失败或返回结果不可解析，则直接回退到 epsilon=1.0 的 A\* 路线。

`LlmWaypointRouteStrategy` 则让模型根据历史慢速热点判断是否需要绕行路点。模型输出的路点不会直接作为最终路线，而会先经过上海区域范围校验，再由 A\* 分段规划并拼接坐标。这样既利用了大语言模型的经验判断能力，又避免模型直接生成不可行道路的问题。

### 4.3.4 城市末端调度实现

城市末端调度由 `DispatchController`、`KMeansClusterServiceImpl`、`GlobalDispatchAdviceServiceImpl` 和 `LogisticsBatchServiceImpl` 协作完成。管理员调用预览接口时，系统从 `dispatch_pool` 中读取待调度订单，提取收货坐标后执行 K-Means 聚类。若请求中未指定 k 值，系统按订单数量平方根自动取值。聚类完成后，系统结合最近 Hub、仓库到簇中心距离、订单数量等信息，调用 LLM 生成批次建议；如果模型不可用，则使用规则降级。

管理员确认调度方案后，系统创建配送批次和批次项，并为每个批次生成路线。路线可以是直接送达，也可以是 Hub-and-Spoke 模式下的干线段和末端段。末端路线支持多停靠点，停靠点信息保存在 `logistics_batch_item` 和路线 `waypoints` 字段中，前端和司机端可以据此展示配送顺序和逐站完成状态。

### 4.3.5 全国运力规划实现

全国运力规划由 `FlowPlanServiceImpl`、`McmfServiceImpl`、`LlmEdgeCostCalibrationServiceImpl` 和 `LlmFlowPlanAdviceServiceImpl` 实现。管理员可通过 `/api/logistics/dispatch/national/plan` 手动触发规划，也可以在配置开启时使用定时触发。

规划开始后，系统先从订单表统计指定日期的跨城 OD 需求，再读取启用状态的 `national_hub` 和 `hub_link`。在运行算法前，系统可调用 LLM 根据前一日流量、容量负载和 Open-Meteo 天气摘要对边费用进行校准；若模型调用失败，则使用基础费用。随后，`McmfServiceImpl` 使用多商品 Dijkstra-SSP 方法逐 OD 对分配流量，共享各干线边剩余容量。结果写入 `flow_plan` 和 `flow_plan_item`，并根据路径生成 `inter_city_batch`。有实际订单绑定的首跳批次被激活为 `CREATED`，后续批次保留为 `CHAINED`，等待上游到达后激活。

### 4.3.6 Hub-and-Spoke 履约链路实现

系统最终形成了较完整的 Hub-and-Spoke 履约闭环：商户发货或个人寄件后，订单进入调度池；跨城订单进入全国干线规划并绑定干线批次；干线批次发车、到达后，系统将订单转入目标城市末端调度池；管理员再执行末端聚类和批次创建；司机接单后按路线执行配送，并通过 GPS 上报和状态按钮推进流程。该链路将订单、仓库、司机、Hub、路线和算法结果连接起来，体现了系统的核心智能化特征。

## 4.4 前端实现

前端采用 Vue3 + Vite + TypeScript 构建，使用 Element Plus 提供表格、表单、弹窗、标签页等后台组件，使用 Pinia 管理用户登录态，使用 axios 封装统一请求。请求实例以 `/api` 为 baseURL，并在请求拦截器中自动添加 Bearer Token；响应拦截器在 401 情况下清除用户状态并跳转登录页。

路由设计按角色划分。管理员页面包括仪表盘、用户审核、订单管理、仓储管理、Hub 作业台、城市末端调度、全国干线调度、全国网络和运力规划；顾客页面包括商品浏览、商户页面、订单、个人信息、个人寄件和目录寄件；商户页面包括商品、订单、库存、仓库和资料；司机页面包括配送任务、导航、车辆、资料和 GPS 测试。

地图可视化主要由 Leaflet 完成。`RouteMap.vue` 展示后端返回的 GeoJSON 路线、起终点标记和最近轨迹点；`LogisticsJourney.vue` 以时间线和路线段方式展示订单全程物流；`NationalNetworkMap.vue` 展示全国 Hub 网络拓扑和干线连接。管理员的 `NationalBusiness.vue` 将干线调度、全国网络和运力规划组织为页签，`LastMileBusiness.vue` 将城市末端调度、配送批次和中转站管理组织为页签，使复杂调度能力在界面上保持清晰。

## 4.5 容器化部署实现

项目提供统一的 `docker-compose.yml`。基础设施层包括 MySQL 8.0、Redis 7.2、RabbitMQ 3.12 management 和 Nacos 2.3.0；后端服务层包括认证、用户、顾客、商店、订单、司机、物流和网关服务；前端层使用 Nginx 承载 Vite 构建后的静态资源。

各微服务目录均提供 Dockerfile，后端镜像基于 Maven 构建 JAR，再使用 JRE 镜像运行。物流服务因需要真实路网数据，额外挂载 OSM 文件和 GraphHopper 缓存目录。前端 Dockerfile 使用 Node 构建静态文件，再复制到 Nginx 容器中，并通过 Nginx 配置将 `/api/` 反向代理到 `gateway-service:8090`，同时使用 `try_files` 支持 Vue Router 的 history 模式。

## 4.6 本章小结

本章结合当前源码说明了系统实现。后端以 Spring Cloud 微服务为基础，通过网关、JWT、Nacos、OpenFeign、RabbitMQ、Redis 和 MyBatis-Plus 实现多服务协作；智能导航模块通过 GraphHopper、A\*、LLM、K-Means 和多商品最小费用流实现路线规划、末端调度和全国运力规划；前端通过 Vue3 和 Leaflet 展示多角色业务页面和地图结果；Docker Compose 则将数据库、中间件、后端服务和前端统一编排。整体实现与第三章设计相对应，能够支撑较完整的智能物流原型系统运行。

# **5 系统展示**

## 5.1 并发性能测试

为了验证系统运行效果，本章从接口性能和多角色功能展示两个角度进行说明。性能测试不单纯追求某个接口的极限指标，而是选取与当前代码实现关系较紧密的业务接口，观察缓存、异步消息和路径规划对系统响应的影响。

测试接口主要包括四类：第一，使用 `GET /api/orders/admin/all` 或 `GET /api/orders/my` 观察订单查询在分页和角色过滤场景下的响应情况；第二，使用 `GET /api/warehouses` 对比仓库列表在启用 Redis 缓存前后的访问效果；第三，使用 `POST /api/logistics/routes` 验证 A\_STAR 策略下的路线创建与规划耗时；第四，使用 `POST /api/orders` 进行并发下单测试，并结合 RabbitMQ 中库存扣减消息的消费情况观察异步链路是否稳定。上述接口均来自当前项目源码，能够覆盖订单、仓库、路线和消息队列等核心路径。

【图 5-1 仓库列表接口缓存前后响应时间对比图】

图 5-1 展示启用缓存后 `GET /api/warehouses` 的平均响应时间变化，用于说明 Redis 对热点仓库查询的优化作用。

【图 5-2 路径规划接口压力测试统计图】

图 5-2 展示 `POST /api/logistics/routes` 在 A\_STAR 策略下的响应时间分布，用于说明系统在真实路网规划场景下的可用性。

## 5.2 个人用户功能展示

顾客侧页面主要覆盖注册登录、商品浏览、商户页面、地址维护、电商下单、个人寄件、订单查询、物流轨迹查看和订单签收等功能。当前前端并未以独立购物车作为核心流程，而是通过商品浏览、商品详情和下单确认完成订单创建，因此论文展示应以“浏览—确认—下单—查看物流”为主线。

在典型流程中，顾客首先通过统一登录页进入系统，随后在商品页面浏览商品并创建订单。订单创建时系统完成地址归属校验、商品状态校验和库存预检，并将订单置为已支付、待发货状态。商户发货和调度执行后，顾客可以在订单页面查看订单状态、物流时间线和地图轨迹。个人寄件页面则允许顾客选择发件地址、收件地址和货物信息，创建不依赖商品购买的寄件订单。

【图 5-3 用户注册与登录页面】

图 5-3 展示用户选择角色、填写注册信息以及登录系统的页面，用于说明系统多角色统一入口的设计方式。

【图 5-4 商品浏览、商品详情与下单确认页面】

图 5-4 展示商品列表、商户页面、商品详情及下单确认信息，以说明顾客从浏览商品到创建订单的业务流程。

【图 5-5 订单详情、个人寄件与物流轨迹页面】

图 5-5 展示订单状态标签、物流时间线、个人寄件表单和地图轨迹组件，用于体现系统对顾客侧物流使用场景的支持。

## 5.3 商户用户功能展示

商户侧承担商品维护、仓库维护、库存管理和订单发货等职责，是电商交易进入物流履约之前的重要环节。系统通过商品列表、仓库列表、库存表格、发货弹窗和仓储作业页面，支持商户完成商品上下架、库存配置、共享仓绑定和订单发货等操作。

在典型流程中，商户先维护商品和仓库信息，并为商品配置可用库存。顾客下单后，商户在待发货订单中选择发货仓库并执行发货。此时订单服务会结合仓库和收货地址信息调用物流服务分配 Hub，并把订单写入调度池；后续是否进入城市末端调度或全国干线调度，由物流服务和管理员调度页面继续处理。因此，商户侧的关键作用是完成商品、库存和发货节点，而不是直接创建全部配送任务。

【图 5-6 商户商品管理页面】

图 5-6 展示商品列表、新增或编辑商品弹窗，用于说明商户如何维护商品信息及上下架状态。

【图 5-7 仓库、库位与库存管理页面】

图 5-7 展示仓库列表、仓库地址配置、库位信息以及商品-仓库库存表格，以体现系统对多仓库存管理的支持。

【图 5-8 待发货订单管理页面】

图 5-8 展示商户查看订单、选择发货仓库并执行发货的操作入口，用于说明商户端在履约流程中的关键作用。

## 5.4 司机用户功能展示

司机侧用于接收配送任务、执行运输、上报状态和完成到站或送达确认。当前项目中的司机任务既包括普通配送，也包括干线段和末端多停靠点路线，因此展示时应分别体现待接单大厅、任务详情、地图导航、GPS 上报、Hub 到达确认和逐站送达等功能。

司机接单后，系统会将司机和配送单绑定到物流路线，并通过消息队列同步路线状态。若任务是干线运输，司机到达 Hub 后触发 `hub.arrival` 消息，物流服务据此激活下游末端路线；若任务是末端多停靠点配送，司机需要按停靠点逐单完成，系统根据批次项状态判断是否完成整条路线。

【图 5-9 司机待接单大厅】

图 5-9 展示普通配送单、干线任务和末端任务的待接单列表，用于说明司机如何认领系统生成的配送任务。

【图 5-10 导航与路线地图页面】

图 5-10 展示 Leaflet 地图、规划路线折线、实时轨迹点与起终点标记，用于体现司机端地图导航和 GPS 上报能力。

【图 5-11 Hub 到达确认与多停靠点送达页面】

图 5-11 展示司机抵达 Hub 后的到达确认按钮以及末端多停靠点逐站完成界面，用于说明配送状态推进机制。

## 5.5 管理员功能展示

管理员侧集中体现系统的全局管理、仓储作业、智能调度和全国网络规划能力。该角色不仅负责商户和司机审核，也负责查看订单、维护调度基础数据、管理 Hub 作业流转、执行城市末端调度、控制干线批次和查看运力规划结果。

管理员的典型操作路径包括：在仪表盘查看系统概况，在用户审核页处理待审核商户和司机；在仓储管理和 Hub 作业台中查看入库、分拣、出库和发车记录；在城市末端调度页面查看调度池订单，触发 K-Means 调度预览并执行批次创建；在全国业务页面查看全国 Hub 网络拓扑、干线批次状态和 MCMF 运力规划结果。通过这些页面，系统从局部配送到全国干线形成了层次化调度展示。

【图 5-12 管理员仪表盘与用户审核页面】

图 5-12 展示订单统计卡片、待审核商户与司机列表等基础管理界面，用于说明管理员对系统全局状态的把握能力。

【图 5-13 仓储管理与 Hub 作业台页面】

图 5-13 展示仓库、库位、入库、分拣、出库和发车等作业信息，用于说明管理员如何查看仓储与中转站运行情况。

【图 5-14 末端调度预览与配送批次详情页面】

图 5-14 展示 K-Means 聚类结果、LLM 建议面板、批次详情时间线与地图，用于体现城市末端调度功能。

【图 5-15 全国网络拓扑与运力规划结果页面】

图 5-15 展示全国 Hub 网络拓扑图、干线批次状态以及 MCMF 运力规划结果和模型分析文本，用于体现系统在全国物流网络层面的智能调度能力。

# 结论

## 6.1 总结

本文围绕“基于微服务架构的智能物流管理系统”完成了需求分析、系统设计、系统实现和功能展示等工作。针对传统物流系统在模块耦合、履约协同、路径规划和跨区域调度方面存在的问题，本文基于当前项目源码构建了一套覆盖顾客、商户、司机和管理员多角色的物流管理原型系统。

在架构方面，系统采用 Spring Boot、Spring Cloud 与 Spring Cloud Alibaba 构建微服务后端，实际划分为认证、用户、顾客、商店与库存、订单、司机、物流和网关等服务，并通过 Nacos、Spring Cloud Gateway、OpenFeign 与 RabbitMQ 完成服务发现、统一入口、同步调用和异步解耦。数据层使用 MySQL 与 MyBatis-Plus，缓存层使用 Redis，认证采用 BCrypt 与 JWT。前端采用 Vue3、Vite、TypeScript、Element Plus、Pinia 和 Leaflet 构建多角色单页应用，并通过 Docker Compose 完成数据库、中间件、后端服务和前端容器的统一部署。

在业务功能方面，系统实现了注册登录、商户与司机审核、顾客地址管理、商品浏览、电商下单、个人寄件、商户发货、库存扣减、仓储作业、司机接单、状态推进、GPS 上报、订单轨迹查询和订单签收等流程。订单服务通过同步校验和异步消息把顾客、商户、库存、配送和物流服务连接起来，使订单能够进入较完整的履约闭环。

在智能导航与调度方面，系统将 `logistics-service` 作为核心承载模块，集成 GraphHopper 读取 OpenStreetMap 路网数据，并在底层道路图上实现 A\* 路径规划；在此基础上提供 LLM 辅助候选路线裁决和战略路点建议。城市末端调度中，系统使用 K-Means 对订单进行空间聚类，并结合规则或大语言模型生成批次建议。跨城干线场景中，系统基于全国 Hub 网络实现多商品最小费用流规划，并将规划结果转化为干线批次和可查询的运力分配结果。

总体来看，本系统已经具备从订单创建、仓库发货、调度池入池、干线规划、Hub 到达、末端配送到物流追踪的主要链路。与普通信息管理系统相比，本文的主要价值在于把微服务工程实践与智能导航、聚类调度、全国运力规划结合起来，验证了这些技术在物流管理场景中协同落地的可行性。

## 6.2 不足与展望

受毕业设计周期、数据来源和运行环境限制，当前系统仍存在一些不足。

首先，系统的数据真实性仍有提升空间。虽然路径规划使用了 OSM 路网数据，地址和仓库坐标也接入了地理编码，但订单密度、司机轨迹、干线需求和部分历史交通数据仍以演示或测试数据为主，尚未与真实物流平台持续联动。

其次，算法能力仍偏原型化。A\* 路径规划当前以距离为主要代价，历史交通只在 LLM 策略中参与辅助判断；K-Means 聚类能够完成空间分组，但尚未充分考虑车辆容量、时间窗、司机工作时长等约束；全国运力规划已经采用多商品流思路，但成本和容量参数仍主要来自静态配置或有限历史信息。

再次，生产级部署和可靠性仍需加强。当前 Docker Compose 适合开发和演示，但无法完全替代 Kubernetes 等生产编排平台；跨服务一致性主要依赖 RabbitMQ、重试和死信队列，尚未形成完整的 Saga 补偿机制；系统监控、链路追踪、日志聚合和自动化告警也有进一步完善空间。

最后，测试体系还不够系统。当前系统可以完成主要功能演示和部分性能验证，但在自动化回归测试、安全测试、异常链路测试、长时间稳定性测试和极端并发测试方面仍不充分。

后续工作可以从以下方向展开：一是接入更真实的订单、定位、库存和路况数据，提高调度结果可信度；二是引入车辆路径问题、时间窗约束和容量约束，提升末端配送优化能力；三是将部署架构迁移到 Kubernetes，并补充监控、日志和链路追踪；四是完善事务补偿、权限审计和自动化测试；五是继续探索大语言模型在路线解释、调度复盘、异常预警和运营建议中的应用，使系统从“可执行的物流平台”进一步发展为“可解释、可优化的智能物流平台”。

# 参考文献

1.  中华人民共和国国家发展和改革委员会. 《“十四五”现代物流发展规划》\[R\]. 2022.

2.  WANG L, ZHANG Y, GAO P. Research on Intelligent Logistics System Based on IoT Technology\[C\]//Journal of Physics: Conference Series. IOP Publishing, 2021, 1881(4): 042047.

3.  LI J, WANG F. Big Data Analytics for Intelligent Logistics Systems: A Review\[J\]. IEEE Access, 2020, 8: 188485-188499.

4.  HE Z, AI Q, LI Y. Artificial Intelligence in Logistics and Supply Chain Management: A Systematic Literature Review\[J\]. International Journal of Logistics Research and Applications, 2022, 25(11-12): 1285-1311.

5.  NEWMAN S. Building Microservices: Designing Fine-Grained Systems\[M\]. Sebastopol: O'Reilly Media, 2015.

6.  LEWIS J, FOWLER M. Microservices: a definition of this new architectural term\[EB/OL\]. (2014-03-25) \[2025-05-04\]. https://martinfowler.com/articles/microservices.html.

7.  RICHARDSON C. Microservices Patterns: With examples in Java\[M\]. Shelter Island: Manning Publications, 2019.

8.  ZIMMERMANN O. Microservices tenets\[J\]. Computer Science-Research and Development, 2017, 32(3-4): 301-310.

9.  BALALAIE A, HEYDARNOORI A, JAMSHIDI P. Microservices architecture enables devops: Migration to a cloud-native architecture\[J\]. IEEE Software, 2016, 33(3): 42-52.

10. CONWAY M E. How do committees invent?\[J\]. Datamation, 1968, 14(4): 28-31.

11. DRAGONI N, GIALLORENZO S, LAFUENTE A L, et al. Microservices: yesterday, today, and tomorrow\[M\]//Present and Ulterior Software Engineering. Cham: Springer, 2017: 195-216.

12. VILLAMIZAR M, GARCIA O, OCHOA L, et al. Cost comparison of running web applications in the cloud using monolithic, microservice, and AWS Lambda architectures\[J\]. Service Oriented Computing and Applications, 2017, 11(2): 233-247.

13. JOSUTTIS N M. SOA in Practice: The Art of Distributed System Design\[M\]. Sebastopol: O'Reilly Media, 2007.

14. SHARMA V, SINGH S. A Comparative Study of Message Queuing Systems: Kafka, RabbitMQ, and ActiveMQ\[C\]//2021 International Conference on Computing, Communication, and Intelligent Systems (ICCCIS). Greater Noida: IEEE, 2021: 104-109.

15. KLEPPMANN M. Designing Data-Intensive Applications\[M\]. Sebastopol: O'Reilly Media, 2017.

16. 张亮, 屈钢, 吴D. 大型网站技术架构:核心原理与案例分析\[M\]. 北京: 电子工业出版社, 2013.

17. HUMBLE J, FARLEY D. Continuous Delivery: Reliable Software Releases through Build, Test, and Deployment Automation\[M\]. Boston: Addison-Wesley Professional, 2010.

18. 陈皓. 消费者驱动的契约测试 (Consumer-Driven Contract Testing)\[EB/OL\]. (2016-09-08) \[2025-05-04\]. https://coolshell.cn/articles/17670.html.

19. 贾志刚, 王志刚, 李娜. 基于微服务架构的物流运输管理系统设计\[J\]. 软件导刊, 2018, 17(11): 104-107.

20. 王珊珊, 孙力. 基于SpringBoot的物流管理系统设计与实现\[J\]. 电脑知识与技术, 2021, 17(23): 88-90.

21. RODRIGUES F, RUIZ L, SICILIA M Á. API Gateways: Taxonomy, Survey, and Challenges\[M\]//Service-Oriented Computing – ICSOC 2018 Workshops. Cham: Springer, 2019: 171-183.

22. MICHAEL T N. Release It!: Design and Deploy Production-Ready Software\[M\]. 2nd ed. Raleigh: Pragmatic Bookshelf, 2018.

23. EVANS E. Domain-Driven Design: Tackling Complexity in the Heart of Software\[M\]. Boston: Addison-Wesley Professional, 2003.

24. 胡强. MySQL数据库常见问题分析与研究\[J\]. 电脑编程技巧与维护, 2019(12): 91-92.

25. ARMS A, HLIBOWICKI A, NORMAN B, et al. R2DBC: Reactive Relational Database Connectivity\[J\]. Queue, 2021, 19(1): 48-69.

26. LUKAS M A, DE GOEDE R. Hands-On Reactive Programming in Spring 5\[M\]. Birmingham: Packt Publishing, 2018.

27. STEFANE M, LUEKE K. Reactive Spring\[M\]. Shelter Island: Manning Publications, 2021.

28. 邱书洋. Redis缓存技术研究及应用\[D\]. 郑州: 郑州大学, 2016.

29. 杨洪. 基于Redis的共享缓存分析\[J\]. 数字技术与应用, 2024, 42(01): 110-112.

30. MERKEL D. Docker: lightweight linux containers for consistent development and deployment\[J\]. Linux Journal, 2014, 2014(239): 2.

**  
**

# 致谢

感谢我的导师牛志华老师，在毕业设计期间对我悉心指导，帮助我修改完成本论文。是您的包容与鼓励，帮助我在艰难时刻克服所有这些苦难。

感谢各位任课老师引导我走上计算机领域的专业道路。

感谢辅导员的关心与帮助，让我不惧失败。

感谢父母多年的养育与栽培。

感谢男朋友一路上的精神支持。

感谢朋友们的陪伴。

付苗

上海大学

2025年05月17日

![](media/image8.jpeg)

